# 一、先搞懂：什么是代理（Proxy）

代理就是**中间人**：

客户端 ↔ 代理 ↔ 服务端

代理负责转发请求和响应，根据**代理站在谁那边**，分为**正向代理**和**反向代理**。

# 二、正向代理 vs 反向代理（核心区别）

## 1. 正向代理（Forward Proxy）

**站在客户端一边，替客户端访问外部服务**

✅ 通俗比喻：**代购**

- 你想买国外商品，自己买不到
- 找代购帮你买，商家不知道真实买家是你
- **你知道目标是谁，目标不知道你是谁**

✅ 特点：

- 代理在**客户端一侧**
- 客户端**必须配置代理**
- 隐藏**客户端真实 IP**
- 服务端看到的是代理 IP

✅ 典型场景：

- 翻墙 / 科学上网
- 公司内网访问外网
- 访问受限资源

## 2. 反向代理（Reverse Proxy）

**站在服务端一边，替服务器接收请求**

✅ 通俗比喻：**公司前台**

- 你找公司办事，只找前台
- 你不知道具体是哪个部门 / 员工处理
- **你不知道真实后端是谁，后端也不知道你**

✅ 特点：

- 代理在**服务端一侧**
- 客户端**无需任何配置**
- 隐藏**后端服务器真实地址**
- 客户端只访问代理地址

✅ 典型场景：

- 负载均衡
- 动静分离
- SSL 卸载
- 接口网关

## 3. 一张表彻底分清

|     维度     |     正向代理      |        反向代理        |
| :----------: | :---------------: | :--------------------: |
|   代理位置   |    客户端一侧     |       服务端一侧       |
|  谁配置代理  |    客户端配置     |       服务端配置       |
|   隐藏对象   | 隐藏**客户端 IP** |   隐藏**后端服务器**   |
|  客户端认知  |  知道目标服务器   |     不知道真实后端     |
| Nginx 常用度 |     极少使用      | **核心功能，大量使用** |

# 三、Nginx 和 代理的关系

Nginx 最核心的能力之一就是**高性能反向代理服务器**，通过 `ngx_http_proxy_module` 模块实现。

## Nginx 反向代理最常用指令：

```nginx
proxy_pass http://后端地址;
```

## 实战示例：代理后端 Tomcat/Node.js

```nginx
server {
    listen 80;
    server_name example.com;

    location /api/ {
        # 反向代理到后端服务
        proxy_pass http://127.0.0.1:8080/api/;
        
        # 传递真实客户端IP给后端
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

## Nginx 反向代理的作用

1. **负载均衡**：分发请求到多台后端服务器
2. **隐藏后端**：提高安全性，不暴露真实服务地址
3. **动静分离**：静态资源 Nginx 直接返回，动态请求转发后端
4. **SSL 卸载**：Nginx 处理 HTTPS，后端用 HTTP
5. **缓存加速**：缓存静态内容，减轻后端压力

## Nginx 正向代理（极少用）

Nginx 也能做正向代理，但需要配置 DNS 解析，一般不用：

```nginx
location / {
    resolver 8.8.8.8;
    proxy_pass $scheme://$host$request_uri;
}
```

# 四、总结（面试必背）

1. **正向代理**：替**客户端**访问外网，隐藏客户端
2. **反向代理**：替**服务端**接收请求，隐藏后端
3. **Nginx ≈ 反向代理神器**，正向代理几乎不用
4. 核心指令：`proxy_pass`，面试常考斜杠问题

如果你需要，我还可以给你讲：

- proxy_pass 末尾**带斜杠和不带斜杠的区别**（面试高频坑）
- Nginx 负载均衡完整配置
- 网关层反向代理实战方案

# 深入：Nginx 的 Proxy 与 ES6 的 Proxy

这两个虽然**名字都叫 Proxy（代理）**，但**完全不是一个东西**，一个是**网络代理**，一个是**JS 对象代理**。

------

# 一、先一句话分清

- **Nginx Proxy**：**网络层面的代理**，转发 HTTP 请求，管的是**服务器之间的通信**。
- **ES6 Proxy**：**语言层面的代理**，拦截 JS 对象操作，管的是**代码里的对象行为**。

------

# 二、深入 Nginx Proxy（反向代理核心）

Nginx 的 Proxy 就是**反向代理**，是后端架构最核心的组件。

## 1. 核心作用

- 接收客户端请求
- 转发给后端服务（Tomcat/Node/Java/Golang）
- 把结果返回给客户端
- **隐藏真实服务器**、做负载均衡、缓存、限流、熔断

## 2. 最核心指令：proxy_pass

```nginx
location /api {
    proxy_pass http://127.0.0.1:3000;
}
```

### 深入重点：**带斜杠 / 与不带斜杠的区别（面试必问）**

- **不带斜杠**：`/api` 会被一起转发
- **带斜杠**：`/api` 会被去掉

例：

```nginx
proxy_pass http://localhost:3000;
请求 /api/user → 转发到 http://localhost:3000/api/user

proxy_pass http://localhost:3000/;
请求 /api/user → 转发到 http://localhost:3000/user
```

## 3. Nginx Proxy 必须加的请求头

```nginx
proxy_set_header Host $host;
proxy_set_header X-Real-IP $remote_addr;        # 真实客户端IP
proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
```

作用：让后端**拿到真实用户 IP**。

## 4. Nginx Proxy 高级功能

- 负载均衡（round_robin /ip_hash/least_conn）
- 动静分离
- HTTPS 卸载
- 缓存加速
- 限流、熔断、白名单

------

# 三、深入 ES6 Proxy（JS 对象代理）

ES6 的 Proxy 是**JavaScript 语言层面的代理**，用来**拦截对象的任何操作**。

## 1. 核心概念

- 目标对象（target）
- 处理器（handler）：里面放**拦截方法**
- 代理对象（proxy）

语法：

```js
const proxy = new Proxy(target, handler)
```

## 2. 能拦截什么？（13 种捕获器）

最常用：

- `get`：读取属性
- `set`：设置属性
- `deleteProperty`：删除属性
- `apply`：调用函数
- `has`：in 操作符

## 3. 最简单示例

```js
const target = { name: "小明" }

const proxy = new Proxy(target, {
  get(target, prop) {
    console.log("读取了属性：" + prop)
    return target[prop]
  },
  set(target, prop, value) {
    console.log("设置了属性：" + prop)
    target[prop] = value
  }
})

proxy.name  // 触发 get
proxy.age = 18 // 触发 set
```

## 4. 实际用途（非常强大）

- **数据响应式**（Vue3 的核心原理）
- 表单验证
- 函数防抖 / 节流包装
- 日志监控
- 防篡改对象
- 兼容层、适配器

## 5. 深入：Vue3 响应式本质

```js
function reactive(target) {
  return new Proxy(target, {
    get(target, key) {
      track() // 收集依赖
      return target[key]
    },
    set(target, key, value) {
      target[key] = value
      trigger() // 触发更新
      return true
    }
  })
}
```

**Vue3 响应式 = ES6 Proxy**

------

# 四、最核心对比（一定要记住）

| 维度 | Nginx Proxy | ES6 Proxy |

|------+-------------+-----------|

| 类型 | **网络代理** | **语言代理** |

| 作用 | 转发 HTTP 请求 | 拦截对象操作 |

| 层面 | 服务 / 网络 | JavaScript 语法 |

| 目标 | 客户端 ←→ 服务器 | JS 对象、数组、函数 |

| 用途 | 反向代理、负载均衡 | 响应式、监控、验证 |

| 核心 | proxy_pass | get /set |

| 隐藏 | 隐藏服务器 | 隐藏对象内部逻辑 |

------

# 五、最终总结（最精炼）

- **Nginx Proxy**：网络请求的中介，**转发请求**。
- **ES6 Proxy**：JS 对象的中介，**拦截操作**。
- Nginx 是**后端架构神器**，ES6 Proxy 是**前端框架核心**。
- 名字相同，领域完全不同。

------

