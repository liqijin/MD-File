# 一、开发环境与基础配置坑

1. **AppID 混淆**
- 测试用测试号，上线必须用正式 AppID，否则无法发布、支付、获取用户信息。
   - 不同环境（开发 / 体验 / 正式）AppID 不同，配置文件写错会导致接口、登录异常。

2. **域名白名单没配置**
- 正式版必须配置**request、uploadFile、downloadFile、socket**合法域名，开发工具可关闭校验，但线上必报错。
   - 不支持`localhost`、`IP地址`，必须是备案域名且 HTTPS。

3. **服务器时间不同步**
- 小程序很多接口依赖时间戳，服务器和客户端时差大会导致登录态、签名、支付异常。
  

# 二、登录与用户体系坑

1. **wx.login 调用时机错误**
- 不能每次进入页面都调用，会导致**session_key 刷新**，解密手机号 / 用户信息失败。
   - 应在登录态失效时才重新登录。

2. **UnionID 和 OpenID 混淆**
- OpenID：同一用户在当前小程序唯一
   - UnionID：同一主体下多小程序 / 公众号互通唯一
- 未绑定开放平台拿不到 UnionID，会导致账号体系错乱。
  
3. **获取手机号权限收紧**
- 必须**按钮点击触发**才能调用，不能自动获取；且现在需要用户主动授权。
  

# 三、接口请求与数据坑

1. **request 并发限制**
- 微信小程序同时最多**5 个请求**，并发过多会被阻塞、超时。
   - 解决方案：请求队列、合并接口、防抖节流。

2. **POST 请求默认格式问题**
- 小程序默认`content-type: application/json`，后端如果用表单接收会拿不到参数。
   - 需要手动设置`header: {'content-type': 'application/x-www-form-urlencoded'}`。

3. **本地存储大小限制**
- `wx.setStorage` 上限**10MB**，超了会直接报错丢失数据。
   - 不要存大量列表、图片 base64。


# 四、页面与路由坑

1. **页面栈最多 10 层**
- 用`wx.navigateTo`不断跳转，超过 10 层会卡死、无法跳转。
   - 列表进详情这类场景建议用`redirectTo`或`navigateBack`。

2. **onLoad /onShow 执行时机混淆**
- onLoad：页面创建只执行一次
- onShow：每次显示都执行
   - 列表刷新、返回刷新逻辑写在 onLoad 会不生效。
   
3. **scroll-view 与页面滚动冲突**

   - 页面开启下拉刷新时，scroll-view 容易出现滚动异常、无法触发下拉。
- 需关闭页面滚动或单独处理 scroll 的下拉刷新。
  

# 五、样式与渲染坑

1. **rpx 适配误区**
- rpx 是根据 750px 设计稿适配，**图片宽高写 rpx，边框、字体建议用 px**。
   - iPad 等设备上 rpx 会被放大，导致样式错乱。

2. **自定义组件样式隔离**
- 组件默认样式隔离，外部样式无法穿透，需开启`styleIsolation: 'shared'`。
   - 使用`externalClasses`实现父传样式。

3. **setData 大数据量性能爆炸**
- 一次性 setData 大量数据（如长列表）会导致卡顿、白屏。
   - 禁止 setData 整个数组，应局部更新、分页加载。


# 六、原生能力与设备坑

1. **权限申请逻辑不完整**
- 定位、相册、相机等权限，用户拒绝后**再次调用不会弹窗**，只能引导去设置页手动开启。
   - 必须做权限状态判断与引导逻辑。

2. **不同平台 API 差异**
- iOS/Android 表现不一致：如键盘弹出顶起页面、选择文件、录音时长限制。
   - 部分 API 仅支持移动端，开发者工具模拟不准确。

3. **canvas 渲染与导出问题**
- 画布宽高必须用 px，不能用 rpx；
   - 生成海报时网络图片需先下载到本地；
- 不同机型字体、位置偏移严重。
  

# 七、支付与业务坑

1. **支付参数签名错误**
- 签名大小写、参数顺序、编码问题是最常见坑。
   - 前端不能参与签名，必须后端生成`paySign`。

2. **支付结果不可信**
- 前端支付成功回调**不能作为业务成功依据**，必须依靠微信支付回调通知。
  
3. **类目与资质不匹配**
- 支付、直播、电商、医疗等需要特殊类目和资质，否则审核被拒、功能封禁。
  

# 八、审核发布坑

1. **微信审核严格场景**
- 诱导分享、诱导关注、测试内容、无实质内容、外链引导 APP 都会被拒。
   - 不能出现 “最、第一、顶级” 等极限词。

2. **提交审核前未关闭调试**
- 体验版带调试标记，正式版必须关闭调试开关。
  
3. **版本更新不及时**
- 小程序有缓存机制，更新后用户可能还看到旧版，需做版本检测强制更新。
  

# 九、性能与体验优化坑

1. **包体积超限**
- 主包**2MB 限制**，超了无法发布，必须做**分包加载**。
   - 图片、资源尽量放 CDN，不要打包进去。

2. **首屏加载慢**
- 接口过多、资源过大、同步请求阻塞会导致白屏。
   - 做骨架屏、分包预加载、接口懒加载。

3. **内存泄漏**
- 定时器、监听事件（onPageScroll、网络状态）在页面卸载时未销毁，导致卡顿闪退。
  

# 十、运营与合规坑

1. **用户协议与隐私政策缺失**
- 必须有独立隐私政策页面，收集用户信息前要告知，否则审核必拒。
  
2. **分享功能滥用**
- 不能强制分享、不能用分享解锁功能，否则被封禁。
  
3. **模板消息 / 订阅消息规则变更**
- 一次性订阅、长期订阅权限控制严格，滥用会被限制发送能力。



# 十一、支付

### 一、微信小程序支付完整流程（核心版）

我会用「前端 + 后端」的协作视角，拆解最标准、避坑的支付流程，同时标注关键坑点：

#### 整体流程（8 步核心）

```
graph TD
    A[用户触发支付] --> B[前端调后端：获取支付参数]
    B --> C[后端：调用微信统一下单API]
    C --> D[微信返回prepay_id等参数]
    D --> E[后端：生成支付签名返回给前端]
    E --> F[前端：调wx.requestPayment发起支付]
    F --> G{支付结果}
    G -->|用户支付成功| G1[微信异步通知后端回调地址]
    G -->|用户取消/失败| G2[前端提示失败，流程结束]
    G1 --> H[后端验证回调签名，更新订单状态]
    H --> I[后端回复微信：SUCCESS（必须）]
```

------

### 二、分角色详细实现（附核心代码）

#### 1. 前端（小程序）流程

**核心步骤**：触发支付 → 拿后端支付参数 → 调支付 API → 处理前端回调（仅提示，不做业务逻辑）

```js
// 1. 点击支付按钮触发
async function payOrder() {
  try {
    // 加载中提示，避免重复点击
    wx.showLoading({ title: '支付中...' });

    // 2. 第一步：调用后端接口，获取支付参数（关键：不传敏感信息，如金额）
    const res = await wx.request({
      url: 'https://你的域名/api/pay/getPayParams', // 后端接口
      method: 'POST',
      data: {
        orderId: 'xxx123456', // 仅传订单ID，金额由后端校验
        openid: '用户的openid' // 登录时已获取，前端缓存
      }
    });

    if (res.data.code !== 200) {
      wx.showToast({ title: res.data.msg, icon: 'none' });
      return;
    }

    // 3. 第二步：调用微信支付API（后端返回的参数直接用）
    const payResult = await wx.requestPayment({
      timeStamp: res.data.data.timeStamp, // 后端生成，字符串类型（坑点！）
      nonceStr: res.data.data.nonceStr,
      package: res.data.data.package, // 格式：prepay_id=xxx
      signType: 'RSA', // 新版推荐RSA，旧版MD5（后端统一）
      paySign: res.data.data.paySign, // 后端签名，前端绝不参与
      success: (res) => {}, // 新版Promise写法可省略
      fail: (err) => {}
    });

    // 4. 前端支付成功回调（仅提示，不更新订单！）
    wx.hideLoading();
    wx.showToast({ title: '支付提交成功，请等待结果' });
    // 跳转到订单详情页（由详情页拉取后端最新订单状态）
    wx.navigateTo({ url: `/pages/order/detail?orderId=xxx123456` });

  } catch (err) {
    wx.hideLoading();
    // 支付失败/取消的处理
    if (err.errMsg === 'requestPayment:fail cancel') {
      wx.showToast({ title: '你取消了支付', icon: 'none' });
    } else {
      wx.showToast({ title: `支付失败：${err.errMsg}`, icon: 'none' });
    }
  }
}
```

**前端关键坑点**：

- `timeStamp` 必须是**字符串**（后端返回数字会导致签名失败）；
- 前端绝不处理「订单支付成功」的业务逻辑（如改状态、发货），仅做提示；
- 禁止在前端拼接支付参数、生成签名（安全风险 + 签名必错）。

#### 2. 后端流程（以 Java 为例，核心逻辑）

**核心步骤**：接收前端订单 ID → 校验订单（金额 / 状态）→ 调微信统一下单 → 生成支付签名返回前端 → 处理微信异步回调

##### 步骤 1：调用微信「统一下单 API」获取 prepay_id

```java
// 1. 构造统一下单参数（关键：所有参数需按微信规则排序签名）
Map<String, String> params = new HashMap<>();
params.put("appid", "你的小程序AppID");
params.put("mch_id", "你的商户号");
params.put("nonce_str", WXPayUtil.generateNonceStr()); // 随机字符串
params.put("body", "商品描述：XX商品"); // 长度限制，不能有特殊字符
params.put("out_trade_no", "xxx123456"); // 商户订单号（唯一）
params.put("total_fee", "100"); // 金额，单位分（坑点！必须是整数）
params.put("spbill_create_ip", "用户IP"); // 前端传或后端获取
params.put("notify_url", "https://你的域名/api/pay/notify"); // 支付回调地址（必须HTTPS）
params.put("trade_type", "JSAPI"); // 小程序固定JSAPI
params.put("openid", "用户的openid"); // 小程序支付必须传openid

// 2. 生成签名（微信官方工具类，避免自己写）
String sign = WXPayUtil.generateSignature(params, "你的商户密钥", WXPayConstants.SignType.MD5);
params.put("sign", sign);

// 3. 调用微信统一下单接口（POST，XML格式）
String xmlResult = wxPayService.unifiedOrder(params);
Map<String, String> resultMap = WXPayUtil.xmlToMap(xmlResult);

// 4. 校验统一下单结果
if ("SUCCESS".equals(resultMap.get("return_code")) && "SUCCESS".equals(resultMap.get("result_code"))) {
    String prepayId = resultMap.get("prepay_id");
    // 生成给前端的支付参数（需再次签名）
    Map<String, String> payParams = new HashMap<>();
    payParams.put("appId", "你的小程序AppID");
    payParams.put("timeStamp", String.valueOf(System.currentTimeMillis() / 1000)); // 秒级时间戳
    payParams.put("nonceStr", WXPayUtil.generateNonceStr());
    payParams.put("package", "prepay_id=" + prepayId); // 固定格式
    payParams.put("signType", "RSA"); // 与统一下单一致

    // 5. 生成paySign（前端调支付的签名，关键！）
    String paySign = WXPayUtil.generateSignature(payParams, "你的API证书私钥", WXPayConstants.SignType.RSA);
    payParams.put("paySign", paySign);

    // 6. 返回给前端
    return Result.success(payParams);
} else {
    // 统一下单失败
    return Result.fail(resultMap.get("err_code_des"));
}
```

##### 步骤 2：处理微信异步回调（最核心，订单状态唯一依据）

```java
/**
 * 微信支付回调接口（POST，XML格式）
 * 坑点：必须返回XML格式的SUCCESS，否则微信会反复回调（最多8次）
 */
@PostMapping("/api/pay/notify")
public String payNotify(HttpServletRequest request) {
    try {
        // 1. 读取微信回调的XML数据
        String xmlData = WXPayUtil.readXmlFromRequest(request);
        Map<String, String> notifyMap = WXPayUtil.xmlToMap(xmlData);

        // 2. 第一步：验证签名（防止伪造回调）
        if (!WXPayUtil.isSignatureValid(notifyMap, "你的商户密钥")) {
            log.error("支付回调签名验证失败：{}", xmlData);
            return WXPayUtil.failXml("签名验证失败"); // 返回失败XML
        }

        // 3. 第二步：校验基本参数
        if (!"SUCCESS".equals(notifyMap.get("return_code"))) {
            log.error("支付回调通信失败：{}", notifyMap.get("err_code_des"));
            return WXPayUtil.failXml("通信失败");
        }
        if (!"SUCCESS".equals(notifyMap.get("result_code"))) {
            log.error("支付回调业务失败：{}", notifyMap.get("err_code_des"));
            return WXPayUtil.failXml("业务失败");
        }

        // 4. 第三步：核心业务校验（防重复回调、金额一致）
        String outTradeNo = notifyMap.get("out_trade_no"); // 商户订单号
        String totalFee = notifyMap.get("total_fee"); // 微信返回的金额（分）
        String transactionId = notifyMap.get("transaction_id"); // 微信订单号

        // 4.1 查本地订单（必须加锁，防重复回调）
        Order order = orderService.getByOrderNo(outTradeNo);
        if (order == null) {
            log.error("订单不存在：{}", outTradeNo);
            return WXPayUtil.failXml("订单不存在");
        }
        // 4.2 校验金额（本地订单金额 vs 微信回调金额）
        if (!String.valueOf(order.getAmount() * 100).equals(totalFee)) { // 本地金额是元，转分
            log.error("金额不一致：订单{}，本地{}，微信{}", outTradeNo, order.getAmount(), totalFee);
            return WXPayUtil.failXml("金额不一致");
        }
        // 4.3 校验订单状态（防重复处理）
        if (order.getStatus() == 2) { // 已支付
            log.info("订单已支付，无需重复处理：{}", outTradeNo);
            return WXPayUtil.successXml(); // 必须返回成功
        }

        // 5. 处理业务逻辑（更新订单状态、扣库存、发通知等）
        orderService.updateOrderStatus(outTradeNo, 2, transactionId); // 改为已支付
        // 其他逻辑：如给用户加积分、推送消息等

        // 6. 必须返回SUCCESS XML（微信收到才会停止回调）
        return WXPayUtil.successXml();

    } catch (Exception e) {
        log.error("支付回调处理异常", e);
        // 异常时返回失败，微信会重试
        return WXPayUtil.failXml("处理异常");
    }
}

// 工具类辅助方法（生成成功/失败XML）
public static String successXml() {
    return "<xml><return_code><![CDATA[SUCCESS]]></return_code><return_msg><![CDATA[OK]]></return_msg></xml>";
}
public static String failXml(String msg) {
    return String.format("<xml><return_code><![CDATA[FAIL]]></return_code><return_msg><![CDATA[%s]]></return_msg></xml>", msg);
}
```

------

### 三、支付回调核心规则（避坑关键）

1. **回调触发时机**：用户支付成功后，微信会立即异步调用你配置的`notify_url`，如果回调失败（如返回非 SUCCESS、超时、签名错误），微信会**重试 8 次**（间隔：15s/15s/30s/3m/10m/20m/30m/30m），直到成功或超时。

2. 回调必须满足的条件：

   - 接口必须是**POST**，接收 XML 格式数据，返回 XML 格式数据；
- 地址必须是**HTTPS**，且在商户平台配置（或统一下单时指定）；
   - 处理时间必须≤5s（超时微信判定失败，会重试）；
   - 无论处理成功 / 失败，都必须返回 XML（SUCCESS/FAIL），不能返回 JSON / 空值。
   
3. 防重复回调处理：

   - 订单表加「支付状态」+「回调次数」字段；
- 处理回调时加**分布式锁**（如 Redis 锁），同一订单同一时间仅处理一次；
   - 处理完成后标记订单为「已支付」，后续重复回调直接返回 SUCCESS。
   

------

### 四、核心坑点总结

1. **金额单位**：微信支付金额是「分」，前端传元、后端忘转分会导致金额错误（如 1 元 = 100 分）；

2. 签名问题：

   - 签名参数必须按 ASCII 码排序，缺少参数 / 参数值错误 / 密钥错误都会导致签名失败；
- 前端`timeStamp`必须是字符串，后端返回数字会导致支付失败；
  
3. 回调逻辑：

   - 前端支付成功回调≠订单真的成功，唯一依据是微信异步回调；
- 回调必须返回 XML 格式的 SUCCESS，否则微信会反复重试；
  
4. **订单唯一性**：商户订单号（out_trade_no）必须唯一，重复会导致统一下单失败；

5. **环境问题**：测试用微信支付沙箱环境，正式环境必须用商户真实密钥 / 证书，且回调地址必须备案 + HTTPS。

### 总结

1. 小程序支付核心是「前端发起请求→后端调微信统一下单→前端调支付 API→微信异步回调后端」，前端仅做交互，后端负责核心校验和状态更新；
2. 支付回调是订单状态的唯一依据，必须验证签名、防重复处理、返回指定 XML 格式；
3. 签名、金额单位、回调返回格式是最容易踩坑的三个点，需严格按微信文档实现。

# 小程序生命周期完整使用指南

小程序生命周期分为**三大核心类型**，是小程序运行的核心机制，掌握它才能正确处理页面加载、数据请求、页面显示 / 隐藏、销毁等逻辑。

我会用**最实用、可直接复制**的写法，带你从零学会使用。

## 一、先分清 3 种生命周期

1. **全局生命周期**（`app.js`）：整个小程序的启动、隐藏、报错
2. **页面生命周期**（页面`.js`）：单个页面的加载、显示、隐藏、卸载
3. **组件生命周期**（自定义组件）：组件的创建、销毁（极少用）

------

## 二、全局生命周期（app.js）

**作用**：控制**整个小程序**的生命周期，只在`app.js`写一次。

### 常用钩子（方法）

- `onLaunch`：小程序**初始化完成时触发**（全局只触发 1 次）
- `onShow`：小程序**启动 / 从后台切回前台**时触发
- `onHide`：小程序**从前台切到后台**时触发
- `onError`：小程序**发生脚本错误**时触发

### 实战代码

```javascript
// app.js
App({
  // 1. 小程序初始化完成（只执行1次，最常用！）
  onLaunch() {
    console.log('✅ 小程序启动了')
    // 这里适合写：获取用户信息、登录、全局数据初始化、云开发初始化
  },

  // 2. 小程序显示（切回前台）
  onShow() {
    console.log('📱 小程序回到前台')
    // 这里适合：刷新全局数据、恢复定时器
  },

  // 3. 小程序隐藏（切到后台）
  onHide() {
    console.log('💤 小程序进入后台')
    // 这里适合：清除定时器、暂停播放、保存临时数据
  },

  // 4. 小程序报错
  onError(err) {
    console.error('❌ 小程序出错：', err)
    // 这里适合：错误上报、日志收集
  }
})
```

------

## 三、页面生命周期（页面.js）

**作用**：控制**单个页面**的加载、渲染、显示、销毁，**最常用、最重要**。

### 常用钩子（方法）

- `onLoad`：页面**加载时触发**（只 1 次）
- `onShow`：页面**显示 / 从其他页返回**时触发
- `onReady`：页面**初次渲染完成**时触发（只 1 次）
- `onHide`：页面**隐藏 / 跳转到其他页**时触发
- `onUnload`：页面**卸载 / 返回销毁**时触发

### 实战代码（直接复制用）

```javascript
// pages/index/index.js
Page({
  // 1. 页面加载（只执行1次，请求数据首选！）
  onLoad(options) {
    console.log('📥 页面加载', options) 
    // options 是页面跳转传参：url?name=xxx&id=xxx
    // 这里适合：发送网络请求、获取参数、初始化数据
  },

  // 2. 页面显示（每次打开/返回都会执行）
  onShow() {
    console.log('👀 页面显示')
    // 这里适合：刷新列表、恢复状态、检查登录
  },

  // 3. 页面渲染完成（只1次）
  onReady() {
    console.log('🎨 页面渲染完毕')
    // 这里适合：操作DOM、设置导航栏标题
  },

  // 4. 页面隐藏（跳走/切后台）
  onHide() {
    console.log('🙈 页面隐藏')
    // 这里适合：暂停播放、清除定时器
  },

  // 5. 页面卸载（返回销毁）
  onUnload() {
    console.log('🗑️ 页面销毁')
    // 这里适合：清理定时器、取消请求、释放资源
  }
})
```

------

## 四、生命周期执行顺序（必背）

### 打开小程序

```
onLaunch(全局)` → `onShow(全局)` → `onLoad(页面)` → `onShow(页面)` → `onReady(页面)
```

### 页面跳转（A → B）

```
A.onHide` → `B.onLoad` → `B.onShow` → `B.onReady
```

### 返回页面（B → A）

```
B.onUnload` → `A.onShow
```

### 切后台 / 切回前台

`onHide(全局)` ← 切后台

`onShow(全局)` ← 切前台

------

## 五、最实用的业务场景

1. **发送网络请求** → 写在 `onLoad`
2. **页面返回刷新数据** → 写在 `onShow`
3. **清除定时器 / 防止内存泄漏** → 写在 `onUnload`
4. **全局登录 / 初始化** → 写在 `onLaunch`
5. **切后台暂停播放** → 写在 `onHide`

------

## 六、快速记忆口诀

- **全局管整个小程序**
- **页面管单个页面**
- **onLoad 只一次，onShow 每次都来**
- **请求放 onLoad，刷新放 onShow**

------

### 总结

1. 全局生命周期在`app.js`，控制小程序启动、后台、报错
2. 页面生命周期在页面`.js`，`onLoad`/`onShow`是开发核心
3. 执行顺序固定：加载 → 显示 → 渲染 → 隐藏 → 卸载
4. 数据请求放`onLoad`，页面刷新放`onShow`
