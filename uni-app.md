# 一、面试速背版（一页纸精简，直接背）

## （一）核心原理（高频）

- 架构：分层架构+适配器模式+条件编译，一套代码→框架层→各端适配器→原生/Web渲染
- 渲染：App端（非自定义原生/自定义webview，nvue纯原生）、小程序原生渲染、H5 DOM渲染
- 编译：开发时webpack/vite编译，发布时按平台生成对应代码
- 生命周期：onLaunch（APP全局）→onLoad→onShow→onReady→onHide/onUnload（页面）；组件=Vue生命周期+页面生命周期

## （二）高频面试题（标准答案）

- \1. uni-app 定义：基于Vue的跨端框架，一套代码编译到iOS、Android、H5、多端小程序
- \2. 优势：Vue语法（学习成本低）、多端兼容、生态丰富、App性能接近原生
- \3. nvue vs vue：nvue=原生渲染（性能高、仅flex布局、css有限），vue=跨端兼容（灵活）
- \4. 数据通信：uni.$emit/$on、Vuex/Pinia、路由传参、uni.setStorageSync
- \5. 条件编译：用// #ifdef 平台 标记，支持APP-PLUS、MP-WEIXIN、H5等
- \6. 打包：小程序→对应原生代码；App→云端/本地打包；H5→静态网站
- \7. 性能优化：图片懒加载+压缩、分包加载、避免频繁setData、nvue长列表、减少页面层级

## （三）核心考点总结

核心：一套代码+条件编译=多端运行；重点：架构、生命周期、通信、跨端适配、性能优化

# 二、实战避坑清单（开发速查，少踩坑）

## （一）样式坑

- 必须用rpx（750rpx=屏幕宽度），小程序不支持*、属性、>子选择器
- 页面样式默认不隔离，需加scoped；nvue仅支持flex布局，不支持百分比宽高（除屏幕）

## （二）生命周期坑

- Vue的created页面返回不重新执行，刷新需用onShow
- 小程序onLoad仅执行1次，onShow每次显示都执行

## （三）路由与页面栈坑

- 小程序页面栈最多10层，跳转用navigateTo（保留）、redirectTo（替换）、switchTab（tabbar）
- 传参过长丢失，需用encodeURIComponent编码

## （四）跨端兼容坑（最痛）

- 小程序不支持window、document、new Image；H5有跨域，小程序/APP无
- 各端API（相机、支付等）有差异，用条件编译+统一封装解决

## （五）图片与资源坑

- 小程序本地图片用相对路径，图片不显示需检查路径/Manifest权限
- 小程序头像/昵称需用button开放能力获取，不能直接调用API

## （六）缓存坑

- uni.setStorage是异步，同步用uni.setStorageSync
- 小程序本地存储上限10MB，不存大量数据（避免卡顿）

## （七）组件与调试坑

- 组件不能用页面生命周期（onLoad等），样式不继承页面样式
- 真机白屏：证书/权限/包名不匹配；小程序上传失败：体积超2M/未分包

## （八）避坑口诀（速记）

rpx定样式，onShow做刷新；路由不超10层，跨端编译来适配；长列表用nvue，分包解决2M限；同步存储用Sync，组件不碰页面周期



## 一、uni-app 核心开发原理（面试八股文重点）

### 1. 跨端架构设计（最核心）

uni-app 采用 **分层架构 + 适配器模式 + 条件编译** 实现一套代码多端运行：

```
你的代码 → uni-app 框架层 → 各端适配器 → 原生渲染/Web渲染
```

- **视图层**：小程序 / APP 用**原生渲染**，H5 用 webview 渲染
- **逻辑层**：统一基于 Vue 语法，抹平各端 API 差异
- 运行时：
  - 小程序：编译成微信 / 支付宝 / 抖音小程序原生代码
  - App（iOS/Android）：基于 **weex/skyline** 渲染引擎
  - H5：直接编译成标准 Vue 项目

### 2. 渲染原理

- App 端：
  - 非自定义组件：原生渲染（性能最优）
  - 自定义组件：webview 渲染（nvue 完全原生）
- **小程序端**：走小程序原生渲染机制
- **H5 端**：标准 DOM 渲染

### 3. 编译原理

- 开发时：`webpack/vite` 编译 + 各端专属插件处理
- 发布时：根据平台生成对应平台代码（条件编译生效）

### 4. 运行模式

- **开发模式**：热重载、sourceMap、调试日志
- **生产模式**：代码压缩、tree-shaking、日志清除

### 5. 生命周期执行顺序（高频面试）

**页面完整生命周期：**

1. `onLaunch`（APP 全局）
2. `onLoad` → `onShow` → `onReady`
3. 后退 / 隐藏：`onUnload` / `onHide`

**组件生命周期：**

Vue 生命周期（created/mounted）+ 页面生命周期（onShow 等）

------

## 二、uni-app 高频八股文（面试必背）

### 1. uni-app 是什么？

基于 Vue 的**跨端开发框架**，一套代码编译到：iOS、Android、H5、微信 / 支付宝 / 百度 / 抖音小程序、快应用等。

### 2. uni-app 相比原生小程序、Flutter、React Native 优势？

- 学习成本低（Vue 语法）
- 一套代码多端，效率极高
- 生态丰富、插件市场成熟
- App 性能接近原生，小程序体验一致

### 3. nvue 是什么？和 vue 页面区别？

- nvue = **native vue**，纯原生渲染页面
- 优点：性能极高、无滚动卡顿、可操作原生
- 缺点：只支持 flex 布局、css 支持有限、不能操作 DOM

### 4. uni-app 数据通信方式

1. `uni.$emit / $on` 全局事件
2. Vuex/Pinia 全局状态
3. 页面路由传参
4. 本地存储 `uni.setStorageSync`

### 5. 条件编译是什么？怎么用？

用特殊注释标记**只在某端运行**的代码：

```js
// #ifdef H5
console.log('只有H5可见')
// #endif
```

支持：APP-PLUS、MP-WEIXIN、MP-ALIPAY、H5 等

### 6. uni-app 打包原理

- 小程序：生成对应平台小程序代码
- APP：云端打包 / 本地离线打包，生成 iOS/Android 安装包
- H5：打包成静态网站

### 7. 如何优化 uni-app 性能？

- 图片懒加载、压缩、webp 格式
- 分包加载（小程序重点）
- 避免频繁 setData
- 使用 nvue 提升长列表性能
- 减少页面层级、避免复杂嵌套

------

## 三、uni-app 实战常见问题 + 痛点坑（开发必看）

### 1. 样式坑

- **单位必须用 rpx**，750rpx = 屏幕宽度
- 小程序不支持：`*` 选择器、属性选择器、`>` 子选择器
- 页面样式默认**不隔离**，想隔离用 `scoped`
- nvue 只支持 flex 布局，不支持百分比宽高（除了屏幕）

### 2. 生命周期坑

- **Vue 的 created 在页面返回时不会重新执行**
- 页面返回刷新必须用 `onShow`
- 小程序 `onLoad` 只执行一次，`onShow` 每次显示都执行

### 3. 路由与页面栈坑

- 小程序页面栈最多 **10 层**，超过会无法跳转

- 跳转用：

  - `uni.navigateTo`（保留页面）
  - `uni.redirectTo`（替换页面）
  - `uni.switchTab`（仅跳 tabbar）

  

- 传参过长会丢失，必须用 `encodeURIComponent`

### 4. 跨端兼容坑（最痛）

- 小程序不支持 `window`、`document`、`new Image`
- H5 有跨域，小程序 / APP 没有
- 各端 API 差异：相机、扫码、支付、分享
- 解决：**条件编译 + 封装统一方法**

### 5. 图片与资源坑

- 小程序本地图片**必须用相对路径**，不能用网络路径当本地
- 图片不显示：路径错误 / 未在 manifest 配置权限
- 小程序头像 / 昵称必须用 button 开放能力获取

### 6. 缓存坑

- `uni.setStorage` 是异步，**同步用 sync 版本**
- 小程序本地存储上限 10MB
- 不要存大量数据，会卡顿

### 7. 自定义组件坑

- 组件里**不能用页面生命周期**（onLoad 等）
- 组件样式不继承页面样式
- 小程序自定义组件名称不能和原生组件重名

### 8. 调试与打包坑

- 真机调试白屏：证书错误、权限缺失、包名不匹配
- 小程序上传失败：代码体积超 2M、未分包
- 云端打包慢、失败：依赖错误、插件冲突

------

## 四、高频面试题答案（直接背）

### 1. uni-app 运行原理？

基于 Vue 语法 + 适配器模式，编译时生成对应平台代码，运行时通过各自渲染引擎渲染，实现一套代码多端运行。

### 2. uni-app 和小程序原生开发区别？

- 跨端能力：uni-app 支持多端，原生只支持单端
- 语法：uni-app 用 Vue，原生用小程序语法
- 工程化：uni-app 支持 npm、分包、脚手架，原生较弱
- 生态：uni-app 插件市场极丰富

### 3. 什么是分包加载？为什么要用？

- 把代码分成主包 + 分包，用户进入对应页面才下载分包
- 解决：小程序**2M 主包限制**、加快首屏加载

### 4. uni-app 性能优化方案？

- 图片优化
- 分包加载
- 长列表用 nvue
- 减少 setData
- 避免复杂嵌套
- 路由合理使用
- 代码压缩、tree-shaking

### 5. uni-app 如何适配多端？

- 样式：rpx
- 代码：条件编译
- API：统一封装 uni.xxx API
- 组件：内置多端适配组件

------

## 五、最实用开发口诀（记住少踩 90% 坑）

1. 样式全写 rpx，少用选择器
2. 刷新数据用 onShow，别用 created
3. 跳转别超 10 层，多用 redirectTo
4. 跨端差异用条件编译
5. 长列表卡顿写 nvue
6. 小程序超 2M 必须分包
7. 同步存储用 xxxSync
8. 组件不能用页面生命周期

------

### 总结

- **原理**：编译时多端适配 + 运行时分端渲染
- **八股**：架构、生命周期、通信、性能优化、跨端方案
- **坑点**：样式、生命周期、路由、跨端、组件、缓存、调试
- **核心**：一套代码 + 条件编译 = 多端运行

# 原生小程序与uni-app 核心对比（面试+实战）

# 一、核心定义（面试速背）

- 原生小程序：某一平台专属开发（如微信小程序），用小程序原生语法（WXML/WXSS/JS），仅支持单端运行。
- uni-app：基于Vue语法的跨端框架，一套代码可编译到微信/支付宝等多端小程序、iOS/Android App、H5，兼容多端。

# 二、核心差异（高频考点+实战重点）

| 对比维度   | 原生小程序                                                  | uni-app                                                      |
| :--------- | :---------------------------------------------------------- | :----------------------------------------------------------- |
| 语法规范   | WXML（模板）、WXSS（样式）、JS（逻辑），有专属API（wx.xxx） | Vue语法（Template/CSS/JS），统一API（uni.xxx），兼容Vue2/Vue3 |
| 跨端能力   | 仅支持单端（如微信），跨端需重复开发多套代码                | 一套代码多端运行，覆盖小程序、App、H5，无需重复开发          |
| 学习成本   | 需学习小程序专属语法、API，上手中等                         | 会Vue即可上手，学习成本低，无需额外记多端API                 |
| 工程化能力 | 原生工程化较弱，需额外配置，插件生态单一（仅单端）          | 内置webpack/vite编译，支持npm、分包、脚手架，插件市场丰富（多端适配） |
| 性能表现   | 单端性能最优，无框架层损耗，适配性最好                      | 多端兼容下，性能略低于原生小程序；App端可通过nvue实现接近原生性能 |
| 开发效率   | 单端开发效率高，多端开发效率极低（重复编码）                | 多端开发效率极高，一套代码适配所有端，后期维护成本低         |
| 调试与打包 | 仅需对应平台开发者工具，打包简单（直接上传平台）            | 支持多端调试，App需云端/本地打包，小程序需编译后上传         |

# 三、适用场景（实战避坑，选对工具）

- 选原生小程序：仅开发单一平台小程序（如只做微信小程序）、对性能要求极高、需深度调用平台专属API（如微信支付、小程序直播专属能力）。
- 选uni-app：需开发多端（小程序+App+H5）、想降低学习成本和开发成本、项目无极致性能要求、需快速迭代上线。

# 四、面试高频问答（直接背）

- 问：原生小程序和uni-app的核心区别？ 
- 答：1. 跨端能力：原生单端，uni-app多端；2. 语法：原生用小程序语法，uni-app用Vue语法；3. 效率：原生单端高效、多端低效，uni-app多端高效；4. 性能：原生略优，uni-app可通过nvue弥补。
- 问：什么时候用原生小程序？什么时候用uni-app？ 
- 答：仅做单一平台、需极致性能/深度调用平台API用原生；多端开发、降本提效用uni-app。
- 问：uni-app相比原生小程序的优势和劣势？ 
- 答：优势：跨端、学习成本低、开发维护高效、生态丰富；劣势：多端兼容下性能略低于原生，部分平台专属API需条件编译适配。

**核心区别**：原生单端 + 专用语法；uni-app 跨端 + Vue 语法。

**优势**：uni-app 开发维护效率高；原生性能最优。

**场景**：单端选原生，多端选 uni-app。



# 生命周期 + 路由 核心对比（面试 + 实战・一页纸）

## 一、核心定义（面试速背）

- **原生小程序**：拥有独立的**应用 / 页面 / 组件生命周期**，路由采用平台专属 `wx.xxx` API，无 Vue 生命周期，仅单端适配。
- **uni-app**：**兼容原生小程序全部生命周期** + 融合 **Vue 生命周期**，路由统一封装为 `uni.xxx` API，一套语法适配多端，是跨端生命周期 + 路由的标准方案。

------

## 二、核心差异（高频考点 + 实战重点）

### 🔴 生命周期 对比表

|   对比维度   |                  原生小程序                  |                           uni-app                            |
| :----------: | :------------------------------------------: | :----------------------------------------------------------: |
| 全局生命周期 |          `onLaunch、onShow、onHide`          |                  完全兼容小程序全局生命周期                  |
| 页面生命周期 | `onLoad、onShow、onReady、onHide、onUnload`  |                 100% 兼容小程序页面生命周期                  |
| 组件生命周期 | 仅支持小程序组件生命周期，**无页面生命周期** |    支持：Vue 生命周期 (created/mounted) + 小程序生命周期     |
|   执行机制   |   页面返回：`onShow` 执行，`onLoad` 不执行   |       和小程序完全一致；**Vue created 页面返回不执行**       |
|   专属特性   |             无 Vue 相关生命周期              | 页面：Vue 生命周期 + 小程序生命周期共存；nvue 有原生专属生命周期 |

### 🟢 路由 对比表

|  对比维度  |           原生小程序            |                    uni-app                    |
| :--------: | :-----------------------------: | :-------------------------------------------: |
|  路由 API  | `wx.navigateTo` 等 5 个核心 API | 统一封装为 `uni.navigateTo`，**语法完全一致** |
| 页面栈限制 |      小程序最多 **10 层**       |         继承小程序限制，多端规则统一          |
|  路由传参  |    URL 拼接字符串，长参丢失     | 用法相同；长参必须 `encodeURIComponent` 编码  |
|  跳转规则  |        仅支持单平台规则         |    多端通用；tabBar 跳转必须用 `switchTab`    |
|  跨端适配  |           仅单端生效            |         支持条件编译适配多端路由差异          |

------

## 三、实战必坑（开发避坑・核心）

### 1. 生命周期避坑

- 原生 /uni-app 通用：**页面刷新数据必须写在 `onShow`**，`onLoad/created` 仅执行一次
- 组件禁止使用页面生命周期（`onLoad` 等）
- uni-app 独有：Vue 生命周期（mounted）+ 小程序生命周期（onReady）可同时使用

### 2. 路由避坑

- 页面栈超 10 层报错：多用 `redirectTo` 替换页面，少用 `navigateTo`
- 传参过长 / 特殊字符：必须用 `encodeURIComponent` 编码，接收端解码
- tabBar 页面：只能用 `switchTab` 跳转，其他 API 无效
- uni-app 跨端：H5/APP 路由差异用条件编译处理

------

## 四、面试高频问答（直接背）

1. **问**：uni-app 和原生小程序生命周期区别？

   **答**：uni-app 完全兼容小程序生命周期，**额外支持 Vue 生命周期**；组件可使用双生命周期，原生小程序组件无页面生命周期。

2. **问**：页面返回刷新为什么用 onShow？

   **答**：原生和 uni-app 一致，`onLoad` 仅页面加载执行一次，`onShow` 每次页面显示都会执行。

3. **问**：两者路由有什么区别？

   **答**：API 名称、用法、页面栈限制完全一致；uni-app 统一为 `uni.xxx` 支持多端，原生仅 `wx.xxx` 单端生效。

4. **问**：路由跳转常见报错原因？

   **答**：页面栈超 10 层、tabBar 用错 API、传参过长未编码、路径错误。