# JavaScript 继承全解

JavaScript 是**基于原型（Prototype）\**的语言，\*\*没有传统类式继承\*\*，其继承的核心是\**原型链机制** + **构造函数借用**。下面从**核心原理、7 种实现方式（含代码 + 优缺点）、使用场景、最佳实践**四个维度全面讲解。

------

## 一、继承核心原理

### 1. 原型三要素

1. **构造函数**：用于创建实例的函数（如 `function Parent(){}`）
2. **原型对象**：每个构造函数自带 `prototype` 属性，指向原型对象
3. **实例对象**：`new 构造函数()` 创建的对象，自带 `__proto__` 指向构造函数的原型

### 2. 原型链

实例的 `__proto__` → 父类原型 → 父类原型的 `__proto__` → `Object.prototype` → `null`

**继承的本质**：让子类的原型链关联到父类，从而**共享父类的属性和方法**。

------

## 二、继承的 7 种实现方式

按**从基础到最优、从 ES5 到 ES6**排序，包含完整代码、优缺点。

### 1. 原型链继承（最基础）

**核心**：子类原型直接指向父类实例

```javascript
// 父类
function Parent() {
  this.name = "父类";
  // 引用类型属性（坑点）
  this.colors = ["red", "blue"];
}
// 父类原型方法
Parent.prototype.say = function () {
  console.log(this.name);
};

// 子类
function Child() {}
// 核心：子类原型 = 父类实例
Child.prototype = new Parent();
// 修正constructor指向（必须）
Child.prototype.constructor = Child;

// 测试
const child1 = new Child();
const child2 = new Child();
child1.colors.push("green"); 
```

✅ **优点**：实现简单，能继承父类**原型方法**

❌ **缺点**：

1. 父类**引用类型属性**会被所有子类实例共享（改一个，全变）
2. 创建子类实例时，**无法向父类构造函数传参**

------

### 2. 构造函数继承（借用 call/apply）

**核心**：在子类构造函数中用 `call()` 调用父类，继承实例属性

```javascript
function Parent(name) {
  this.name = name;
  this.colors = ["red", "blue"];
}

function Child(name, age) {
  // 核心：借用父类构造函数，继承属性+传参
  Parent.call(this, name);
  this.age = age;
}

// 测试
const child1 = new Child("子类1", 18);
child1.colors.push("green");
const child2 = new Child("子类2", 20);
```

✅ **优点**：

1. 解决了引用类型共享问题

2. 可以向父类构造函数传参

   ❌ 缺点：

   无法继承父类原型方法，方法只能定义在构造函数中，无法复用

------

### 3. 组合继承（伪经典继承）

**核心**：融合「原型链继承方法 + 构造函数继承属性」，ES5 最常用

```javascript
function Parent(name) {
  this.name = name;
  this.colors = ["red", "blue"];
}
Parent.prototype.say = function () {
  console.log(this.name);
};

function Child(name, age) {
  // 1. 继承实例属性（第1次调用父类构造）
  Parent.call(this, name);
  this.age = age;
}

// 2. 继承原型方法（第2次调用父类构造）
Child.prototype = new Parent();
Child.prototype.constructor = Child;
```

✅ **优点**：

1. 可传参、引用类型独立

2. 继承父类原型方法，实现复用

   ❌ 缺点：父类构造函数被调用 2 次，子类原型上会有多余的父类属性

------

### 4. 原型式继承

**核心**：基于现有对象，用 `Object.create()` 浅拷贝创建新对象

```javascript
// 父对象
const parent = {
  name: "父类",
  colors: ["red"],
  say() { console.log(this.name); }
};

// 核心：创建继承parent的新对象
const child = Object.create(parent);
```

✅ **优点**：无需定义构造函数，适合**单对象简单继承**

❌ **缺点**：引用类型属性共享，无法传参

------

### 5. 寄生式继承

**核心**：在原型式继承基础上封装，给新对象扩展方法

```javascript
// 封装继承+扩展逻辑
function createChild(parentObj) {
  const child = Object.create(parentObj);
  // 扩展子类方法
  child.eat = function () {
    console.log("吃饭");
  };
  return child;
}

const parent = { name: "父类" };
const child = createChild(parent);
```

✅ **优点**：快速给继承对象添加方法

❌ **缺点**：同原型式继承，引用类型共享、方法无法复用

------

### 6. 寄生组合式继承（🔥 ES5 最佳实践）

**核心**：解决组合继承的「两次调用父类构造」问题，用 `Object.create()` 替代 `new Parent()`

```javascript
function Parent(name) {
  this.name = name;
  this.colors = ["red"];
}
Parent.prototype.say = function () {};

function Child(name, age) {
  // 仅1次调用父类构造（继承属性）
  Parent.call(this, name);
  this.age = age;
}

// 核心：子类原型 = 父类原型的副本（不调用父类构造）
Child.prototype = Object.create(Parent.prototype);
Child.prototype.constructor = Child;
```

✅ **优点**：

1. 只调用 1 次父类构造，无多余属性

2. 完美继承：可传参、引用独立、原型方法复用

   ❌ 缺点：写法稍繁琐

------

### 7. ES6 class extends（🔥 开发首选）

**核心**：ES6 语法糖，**底层就是寄生组合式继承**，语法更简洁

```javascript
// 父类
class Parent {
  constructor(name) {
    this.name = name;
    this.colors = ["red"];
  }
  // 原型方法
  say() {
    console.log(this.name);
  }
}

// 子类继承
class Child extends Parent {
  constructor(name, age) {
    // 等价于 Parent.call(this, name)
    super(name);
    this.age = age;
  }
}
```

✅ **优点**：

1. 语义清晰、语法简洁，符合现代编程习惯

2. 自动修正 constructor，无需手动处理原型

   ❌ 缺点：本质还是原型继承，只是语法糖，需理解底层原理

------

## 三、各继承方式对比总结

|     继承方式      |           优点           |          缺点          | 推荐指数 |
| :---------------: | :----------------------: | :--------------------: | :------: |
|    原型链继承     |    简单，继承原型方法    |   引用共享、无法传参   |    ⭐     |
|   构造函数继承    |     可传参、引用独立     |    无法继承原型方法    |    ⭐     |
|     组合继承      |   兼顾属性 + 方法继承    |   父类构造调用 2 次    |   ⭐⭐⭐    |
|    原型式继承     | 无需构造函数，简单浅继承 |   引用共享、无法传参   |    ⭐⭐    |
|    寄生式继承     |     快速扩展对象方法     | 引用共享、方法无法复用 |    ⭐⭐    |
|  寄生组合式继承   |     完美继承，无冗余     |       写法稍复杂       |  ⭐⭐⭐⭐⭐   |
| ES6 class extends |    语法简洁、语义清晰    |   语法糖，需理解底层   |  ⭐⭐⭐⭐⭐   |

------

## 四、使用场景

1. **ES6 class extends（首选）**

   日常业务开发、框架开发（React/Vue 组件、类封装），语法简洁，团队易维护。

2. **寄生组合式继承**

   兼容 ES5 环境、底层库 / 框架开发，需要极致性能和无冗余的继承。

3. **组合继承**

   老旧项目兼容、简单业务场景，对性能要求不高的场景。

4. **原型式 / 寄生式继承**

   无需构造函数、**单对象扩展**（如工具对象、配置对象继承）。

5. **原型链 / 构造函数继承**

   仅用于学习原型原理，**绝不推荐生产环境使用**。

------

### 总结

1. JS 继承的核心是**原型链**，本质是子类通过原型链共享父类的属性和方法；
2. ES5 **寄生组合式继承** 是理论最优解，ES6 `class extends` 是工程化最优解；
3. 日常开发直接用 `class extends`，底层原理理解原型链即可。



# JavaScript 函数及相关扩展 超详细全解

JavaScript 函数是**核心编程单元**，是一段可复用、可调用的代码块，用于封装逻辑、处理数据、返回结果。从基础定义到 ES6+ 高级扩展，我会分模块带你彻底吃透，包含用法、原理、实战场景。

## 一、函数基础（核心概念）

### 1. 函数的本质

- 函数是**一等公民**：可以作为变量、参数、返回值、数组元素使用；
- 函数是对象：拥有属性和方法（如 `length`、`call()`、`apply()`）；
- 作用：代码复用、模块化、封装逻辑、处理业务。

### 2. 基础定义方式

#### （1）函数声明（最常用）

```javascript
// 格式：function 函数名(参数){ 执行体 }
function sum(a, b) {
  return a + b; // return 返回结果，无return则返回undefined
}
console.log(sum(1,2)); // 3
```

**特点**：存在**函数提升**，声明前可调用。

#### （2）函数表达式

```javascript
// 匿名函数表达式
const add = function(a, b) { return a + b };

// 命名函数表达式（便于调试）
const mul = function multi(a,b) { return a*b };
```

**特点**：**无函数提升**，必须先定义后调用。

#### （3）构造函数（极少用）

```javascript
const sub = new Function('a','b','return a - b');
```

### 3. 核心特性

1. **参数**：实参个数可多于 / 少于形参，通过 `arguments` 获取所有实参（类数组）
2. **返回值**：`return` 终止函数并返回值，无返回值默认 `undefined`
3. **作用域**：函数内变量无法外部直接访问（闭包除外）
4. **this 指向**：默认指向调用者（后续扩展详解）

------

## 二、函数核心扩展（ES5+ 必备）

### 1. 立即执行函数（IIFE）

**作用**：创建独立作用域，避免变量污染，立即执行函数。

```javascript
// 写法
(function(){
  let a = 10; // 外部无法访问
  console.log(a);
})();

// 带参数
(function(x,y){console.log(x+y)})(1,2); // 3
```

### 2. 函数作为参数 / 返回值（高阶函数）

JavaScript 支持**函数嵌套、函数传参**，这是回调、闭包的基础。

```javascript
// 函数作为参数（回调函数）
function exec(fn, a, b) {
  return fn(a, b);
}
console.log(exec(sum, 3,4)); // 7

// 函数作为返回值
function outer() {
  return function inner() { return 'inner' }
}
```

### 3. 闭包（高级核心）

**定义**：函数嵌套时，内部函数访问外部函数变量，形成闭包。

**作用**：保留变量、私有化变量、实现缓存 / 模块化。

```javascript
function counter() {
  let count = 0; // 私有变量
  return function() {
    count++;
    return count;
  }
}
const c = counter();
console.log(c()); //1
console.log(c()); //2
```

**注意**：滥用闭包会造成内存泄漏。

------

## 三、ES6 函数扩展

### 1. 箭头函数（最常用扩展）

**语法**：`(参数) => { 执行体 }`

```javascript
// 基础写法
const add = (a,b) => a + b; // 单行省略{}和return

// 1个参数可省略()
const double = x => x*2;

// 无参数必须写()
const hello = () => 'hello';
```

**核心特性（必背）**：

1. **没有自己的 this**：继承外层作用域的 this
2. **没有 arguments**：用剩余参数 `...args` 替代
3. **不能作为构造函数**：不能用 `new`
4. **没有 prototype**

**适用场景**：回调函数、简化代码；**不适用**：对象方法、构造函数。

### 2. 参数默认值

ES6 支持直接给参数设置默认值，简化逻辑：

```javascript
function greet(name = 'Guest') {
  return `Hello ${name}`;
}
console.log(greet()); // Hello Guest
```

### 3. 剩余参数（...rest）

接收**不确定数量的实参**，转为真数组（替代 arguments）：

```javascript
function sum(...nums) {
  return nums.reduce((a,b)=>a+b);
}
sum(1,2,3,4); // 10
```

### 4. 展开运算符（...）

与剩余参数语法相同，作用相反：**打散数组 / 对象**

```javascript
// 数组展开
const arr = [1,2,3];
console.log(...arr); // 1 2 3

// 合并数组
const newArr = [...arr,4,5];
```

### 5. 函数参数解构

直接从参数对象中提取值：

```javascript
function printUser({name, age}) {
  console.log(name, age);
}
printUser({name:'张三', age:20});
```

------

## 四、函数高级方法（改变 this 指向）

JS 提供三个方法手动修改函数 `this` 指向，是面试高频：

### 1. call()

**立即执行**，参数逐个传递：

```javascript
function say() { console.log(this.name) }
const obj = {name:'李四'}
say.call(obj); // 李四
```

### 2. apply()

**立即执行**，参数以**数组**传递：

```javascript
sum.apply(null, [1,2,3]); // 6
```

### 3. bind()

**返回新函数**，永久绑定 this，不立即执行：

```javascript
const newSay = say.bind(obj);
newSay(); // 李四
```

**区别总结**：

- call/apply：立即执行；bind：返回函数
- call：参数散列；apply：参数数组

------

## 五、函数进阶扩展

### 1. 生成器函数（function*）

可**暂停 / 恢复**执行，用于异步流程控制（async/await 基础）：

```javascript
function* gen() {
  yield 1;
  yield 2;
  return 3;
}
const g = gen();
console.log(g.next()); // {value:1, done:false}
```

### 2. 异步函数（async/await）

基于 Promise 的异步语法，**同步写法处理异步**：

```javascript
async function fetchData() {
  const res = await fetch('url');
  const data = await res.json();
  return data;
}
```

### 3. 函数柯里化

将多参数函数转为**单参数链式调用**，用于参数复用、延迟执行：

```javascript
const curriedSum = a => b => a + b;
curriedSum(1)(2); //3
```

### 4. 纯函数

**相同输入永远得到相同输出**，无副作用，React/Redux 核心：

```javascript
// 纯函数
function pure(a) { return a*2 }

// 非纯函数（修改外部变量）
let b = 0;
function impure() { b++ }
```

------

## 六、函数使用场景总结

|  函数类型   |           适用场景            |
| :---------: | :---------------------------: |
|  普通函数   |    独立业务逻辑、对象方法     |
|  箭头函数   | 回调、简化代码、保留外层 this |
|    闭包     |    私有变量、计数器、缓存     |
|  高阶函数   |   函数封装、通用工具、回调    |
| async/await |    异步请求、异步流程控制     |
|   柯里化    |      参数复用、逻辑复用       |

------

### 总结

1. **基础**：函数声明 / 表达式、参数、return、this 是根基；
2. **ES6 核心**：箭头函数、默认参数、剩余 / 展开运算符是开发必备；
3. **高级**：闭包、call/apply/bind、async/await 是进阶关键；
4. **原则**：优先纯函数、避免全局污染、合理使用闭包。