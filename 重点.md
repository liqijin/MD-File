# Vue3 框架 & WebSocket 原理

## 一、Vue3 核心八股文

### 1. Vue3 相比 Vue2 的核心升级？

> Vue3 相比 Vue2 主要有以下核心升级，且这些升级在系统开发中能解决实际问题：
>
> 1. **底层架构重构**：基于 Proxy 替代 Vue2 的 Object.defineProperty 实现响应式，解决了数组下标修改、对象新增属性无法监听的问题，在实时数据监控系统中，能更精准监听海量数据的动态变化；
> 2. **Composition API 引入**：替代 Options API 的碎片化写法，将同一业务逻辑（如告警规则配置）聚合，便于复杂系统的代码复用与维护；
> 3. **体积优化**：通过 Tree-Shaking 实现按需打包，系统通常集成多模块功能，可显著减小包体积、提升页面加载速度；
> 4. **性能提升**：虚拟 DOM 重写、编译优化（PatchFlag 标记动态节点），在大屏可视化场景下，大幅降低高频数据更新时的渲染开销；
> 5. **更好的 TypeScript 支持**：企业级系统注重代码可维护性，TypeScript 可提升团队协作效率，减少类型相关 Bug。

### 2. Vue3 的响应式原理（Proxy 实现）？

> Vue3 采用 ES6 的 Proxy 代理整个目标对象，配合 Reflect 实现响应式，核心逻辑如下：
>
> 1. **创建代理对象**：通过 `new Proxy(target, handler)` 拦截对象的读取、修改、删除等操作，相比 Vue2 的 Object.defineProperty，能监听数组下标修改、对象新增 / 删除属性；
>
> 2. **依赖收集**：读取属性时（get 拦截），将当前执行的副作用函数（如组件渲染函数）收集到「依赖映射表」中；
>
> 3. **触发更新**：修改属性时（set 拦截），从依赖映射表中取出对应的副作用函数并执行，触发视图更新；
>
> 4. Reflect 作用：保证拦截操作的行为与原生一致，且能正确返回操作结果，比如 
>
>    ```
>    Reflect.set(target, key, value)
>    ```
>
>     会返回布尔值表示是否设置成功。
>
>    （场景适配）：在实时数据推送场景中，Proxy 能监听实时更新的数据），确保数据变化即时同步到监控面板。

### 3. Composition API 核心用法 & 优势？

> 1. 核心 API：
>
>    - `setup()`：Vue3 组件的入口，替代 beforeCreate/created，所有 Composition API 在此使用；
>    - `ref/reactive`：创建响应式数据（ref 用于基本类型，reactive 用于引用类型）；
>    - `computed/watch`：计算属性与监听，watch 支持监听多个源、深度监听；
>    - `toRef/toRefs`：将 reactive 对象的属性转为 ref，解决解构丢失响应式问题；
>    - `生命周期钩子`：如 onMounted、onUpdated，替代 Options API 的生命周期；
>
>    
>
> 2. 核心优势：
>
>    - 逻辑聚合：将系统中「告警处理」「数据采集」等独立业务逻辑聚合，而非分散在 data/methods/watch 中；
>    - 逻辑复用：可将通用逻辑（如数据格式转换、权限校验）封装为自定义 Hook（如 usePowerData、useAuth），跨组件复用；
>    - 类型友好：配合 TypeScript 提升代码可读性与可维护性，适配系统的长期迭代需求。
>
>    

### 4. Vue3 的 Teleport（瞬移组件）使用场景？

> Teleport 能将组件渲染到 DOM 树的指定位置（而非父组件嵌套层级中），核心使用场景：
>
> 1. 系统中的全局弹窗（如告警提示、操作确认框）：避免被父组件的样式 / 层级限制，始终渲染在 body 下，保证层级最高；
> 2. 大屏可视化的悬浮操作栏：脱离父容器的布局约束，固定在屏幕指定位置；
> 3. 用法示例：
>
> ```vue
> <teleport to="body">
>   <el-dialog v-model="visible" title="告警">
>     <!-- 告警内容 -->
>   </el-dialog>
> </teleport>
> ```

### 5. Vue3 如何做性能优化？

> 结合系统高实时、高可靠的特点，Vue3 性能优化主要从以下方面入手：
>
> 1. **响应式优化**：使用 `shallowRef/shallowReactive` 监听非深层数据（如静态配置），减少依赖收集开销；
> 2. 渲染优化：
>    - 用 `v-memo` 缓存高频渲染的列表（如实时列表），仅当依赖数据变化时重新渲染；
>    - 长列表使用虚拟滚动（如 `vue-virtual-scroller`），避免一次性渲染上万条设备数据；
> 3. **打包优化**：通过 `defineConfig` 配置 externals，将 Element Plus、ECharts 等库通过 CDN 引入，减小打包体积；
> 4. **异步组件**：将系统的非核心模块（如历史数据查询）设为异步组件，按需加载：
>
> ```javascript
> const HistoryData = defineAsyncComponent(() => import('./HistoryData.vue'))
> ```

## 二、WebSocket 原理 & 实战八股文

### 1. WebSocket 核心原理？与 HTTP 的区别？

> 1. 核心原理：
>
>    WebSocket 是基于 TCP 的全双工通信协议，通过「一次握手」建立持久化连接，实现客户端与服务端的双向实时通信：
>
>    - 握手阶段：客户端通过 HTTP 请求（携带 `Upgrade: websocket` 等头信息）发起连接，服务端确认后，协议升级为 WebSocket；
>    - 通信阶段：连接建立后，双方可随时发送二进制 / 文本数据，无需重复建立连接；
>    - 断开阶段：任意一方可主动关闭连接，也可通过心跳检测处理异常断开。
>
> 2. 与 HTTP 的核心区别：
>
>    表格
>
>    | 维度     | HTTP                            | WebSocket              |
>    | :------- | :------------------------------ | :--------------------- |
>    | 通信方式 | 单向（客户端请求 - 服务端响应） | 全双工（双向实时通信） |
>    | 连接状态 | 无状态（每次请求独立）          | 长连接（持久化）       |
>    | 头部开销 | 大（每次请求携带完整头）        | 小（仅握手阶段有开销） |
>    | 适用场景 | 普通数据请求                    | 实时推送（如告警）     |

### 2. WebSocket 实战使用步骤（Vue3 中）？

> 在 Vue3 项目中使用 WebSocket 对接实时数据，核心步骤如下：
>
> 1. **建立连接**：在组件的 `onMounted` 中创建 WebSocket 实例，监听连接事件：
>
> ```javascript
>import { onMounted, onUnmounted, ref } from 'vue';
> 
> export default {
>   setup() {
>     const ws = ref(null);
>     const powerData = ref({}); // 实时数据
> 
>     onMounted(() => {
>       // 建立连接（后端 WebSocket 地址）
>       ws.value = new WebSocket('ws://xxx.xxx.xxx:8080/power/real-time');
>       
>       // 连接成功回调
>       ws.value.onopen = () => {
>         console.log('WebSocket 连接成功');
>         // 向服务端发送订阅指令（如订阅某数据）
>         ws.value.send(JSON.stringify({ type: 'subscribe', area: '华东' }));
>       };
> 
>       // 接收服务端推送的数据
>       ws.value.onmessage = (e) => {
>         const data = JSON.parse(e.data);
>         powerData.value = data; // 更新实时数据
>       };
> 
>       // 连接错误回调
>       ws.value.onerror = (err) => {
>         console.error('WebSocket 错误：', err);
>       };
>     });
> 
>     // 组件销毁时关闭连接
>     onUnmounted(() => {
>       if (ws.value) {
>         ws.value.close();
>       }
>     });
> 
>     return { powerData };
>   }
> };
> ```
> 
> 1. 心跳检测（解决连接异常）：
>
>    系统要求高可靠，需添加心跳检测，避免连接假死：
>
> ```javascript
>// 心跳检测
> let heartBeatTimer = null;
>// 连接成功后启动心跳
> ws.value.onopen = () => {
>   heartBeatTimer = setInterval(() => {
>     ws.value.send(JSON.stringify({ type: 'heartbeat' }));
>   }, 30000); // 每30秒发送一次心跳
> };
> // 断开连接时清除心跳
> ws.value.onclose = () => {
>   clearInterval(heartBeatTimer);
>   // 尝试重连（场景需保障连接稳定性）
>   setTimeout(() => { /* 重新建立连接逻辑 */ }, 5000);
> };
> ```

### 3. WebSocket 在场景的核心应用？

> WebSocket 是前端系统的核心技术，主要用于：
>
> 1. **实时数据监控**：电压、电流、功率等指标的秒级推送，实时更新监控大屏；
> 2. **告警即时推送**：设备故障、参数越限等告警信息，无需前端轮询，服务端主动推送；
> 3. **指令双向交互**：前端发送设备控制指令（如开关合闸 / 分闸），服务端实时返回执行结果；
> 4. **多端同步**：调度中心、移动端、大屏的实时数据同步，保障数据一致性。

### 4. WebSocket 常见问题 & 解决方案？

> 1. 连接断开 / 假死：
>
>    解决方案：添加心跳检测（定时发送心跳包），断开后自动重连，并重订阅数据；
>
> 2. 数据量大导致卡顿：
>
>    解决方案：前端对推送的数据做节流处理，非核心数据合并更新，核心告警数据优先处理；
>
> 3. 跨域问题：
>
>    解决方案：服务端配置 CORS（允许 WebSocket 跨域），或通过 Nginx 反向代理转发 WebSocket 请求；
>
> 4. 数据安全性：
>
>    解决方案：使用 WSS（WebSocket + SSL）加密传输，数据添加签名校验，防止篡改。

## 三、总结

### Vue3 核心关键点

1. Vue3 基于 Proxy 实现响应式，Composition API 提升复杂业务的代码复用性，性能优化适配高频数据更新场景；
2. 场景下需重点掌握响应式优化、异步组件、虚拟滚动等实战技巧，保障系统高可靠、高性能。

### WebSocket 核心关键点

1. WebSocket 是全双工长连接协议，通过一次握手实现双向实时通信，适配实时数据推送场景；
2. 实战中需实现心跳检测、自动重连、数据加密，解决系统的高可靠、高安全需求。

## 第一部分：Vue3 核心进阶（针对 主流框架要求）

### 1. Vue3 的响应式原理与 Vue2 有什么区别？为什么用 Proxy？

- **背诵话术：**

  > "Vue2 使用的是 `Object.defineProperty`，它通过拦截属性的 `get` 和 `set` 来实现响应式，但存在局限性：无法监听对象属性的增加和删除，也无法完美支持数组索引的修改。 **Vue3 改用 Proxy**。Proxy 是直接代理整个对象，能够拦截包括属性读写、属性删除、`in` 操作符等 13 种操作。它的**优势**在于：
  >
  > 1. **性能更好**：不需要在初始化时递归遍历对象所有属性，而是延迟到访问时才处理。
  > 2. **功能更强**：原生支持数组和 Map/Set。
  > 3. **内存占用低**：只有在用到深层数据时才会进行递归代理。"

### 2. Composition API (组合式 API) 解决了什么问题？

- **背诵话术：**

  > "在处理大型复杂系统时，Options API 的逻辑会变得非常分散（Data、Methods、Computed 散落在各处）。 **Composition API 的核心价值是逻辑复用和代码组织**。它允许我们将相同功能的代码（如：WebSocket 的心跳逻辑、窗口缩放监听）封装成独立的 `useHook` 函数。这不仅提高了代码的可维护性，还带来了更好的 **TypeScript 类型推导**支持。"

### 3. Vue3 的编译优化（Patch Flags / Hoisting）

- **背诵话术：**

  > "Vue3 在模板编译阶段做了很多‘静态标记’。
  >
  > 1. **静态提升（Hoisting）**：将不随数据变化的静态节点提到 `render` 函数之外，避免每次更新都重新创建。
  > 2. **补丁标记（Patch Flags）**：在 VNode 创建时标记哪些属性是动态的（如文本、类名）。在 Diff 算法阶段，Vue 只会对比有标记的动态节点，跳过静态内容，这使得更新性能提升了数倍。"

------

## 第二部分：WebSocket 原理与实战（针对 网络协议要求）

### 1. WebSocket 的握手过程是怎样的？它和 HTTP 是什么关系？

- **背诵话术：**

  > "WebSocket 的连接是基于 HTTP 协议进行握手的，这叫**协议升级（Protocol Upgrade）**。
  >
  > 1. 客户端发送一个标准 HTTP 请求，Header 中包含 `Upgrade: websocket` 和 `Connection: Upgrade`。
  > 2. 服务端如果支持，会返回 **101 Switching Protocols** 状态码。 握手成功后，底层 TCP 连接会保持开启，双方进入**全双工（Full-duplex）**通信，不再需要 HTTP 的请求头，极大地减少了数据开销。"

### 2. 在监控中，如何保证 WebSocket 的高可用性（断线与心跳）？

- **背诵话术：**

  > "在实时性要求场景，网络波动是常态，必须设计完善的**容错机制**：
  >
  > 1. **心跳检测（Heartbeat）**：客户端每隔固定时间（如 30s）发送一个 `Ping` 包，服务端响应 `Pong`。如果连续几次未收到响应，说明连接已失效。
  > 2. **断线自动重连**：当触发 `onclose` 或心跳超时时，启动重连。我会使用**指数退避算法（Exponential Backoff）**，即重连间隔从 1s、2s、4s、8s 逐渐增加，避免在网络灾难时瞬间大量请求冲击服务器。
  > 3. **状态同步**：重连成功后，需要重新发送当前页面的订阅参数，确保数据流无缝恢复。"

### 3. Nginx 如何配置支持 WebSocket？

- **背诵话术：**

  > "由于 WebSocket 握手是特殊的 HTTP 请求，Nginx 默认不会转发 `Upgrade` 头。 我们需要在 `location` 块中明确设置：
  >
  > ```Nginx
  >proxy_set_header Upgrade $http_upgrade;
  > proxy_set_header Connection "upgrade";
  > ```
  > 
  > 此外，针对长连接，通常需要调大 `proxy_read_timeout`，防止 Nginx 在没有数据传输时主动切断连接（默认通常是 60s）。"

------

## 第三部分：综合场景题

**面试官：** “如果你负责一个设备实时告警模块，后端每秒推送 500 条数据，Vue 页面卡顿怎么办？”

- **背诵话术（高级前端进阶）：**

  > "这种情况通常是由于 **DOM 渲染频率超过了显示器刷新率（60Hz）** 导致的。我的优化策略是：
  >
  > 1. **数据节流（Throttling）**：在前端设置缓冲区，将每秒 500 次更新聚合，利用 `requestAnimationFrame` 每 16.7ms 或每 100ms 批量更新一次 Vue 状态。
  > 2. **浅层响应式**：对于只用于展示的大规模数据，使用 `shallowRef` 或 `markRaw`，跳过 Vue 对深层属性的代理，减轻 CPU 负担。
  > 3. **虚拟列表（Virtual List）**：如果列表极长，只渲染可视区域的 DOM 节点，确保内存占用稳定。"

## WebSocket 四个默认连接状态 (`readyState`)

这些状态描述了WebSocket连接的生命周期： 

1. **CONNECTING (0)**:
   - **描述**: 连接尚未建立，正在进行握手过程。
   - **特点**: 刚创建 `new WebSocket()` 时。
2. **OPEN (1)**:
   - **描述**: WebSocket 握手成功，连接已建立，可以进行全双工数据传输。
   - **特点**: 触发 `onopen` 事件。
3. **CLOSING (2)**:
   - **描述**: 连接正在进行关闭握手，正在关闭连接。
   - **特点**: 调用了 `ws.close()`，但尚未完全断开。
4. **CLOSED (3)**:
   - **描述**: 连接已关闭，或者无法打开。	
   - **特点**: 触发 `onclose` 事件。

# 低代码开发

## 一、核心概念类

### 1. 什么是低代码开发？核心价值是什么？

> 低代码开发是一种**可视化 + 少量编码**的开发模式，通过拖拽式组件、可视化配置、预制模板等方式，降低传统编码的门槛，快速构建应用系统。
>
> 其核心价值体现在：
>
> 1. **提效降本**：系统需求迭代快、场景多，低代码可将开发周期缩短 50% 以上，减少重复编码工作量；
> 2. **降低门槛**：让非专业开发人员（如业务人员）也能参与应用搭建，实现 “业务人员自主配置”；
> 3. **快速适配需求**：业务规则频繁调整（如告警阈值、报表格式），低代码可通过可视化配置快速修改，无需重新编译部署；
> 4. **统一技术底座**：企业多系统并存，低代码平台可统一组件库、数据接口，降低系统集成成本。

### 2. 低代码平台的核心架构 / 组成部分？

> 低代码平台的核心架构可分为四层，适配场景的设计如下：
>
> 1. **基础层**：包含服务器、数据库、云资源等，场景需适配国产化数据库（如达梦、人大金仓）和私有云环境；
> 2. **引擎层**（核心）：包括可视化渲染引擎、表单引擎、流程引擎、数据引擎，是低代码的 “核心动力”—— 比如工单流程可通过流程引擎配置审批节点，实时数据可通过数据引擎对接接口；
> 3. **组件层**：预制通用组件（按钮、表格）+ 行业组件（大屏、设备监控组件、告警组件），支持自定义组件扩展；
> 4. **应用层**：最终交付的应用（如运维工单系统、设备监控面板、报表分析系统），支持多端适配（PC 端、大屏、移动端）。

### 3. 低代码 vs 传统开发 vs 零代码，区别是什么？

> 三者核心区别可通过表格清晰区分，尤其适配场景的选型逻辑：
>
> |   维度   |               低代码开发               |            传统开发            |            零代码开发            |
>| :------: | :------------------------------------: | :----------------------------: | :------------------------------: |
> | 开发方式 |         可视化配置 + 少量编码          |           纯手写代码           |       纯可视化拖拽，无编码       |
> | 技术门槛 |         中等（懂基础编程即可）         |      高（需精通多技术栈）      |      极低（纯业务人员操作）      |
> |  灵活性  |       高（可自定义组件 / 逻辑）        |       极高（完全自定义）       |       低（仅支持预制功能）       |
> | 适用场景 | 中复杂业务系统（如运维调度、数据监控） | 核心底层系统（如实时数据采集） | 简单应用（如报表填报、简单查询） |
> | 选型建议 |      主流选择（平衡效率与灵活性）      |        核心底层系统使用        |         轻量业务场景补充         |

## 二、实战应用类

### 1. 低代码平台的核心开发流程？

> 以 “设备告警配置系统” 为例，低代码开发流程如下：
>
> 1. **需求梳理**：明确告警规则配置、告警推送、历史查询三大核心需求；
> 2. 可视化搭建：
>    - 拖拽预制组件：设备选择组件、告警阈值输入框、表格（展示告警规则）、大屏图表（展示告警统计）；
>    - 配置页面布局：适配监控大屏的 16:9 分辨率，设置组件联动（如选择设备后自动加载该设备历史告警）；
> 3. 数据对接：
>    - 通过低代码平台的 “数据连接器” 对接实时数据库（如 InfluxDB）、设备管理系统 API；
>    - 配置数据映射规则（如将接口返回的 “值” 映射到表单字段，设置越限告警逻辑）；
> 4. 业务逻辑配置：
>    - 通过 “逻辑编排器” 配置告警规则：当值 > 阈值时，自动触发推送（对接企业微信 / 短信接口）；
>    - 设置权限控制：不同岗位（运维、调度）可见不同告警配置项；
> 5. 测试与发布：
>    - 模拟设备数据测试告警触发逻辑；
>    - 一键发布到私有云，支持灰度发布（先部署到测试班组验证）；
> 6. 运维与迭代：
>    - 通过低代码平台的 “应用监控” 查看系统运行状态；
>    - 业务人员可直接在平台修改告警阈值，无需开发介入。

### 2. 低代码平台如何实现自定义组件扩展？

> 场景常需定制化组件（如拓扑图、设备状态组件），低代码平台的扩展方式如下：
>
> 1. 组件开发规范
>
>    ：遵循平台的组件开发标准（如 Vue/React 组件规范），开发自定义组件；
>
>    示例代码（适配 Vue3 的低代码自定义组件）：
>
>    ```vue
>    <template>
>      <div class="power-device-status">
>        <!-- 设备状态组件：显示设备名称、电压、状态（正常/告警） -->
>        <span>{{ deviceName }}</span>
>        <span class="voltage">{{ voltage }}kV</span>
>        <span :class="status === 'error' ? 'alert' : 'normal'">{{ statusText }}</span>
>      </div>
>    </template>
>    <script setup>
>    import { defineProps, computed } from 'vue';
>    // 定义组件属性（低代码平台可配置）
>    const props = defineProps({
>      deviceId: { type: String, required: true }, // 设备ID
>      deviceName: { type: String, required: true }, // 设备名称
>      voltage: { type: Number, default: 0 }, // 电压值
>      status: { type: String, default: 'normal' } // 状态：normal/error
>    });
>    // 计算状态文本
>    const statusText = computed(() => {
>      return props.status === 'error' ? '告警（需巡检）' : '正常运行';
>    });
>    </script>
>    ```
>
>    
>
> 2. **组件注册**：将开发好的组件打包为插件，上传到低代码平台的 “组件市场”，完成注册；
>
> 3. **可视化配置**：在低代码平台拖拽该自定义组件后，可在右侧面板配置`deviceId`、`deviceName`等属性，绑定接口数据；
>
> 4. **复用与维护**：注册后的组件可在所有应用中复用，后续升级只需更新组件插件，无需修改应用。

### 3. 低代码开发在场景的典型应用案例？

> 结合实际经验，低代码核心应用场景有 3 类：
>
> 1. 运维工单系统：
>    - 可视化配置工单流程（派单→接单→处理→验收），对接设备台账；
>    - 业务人员可自定义工单字段（如设备故障类型、处理时长），无需开发介入；
> 2. 实时监控大屏：
>    - 拖拽专用组件（拓扑图、指标表盘、告警列表），对接实时数据接口；
>    - 支持大屏布局一键适配不同尺寸的监控室屏幕；
> 3. 报表分析系统：
>    - 可视化配置报表模板（如日 / 周 / 月负荷报表），自动从数据库拉取数据；
>    - 支持报表导出、定时推送，满足监管上报需求。

## 三、问题与解决方案类

### 1. 低代码平台的常见痛点？场景如何解决？

> 低代码平台在应用中易遇到 4 类痛点，对应解决方案如下：
>
> 1. 灵活性不足（无法满足复杂业务逻辑）：
>
>    解决方案：采用 “低代码 + 原生代码” 混合开发 —— 核心复杂逻辑（如算法、实时数据解析）用原生代码开发为自定义插件，低代码平台调用插件，兼顾效率与灵活性；
>
> 2. 性能问题（数据量大，页面卡顿）：
>
>    解决方案：
>
>    ① 配置数据分页 / 虚拟滚动，避免一次性加载上万条设备数据；
>
>    ② 开启组件缓存，对高频访问的监控页面缓存渲染结果；
>
>    ③ 对接时序数据库，优化数据查询效率；
>
> 3. 国产化适配问题（要求国产化软硬件）：
>
>    解决方案：选择适配国产化操作系统（麒麟、统信）、数据库（达梦、人大金仓）、浏览器（360 安全浏览器、中标麒麟浏览器）的低代码平台，提前做兼容性测试；
>
> 4. 数据安全问题（数据涉密）：
>
>    解决方案：
>    
>    ① 配置细粒度权限（如按区域 / 岗位控制数据可见性）；
>    
>    ② 对接统一身份认证系统（如 LDAP）；
>    
>    ③ 数据传输加密（HTTPS/WSS），操作日志全程留痕。

### 2. 低代码平台如何与现有系统集成？

> 企业已有大量 legacy 系统（如 SCADA、EMS、设备管理系统），低代码平台的集成方案如下：
>
> 1. **接口集成**：通过低代码平台的 “API 连接器” 对接现有系统的 RESTful API、WebService 接口，实现数据互通（如从 SCADA 系统拉取实时数据）；
> 2. **数据库集成**：直连现有系统的数据库（需做好权限控制），支持跨库查询（如关联设备台账库和告警库生成报表）；
> 3. **消息集成**：对接消息中间件（如 RabbitMQ、Kafka），实现系统间事件联动（如设备告警触发工单系统派单）；
> 4. **单点登录集成**：对接统一认证中心（如 CAS、OAuth2.0），实现低代码应用与现有系统的单点登录，无需重复登录；
> 5. **嵌入式集成**：将低代码开发的应用以 iframe / 组件形式嵌入现有系统，保持操作体验一致。

## 四、进阶思考类

### 1. 低代码平台的技术选型思路？

> 场景选择低代码平台需重点关注 6 个维度：
>
> 1. **国产化适配**：必须支持国产化操作系统、数据库、浏览器，符合等保三级要求；
> 2. **行业适配性**：是否提供预制组件（如拓扑图、设备监控、工单流程），减少自定义开发成本；
> 3. **扩展性**：支持自定义组件、自定义 API、原生代码嵌入，满足复杂业务需求；
> 4. **性能**：能支撑海量数据的加载与渲染（如十万级设备数据），支持分布式部署；
> 5. **安全合规**：具备数据加密、权限管控、日志审计能力，符合数据安全规范；
> 6. **生态兼容性**：能与现有中间件、大数据平台、物联网平台集成。

### 2. 低代码开发的未来发展趋势？对前端开发的影响？

> 低代码的未来趋势及对前端开发的影响，结合场景分析：
>
> 1. **趋势 1：行业化深耕**：低代码平台会针对业推出专属版本，预制更多行业组件和业务模板，降低行业适配成本；
>
> 2. **趋势 2：AI 赋能**：AI 可自动生成应用模板、智能识别业务需求并推荐组件配置、自动排查低代码应用的性能问题；
>
> 3. **趋势 3：全栈低代码**：从前端可视化延伸到后端接口、数据库配置，实现 “一站式全栈开发”；
>
> 4. 对前端开发的影响：
>
>    - 前端开发不再局限于 “纯编码”，需掌握低代码平台的组件开发、集成适配能力；
>- 核心价值从 “写代码” 转向 “设计解决方案”—— 如为场景设计低代码 + 原生代码的混合开发方案；
>    - 需深入理解业务，将业务逻辑转化为低代码平台的配置规则，成为 “业务 + 技术” 的复合型开发者。

## 五、总结

### 低代码开发核心关键点

1. 低代码是 “可视化配置 + 少量编码” 的开发模式，核心价值是提效降本，场景需重点关注国产化、安全性、扩展性；
2. 实战中需掌握 “可视化搭建 + 自定义组件扩展 + 系统集成” 三大核心能力，适配复杂业务需求；
3. 场景下低代码并非替代传统开发，而是 “低代码 + 原生代码” 混合模式，平衡效率与灵活性



# 数据可视化 & 数据大屏 

# 一、基础概念类

## 1. 什么是数据可视化？

> 数据可视化就是把**结构化 / 非结构化数据**通过**图表、图形、地图、大屏**等视觉方式呈现，让数据更容易理解、分析、决策。
>
> 前端常用技术：ECharts、D3.js、Three.js、AntV、Highcharts 等。

## 2. 什么是数据大屏？

> 数据大屏是**超高分辨率、实时更新、视觉冲击力强**的数据展示系统，
>
> 常用于监控中心、指挥中心、运营大屏。
>
> 特点：
>
> - 大分辨率（1920*1080、3840*2160、4K）
> - 实时数据推送
> - 视觉效果强
> - 自适应布局
> - 数据告警、联动

------

# 二、大屏适配方案（面试官必问）

## 3. 前端大屏如何做自适应？

背诵版答案：

> 大屏适配主要有三种方案：
>
> 1. scale 缩放方案（最常用、最稳定）
>
>    给最外层容器设置 transform: scale()，按设计稿比例动态计算缩放值，保持全屏不变形。
>
> 2. vw/vh 方案
> 
>    全部使用相对单位，1vw = 屏幕宽度的 1%。
>
> 3. rem + 动态根字体
>
>    根据屏幕宽度计算 html 的 font-size。

**企业级项目首选：scale 方案**

因为大屏一般是固定分辨率，不会被用户缩放，最稳定。

## 4. 大屏 scale 适配核心代码（可背）

```javascript
function setScale() {
  const designWidth = 1920
  const designHeight = 1080
  const scaleX = document.body.clientWidth / designWidth
  const scaleY = document.body.clientHeight / designHeight
  document.querySelector('#app').style.transform = `scale(${scaleX}, ${scaleY})`
}
```

------

# 三、核心技术栈类

## 5. 前端数据可视化常用库有哪些？

背诵答案：

1. **ECharts** —— 图表最多、配置简单、企业使用率最高（、大屏首选）
2. **AntV G2/G6** —— 阿里出品，关系图、流程图、可视化架构图
3. **D3.js** —— 高度自定义，底层绘图
4. **Three.js** —— 3D 可视化、3D 机房、3D 拓扑
5. **WebGL** —— 高性能渲染

## 6. 为什么大屏项目都用 ECharts？

> 因为 ECharts：
>
> - 配置简单
>
> - 图表类型丰富
>
> - 性能好
>
> - 支持大数据量
>
> - 支持自定义样式
>
> - 支持动态刷新、轮播、告警
>
>   非常适合、监控、实时数据大屏。

------

# 四、ECharts 

## 7. ECharts 实现实时更新数据怎么做？

> 使用 `setInterval` 或 WebSocket 推送新数据，
>
> 调用 `chart.setOption()` 更新数据即可。

## 8. ECharts 如何优化性能？（高频）

背诵版：

1. 开启 `animation: false` 关闭动画
2. 使用 `throttle` 节流更新
3. 大数据量使用 `dataZoom`
4. 尽量复用 chart 实例，不重复创建
5. 页面隐藏时停止更新
6. 使用 WebWorker 处理数据

## 9. ECharts 响应式如何做？

> 监听 window.onresize 事件，调用 `chart.resize()`。

# 五、数据大屏核心技术（面试官最爱问）

## 10. 大屏数据从哪里来？

1. **接口轮询**
2. **WebSocket 实时推送**（、监控大屏必用）
3. **MQTT**（物联网设备数据）
4. **Kafka + WebSocket**（超大型实时数据）
5. 本地 mock

## 11. 大屏为什么要用 WebSocket？

> 因为大屏需要**秒级 / 毫秒级实时数据**，
>
> HTTP 轮询浪费资源，
>
> WebSocket 长连接、低延迟、轻量、服务端可主动推送。

## 12. 大屏常见视觉效果技术

1. **动态背景**：Canvas / SVG
2. **地球、3D 拓扑**：Three.js / ECharts GL
3. **流光效果**：CSS 动画 + SVG
4. **科技感边框**：伪元素 + 渐变 + 动画
5. **数字滚动**：countUp.js

------

# 六、 / 监控大屏业务类（你最需要）

## 13. 大屏一般展示什么内容？

背诵版：

- 实时负荷、电流、电压
- 设备状态（正常 / 告警 / 离线）
- 告警统计、告警等级
- 场站分布、线路拓扑
- 发电量、功率、负荷预测
- 设备在线率
- 实时告警推送

## 14. 前端在大屏项目中负责什么？

1. 页面布局与自适应
2. ECharts 图表开发
3. WebSocket 实时数据接入
4. 告警联动、颜色状态
5. 3D 拓扑 / 地图渲染
6. 性能优化

## 15. 大屏遇到卡顿怎么优化？（超级高频）

背诵答案：

1. **降低 ECharts 渲染频率**（节流）
2. **关闭 ECharts 动画**
3. **WebSocket 数据合并推送**
4. **使用虚拟列表**避免大量 DOM
5. **页面隐藏时暂停渲染**
6. **使用 WebWorker 计算数据**
7. **图片压缩、懒加载**

------

# 七、综合面试题（最容易被问）

## 16. 你们大屏用什么技术栈？

标准回答：

> Vue3 + Vite + ECharts + WebSocket + Scale 适配 + Element Plus

## 17. 你做过数据大屏吗？遇到什么难点？

标准背诵话术：

> 做过实时监控大屏。
>
> 主要难点：
>
> 1. **大屏多分辨率适配**
>2. **海量数据实时更新不卡顿**
> 3. **WebSocket 断线重连与心跳**
> 4. **ECharts 性能优化**
> 5. **视觉效果还原**



# Promise 及相关技术

## 一、核心概念类（必背）

### 1. 什么是 Promise？解决了什么问题？

> Promise 是 ES6 引入的**异步编程解决方案**，本质是一个对象，用于表示异步操作的最终完成（或失败）及其结果值。
>
> 它主要解决了传统回调函数带来的两大问题：
>
> 1. **回调地狱**：异步操作嵌套调用时，代码层层缩进、可读性差、维护成本高（如场景中先获取设备列表→再查设备数据→最后推送告警，嵌套回调会非常繁琐）；
>
> 2. 异步操作难以管理：无法方便地实现异步操作的串行 / 并行执行、错误捕获（如多设备数据并行请求时，传统回调难以统一处理成功 / 失败）。
>
>    补充（场景）：在实时数据大屏中，Promise 可统一管理 WebSocket 连接、多接口数据请求、告警推送等异步操作，让代码逻辑更清晰。

### 2. Promise 的三种状态及特点？

> Promise 有且仅有三种状态，状态一旦改变就**不可逆**：
>
> 1. **pending（等待态）**：初始状态，异步操作未完成（如接口请求中、WebSocket 连接建立中）；
>
> 2. **fulfilled（成功态）**：异步操作成功（如接口返回数据、WebSocket 连接成功）；
>
> 3. rejected（失败态）：异步操作失败（如接口超时、WebSocket 连接断开）。
>
>    核心特点：
>
> - 状态只能从 pending → fulfilled 或 pending → rejected；
> - 状态改变后，无论多次调用 `then`/`catch`，都会立即返回该状态的结果。

### 3. Promise 的基本用法（核心代码模板）

```javascript
// 基础示例（场景：请求单台设备数据）
const getPowerDeviceData = (deviceId) => {
  return new Promise((resolve, reject) => {
    // 模拟异步操作：调用设备数据接口
    fetch(`/api/power/device/${deviceId}`)
      .then(res => res.json())
      .then(data => {
        // 异步成功，触发resolve
        resolve(data); 
      })
      .catch(err => {
        // 异步失败，触发reject
        reject(`设备${deviceId}数据请求失败：${err.message}`); 
      });
  });
};

// 调用Promise
getPowerDeviceData('dev-1001')
  .then(data => {
    console.log('设备数据：', data); // 处理成功数据（如渲染到大屏）
  })
  .catch(err => {
    console.error('错误：', err); // 处理失败（如触发告警）
  });
```

## 二、核心方法类（高频考点）

### 1. Promise.prototype.then()

> `then` 方法是 Promise 的核心，用于处理异步操作的成功 / 失败结果：
>
> 1. 接收两个参数：`onFulfilled`（成功回调）、`onRejected`（失败回调，可选）；
> 2. **返回一个新的 Promise**，支持链式调用（解决回调地狱的关键）；
> 3. 场景示例（链式调用）：
>
> ```javascript
>// 先获取区域列表 → 再获取该区域设备列表 → 最后获取首台设备数据
> getAreaList()
>   .then(areaList => {
>     const targetArea = areaList[0];
>     return getDeviceList(targetArea.id); // 返回新的Promise
>   })
>   .then(deviceList => {
>     const firstDevice = deviceList[0];
>     return getPowerDeviceData(firstDevice.id); // 继续返回新的Promise
>   })
>   .then(deviceData => {
>     console.log('最终数据：', deviceData); // 渲染到大屏
>   })
>   .catch(err => {
>     console.error('流程错误：', err); // 统一捕获所有环节的错误
>   });
> ```

### 2. Promise.prototype.catch()

> `catch` 用于捕获 Promise 链中的所有错误，等价于 `then(null, onRejected)`：
>
> 1. 捕获范围：当前 Promise 及之前所有链式调用中抛出的错误；
> 2. 场景注意点：系统对错误敏感，需通过 `catch` 统一处理接口超时、数据格式异常等问题，避免大屏崩溃；
> 3. 示例：
>
> ```javascript
> getPowerDeviceData('dev-1001')
>   .then(data => {
>     if (!data.voltage) {
>       throw new Error('数据缺失'); // 主动抛出错误
>     }
>     return data;
>   })
>   .catch(err => {
>     // 捕获接口错误 + 主动抛出的错误
>     console.error('数据异常：', err);
>     // 兜底处理：使用备用数据
>     return { voltage: 0, status: '异常' };
>   });
> ```

### 3. Promise.prototype.finally()

> `finally` 方法在 Promise 状态改变后**无论成功 / 失败都会执行**，常用于清理操作：
>
> 1. 不接收参数，无法判断 Promise 状态；
> 2. 场景：关闭数据加载动画、释放 WebSocket 连接资源、记录操作日志等；
> 3. 示例：
>
> ```javascript
> getPowerDeviceData('dev-1001')
>   .then(data => renderData(data))
>   .catch(err => handleError(err))
>   .finally(() => {
>     // 无论成功/失败，都关闭加载动画
>     document.querySelector('.loading').style.display = 'none';
>   });
> ```

### 4. Promise.all()

> 用于**并行执行多个异步操作**，全部成功才返回，任意一个失败则立即失败：
>
> 1. 接收一个可迭代对象（如数组），元素为 Promise 实例；
>2. 返回一个新 Promise，成功结果为所有 Promise 结果的数组（顺序与传入一致）；
> 3. 场景：同时请求多台设备数据，全部返回后统一渲染大屏；
> 4. 示例：
> 
> ```javascript
>// 并行请求3台设备数据
> const promiseArr = [
>   getPowerDeviceData('dev-1001'),
>   getPowerDeviceData('dev-1002'),
>   getPowerDeviceData('dev-1003')
> ];
> 
> Promise.all(promiseArr)
>   .then(allData => {
>     // allData 是3台设备数据的数组，顺序与promiseArr一致
>     renderDeviceList(allData); // 渲染到
>   })
>   .catch(err => {
>     // 任意一台设备请求失败，立即触发
>     console.error('部分设备数据请求失败：', err);
>   });
> ```

### 5. Promise.race()

> 多个异步操作**竞速执行**，只要有一个完成（成功 / 失败），就返回该结果：
>
> 1. 接收可迭代对象，返回新 Promise；
> 2. 场景：接口请求设置超时时间（接口请求与定时器竞速）；
> 3. 示例：javascript运行
>
> ```javascript
> // 接口请求，超时时间5秒
> const requestWithTimeout = (deviceId) => {
>   const requestPromise = getPowerDeviceData(deviceId);
>   const timeoutPromise = new Promise((_, reject) => {
>     setTimeout(() => reject(new Error('请求超时')), 5000);
>   });
>   // 竞速：接口请求 vs 5秒定时器
>   return Promise.race([requestPromise, timeoutPromise]);
> };
> 
> requestWithTimeout('dev-1001')
>   .then(data => console.log(data))
>   .catch(err => {
>     console.error('接口超时：', err); // 触发超时告警
>   });
> ```

### 6. Promise.allSettled()

> 等待所有异步操作**全部完成（无论成功 / 失败）**，返回所有操作的结果：
>
> 1. 成功结果为数组，每个元素包含 `status`（fulfilled/rejected）和 `value`/`reason`；
>2. 场景：批量请求设备数据，即使部分失败，也展示成功的数据，失败的标注异常；
> 3. 示例：
> 
> ```javascript
>Promise.allSettled(promiseArr)
>   .then(results => {
>     results.forEach((result, index) => {
>       if (result.status === 'fulfilled') {
>         renderDeviceData(index, result.value); // 渲染成功数据
>       } else {
>         renderDeviceError(index, result.reason); // 标注失败设备
>       }
>     });
>   });
> ```

### 7. Promise.resolve() / Promise.reject()

> 快速创建已决议的 Promise 实例：
>
> 1. `Promise.resolve(value)`：创建成功态的 Promise，value 可为普通值 / 另一个 Promise；
> 2. `Promise.reject(reason)`：创建失败态的 Promise，reason 为错误原因；
> 3. 场景：封装同步数据为 Promise 格式，统一异步代码逻辑；
> 4. 示例：javascript运行
>
> ```javascript
> // 同步数据转为Promise（静态配置数据）
> const getStaticConfig = () => {
>   const config = { threshold: 220, unit: 'kV' };
>   return Promise.resolve(config); // 统一返回Promise，适配异步逻辑
> };
> ```

## 三、进阶应用类（场景适配）

### 1. async/await 与 Promise 的关系？

> async/await 是 ES7 引入的**语法糖**，底层完全基于 Promise 实现，让异步代码更像同步代码：
>
> 1. `async` 函数返回值一定是 Promise（无论是否显式返回）；
> 2. `await` 只能在 `async` 函数中使用，等待 Promise 状态改变并返回结果；
> 3. 场景示例（简化异步逻辑）：javascript运行
>
> ```javascript
> // 异步函数：获取区域设备数据
> async function getAreaDeviceData(areaId) {
>   try {
>     // 等待区域列表请求完成
>     const areaList = await getAreaList();
>     // 等待设备列表请求完成
>     const deviceList = await getDeviceList(areaId);
>     // 等待多设备数据并行请求完成
>     const deviceData = await Promise.all(
>       deviceList.map(dev => getPowerDeviceData(dev.id))
>     );
>     return deviceData;
>   } catch (err) {
>     // 统一捕获所有异步错误
>     console.error('数据请求失败：', err);
>     throw err; // 向上抛出，供外层处理
>   } finally {
>     console.log('请求流程结束');
>   }
> }
> 
> // 调用
> getAreaDeviceData('area-001')
>   .then(data => renderData(data))
>   .catch(err => handleError(err));
> ```

### 2. Promise 如何解决回调地狱？

> 回调地狱示例（场景）：javascript运行
>
> ```javascript
> // 传统回调：获取区域→设备→数据→推送告警（层层嵌套）
> getAreaList(function(areaList) {
>   getDeviceList(areaList[0].id, function(deviceList) {
>     getPowerDeviceData(deviceList[0].id, function(deviceData) {
>       pushAlarm(deviceData, function(res) {
>         console.log('告警推送成功');
>       }, function(err) {
>         console.error('告警推送失败');
>       });
>     }, function(err) {
>       console.error('设备数据请求失败');
>     });
>   }, function(err) {
>     console.error('设备列表请求失败');
>   });
> }, function(err) {
>   console.error('区域列表请求失败');
> });
> ```
>
> Promise 解决方案（链式调用 /async/await）：javascript运行
>
> ```javascript
> // 方案1：Promise 链式调用
> getAreaList()
>   .then(areaList => getDeviceList(areaList[0].id))
>   .then(deviceList => getPowerDeviceData(deviceList[0].id))
>   .then(deviceData => pushAlarm(deviceData))
>   .then(res => console.log('告警推送成功'))
>   .catch(err => console.error('流程错误：', err));
> 
> // 方案2：async/await（更简洁）
> async function handleAlarmFlow() {
>   try {
>     const areaList = await getAreaList();
>     const deviceList = await getDeviceList(areaList[0].id);
>     const deviceData = await getPowerDeviceData(deviceList[0].id);
>     const res = await pushAlarm(deviceData);
>     console.log('告警推送成功');
>   } catch (err) {
>     console.error('流程错误：', err);
>   }
> }
> ```

### 3. Promise 错误捕获的注意事项（场景重点）

> 1. **未捕获的 Promise 错误**：系统中未捕获的 Promise 错误会导致大屏卡死，需确保所有 Promise 都有 `catch` 或 `try/catch`；
> 2. **链式调用中的错误**：错误会沿 Promise 链向下传递，直到被 `catch` 捕获；
> 3. **async/await 的错误捕获**：必须用 `try/catch` 包裹，否则错误会静默失败；
> 4. **全局捕获**：大屏可通过 `window.addEventListener('unhandledrejection')` 捕获未处理的 Promise 错误，兜底处理。

## 四、面试高频问题（直接背诵）

### 1. Promise.all 和 Promise.race 的区别？

> 1. Promise.all：并行执行，**全部成功才返回成功结果数组**，任意一个失败则立即失败；
>
> 2. Promise.race：竞速执行，
>
>    只要有一个完成（成功 / 失败）就返回该结果
>
>    ，其余操作仍会执行但结果被忽略；
>
>    场景：all 用于批量获取核心设备数据（缺一不可），race 用于接口超时控制。

### 2. async/await 相比 Promise 链式调用有什么优势？

> 1. 代码更简洁，逻辑更清晰，接近同步代码写法；
> 2. 错误处理更直观（try/catch 统一捕获），无需在链式调用中多次处理错误；
> 3. 便于调试（断点可直接命中 await 行）。

### 3. Promise 为什么状态不可逆？

> 为了保证异步操作结果的**确定性**：如果状态可逆转，同一个 Promise 可能多次触发成功 / 失败回调，导致业务逻辑混乱（如告警推送多次、数据重复渲染）。

### 4. 手写 Promise 核心逻辑（简化版）

```javascript
class MyPromise {
  constructor(executor) {
    this.status = 'pending'; // 初始状态
    this.value = undefined; // 成功结果
    this.reason = undefined; // 失败原因
    this.onFulfilledCallbacks = []; // 成功回调队列
    this.onRejectedCallbacks = []; // 失败回调队列

    // 成功回调
    const resolve = (value) => {
      if (this.status === 'pending') {
        this.status = 'fulfilled';
        this.value = value;
        // 执行所有成功回调
        this.onFulfilledCallbacks.forEach(fn => fn());
      }
    };

    // 失败回调
    const reject = (reason) => {
      if (this.status === 'pending') {
        this.status = 'rejected';
        this.reason = reason;
        // 执行所有失败回调
        this.onRejectedCallbacks.forEach(fn => fn());
      }
    };

    try {
      executor(resolve, reject);
    } catch (err) {
      reject(err);
    }
  }

  then(onFulfilled, onRejected) {
    // 兼容不传回调的情况
    onFulfilled = typeof onFulfilled === 'function' ? onFulfilled : v => v;
    onRejected = typeof onRejected === 'function' ? onRejected : e => { throw e };

    const newPromise = new MyPromise((resolve, reject) => {
      if (this.status === 'fulfilled') {
        // 异步执行，保证newPromise已创建
        setTimeout(() => {
          try {
            const res = onFulfilled(this.value);
            resolve(res);
          } catch (err) {
            reject(err);
          }
        });
      }

      if (this.status === 'rejected') {
        setTimeout(() => {
          try {
            const res = onRejected(this.reason);
            resolve(res);
          } catch (err) {
            reject(err);
          }
        });
      }

      if (this.status === 'pending') {
        // 存入回调队列
        this.onFulfilledCallbacks.push(() => {
          setTimeout(() => {
            try {
              const res = onFulfilled(this.value);
              resolve(res);
            } catch (err) {
              reject(err);
            }
          });
        });

        this.onRejectedCallbacks.push(() => {
          setTimeout(() => {
            try {
              const res = onRejected(this.reason);
              resolve(res);
            } catch (err) {
              reject(err);
            }
          });
        });
      }
    });

    return newPromise; // 支持链式调用
  }

  catch(onRejected) {
    return this.then(null, onRejected);
  }

  finally(callback) {
    return this.then(
      value => MyPromise.resolve(callback()).then(() => value),
      reason => MyPromise.resolve(callback()).then(() => { throw reason })
    );
  }
}

// 静态方法
MyPromise.resolve = function(value) {
  return new MyPromise(resolve => resolve(value));
};

MyPromise.reject = function(reason) {
  return new MyPromise((_, reject) => reject(reason));
};
```

## 五、总结

### Promise 核心关键点

1. Promise 是异步编程解决方案，解决回调地狱，状态不可逆（pending→fulfilled/rejected）；
2. 核心方法：then/catch/finally 用于链式处理，all/race/allSettled 用于多异步操作管理；
3. async/await 是 Promise 语法糖，让异步代码更简洁，场景中可简化多步骤异步逻辑；
4. 前端需重点掌握：Promise 批量请求设备数据、接口超时控制、错误统一捕获，保障大屏稳定性。

# 事件队列

------

## 1. 核心定义：什么是事件循环？

- **背诵话术：**

  > “JavaScript 是单线程的，为了处理异步操作（如 `setTimeout`、`Promise`、`WebSocket` 接收数据）而不阻塞主线程，浏览器设计了**事件循环机制**。 它不断地从**任务队列**中读取任务并放入**执行栈**中执行。其核心流程是：执行同步代码 -> 处理微任务 -> 尝试渲染更新 -> 执行宏任务。”

------

## 2. 宏任务（MacroTask） vs 微任务（MicroTask）

面试中经常要求区分两者，这是理解执行顺序的关键。

- **背诵话术：**

  > “任务分为宏任务和微任务，它们的优先级和执行时机不同：
  >
  > - **微任务（Microtask）：** 优先级更高。常见的有 `Promise.then`、`MutaionObserver` 以及 Vue 的 `nextTick`。在一个宏任务执行完后，会清空**所有**当前排队的微任务。
  > - **宏任务（Macrotask）：** 常见的有 `script` 整体代码、`setTimeout`、`setInterval`、`I/O` 以及你 JD 中提到的 `WebSocket` 事件。 **结论：** 每次执行完一个宏任务后，都会检查并清空整个微任务队列，然后再去取下一个宏任务。”

------

## 3. 结合 Vue3 的实战理解

针对你岗位中的 **Vue3** 要求，面试官可能会问：**“为什么 Vue 的 DOM 更新是异步的？”**

- **背诵话术：**

  > “Vue 的响应式更新利用了**微任务队列**。当你修改数据后，Vue 不会立即更新 DOM，而是开启一个队列，缓冲在同一事件循环中发生的所有数据变更。 这样做是为了**性能优化**：如果在同步代码中连续修改 100 次数据，Vue 会通过 `Promise.then`（微任务）在当前任务结束前只触发一次 DOM 更新，避免了不必要的重复渲染。”

------

## 4. 结合监控大屏的性能优化

针对你 JD 中的“复杂数据对接”和“性能优化”，这是一个高阶回答点。

- **面试场景：** “WebSocket 每秒推送大量数据，导致页面掉帧怎么办？”

- **背诵话术：**

  > “如果 WebSocket 的回调（宏任务）执行太频繁，或者回调里逻辑太重，会挤占浏览器的渲染时间（通常 16.7ms 一帧）。 我的优化思路是利用 **`requestAnimationFrame` (rAF)**。rAF 的执行时机在微任务之后、浏览器重绘之前。我会将 WebSocket 收到的数据先存在缓冲区，然后通过 rAF 在浏览器下一次重绘前统一处理 UI 更新，这样能确保数据更新频率与屏幕刷新率同步，避免丢帧和掉帧。”

------

### 💡 记忆口诀

1. **先同步，后异步。**
2. **异步里面：先微后宏。**
3. **微任务清空，宏任务只取一个（执行完后再看微任务）。**

# 数据洪流

#### 第一阶段：定位问题（发现现象）

“首先，我会复现环境。我会打开 Chrome DevTools 的 **Performance** 选项卡进行录制，观察帧率（FPS）和 CPU 占用。如果看到 FPS 随着时间推移不断下降，而 **Memory** 选项卡中的 **Heap（堆内存）** 曲线呈阶梯状上升且从不回落，我基本可以断定发生了**内存泄漏**。”

#### 第二阶段：深度溯源（寻找真凶）

“接着，我会使用 **Heap Snapshot（堆快照）** 进行对比分析：

1. 在页面刚打开时取一个快照，运行 1 小时后再取一个。
2. 通过 **Comparison（对比）** 视图，查看哪些对象（如 `Object` 或 `Array`）增量最大。
3. 在这种高频 WebSocket 场景下，我通常会重点检查两个地方：
   - **未销毁的监听器：** 是否在组件卸载或频繁重连时，重复绑定了 WebSocket 的 `onmessage` 事件或 `setInterval`。
   - **闭包引用：** WebSocket 的回调函数中是否引用了已经销毁的组件变量，导致垃圾回收（GC）无法释放内存。”

#### 第三阶段：针对 Vue3 的专项优化

“针对 Vue3，我会检查数据响应式的压力：

- **过度响应式：** 每秒推几百条数据，如果我们用 `ref` 或 `reactive` 包装整个巨大的设备状态树，Vue 的依赖追踪（Track/Trigger）会消耗大量 CPU。
- **解决方案：** 我会将这些仅用于展示、不涉及表单修改的数据，改用 **`shallowRef`** 或 **`markRaw`**。这样 Vue 只会监听对象引用的变化，而不会递归监听内部属性，能显著降低内存开销和渲染压力。”

#### 第四阶段：网络与渲染层优化

“如果内存正常但依然卡顿，那就是**主线程阻塞（Main Thread Blocking）**：

- **数据节流：** 我会引入一个‘缓冲区’。WebSocket 收到数据后不立即推给 Vue，而是先存入数组，利用 `requestAnimationFrame` 每 16.7ms 批量更新一次 UI。
- **长连接溢出：** 检查 WebSocket 是否因为服务端推送过快，导致前端处理不过来，堆积了大量的‘待处理任务’。我会建议后端增加**按需订阅**机制，只推送当前可视区域内的设备数据。”



# Vue2/Vue3 高频面试八股文（核心考点 + 标准答案）

## 一、Vue 核心基础（通用）

### 1. Vue 核心特性

- **渐进式框架**：按需使用核心功能（如模板、响应式），无需一次性引入所有特性，可逐步集成路由、状态管理等；
- **双向数据绑定**：v-model 语法糖，简化表单交互（Vue2/Vue3 底层实现有差异）；
- **组件化开发**：将页面拆分为独立可复用组件，降低耦合度；
- **虚拟 DOM**：通过抽象 DOM 结构减少真实 DOM 操作，提升渲染性能；
- **指令系统**：内置 v-if/v-for/v-bind 等指令，简化 DOM 操作逻辑。

### 2. Vue2 vs Vue3 核心差异（高频）

|      维度       |          Vue2           |                             Vue3                             |
| :-------------: | :---------------------: | :----------------------------------------------------------: |
|    核心架构     |  Options API（选项式）  |         Composition API（组合式）+ Options API 兼容          |
|   响应式原理    |  Object.defineProperty  |                       Proxy + Reflect                        |
|    生命周期     | beforeCreate/created 等 | setup 替代 beforeCreate/created，其余钩子加 `on` 前缀（如 onMounted） |
|     根节点      |    只能有一个根节点     |                   支持多根节点（Fragment）                   |
|    数据定义     |     data 必须是函数     |                setup 中通过 ref/reactive 定义                |
|      性能       |     无专属优化 API      |            支持 Teleport、Suspense，重写虚拟 DOM             |
| TypeScript 支持 |    弱（需额外配置）     |                  原生支持（源码用 TS 重构）                  |

## 二、核心考点详解

### 1. 响应式原理（面试必问）

#### Vue2 响应式原理

- **核心 API**：`Object.defineProperty()`，劫持对象的 `get/set` 方法；

- 实现逻辑：

  1. 遍历 data 中的所有属性，为每个属性添加 `get/set`；
  2. 读取属性时触发 `get`，收集依赖（Watcher）；
  3. 修改属性时触发 `set`，通知依赖更新视图；

- 缺陷：

  - 无法监听数组下标 / 长度变化（如 `arr[0] = 1`、`arr.length = 0`）；
  - 无法监听对象新增 / 删除属性（需手动调用 `Vue.set/$set`）；
  - 深度监听需递归遍历，性能开销大。


#### Vue3 响应式原理

- **核心 API**：`Proxy + Reflect`，代理整个对象而非单个属性；

- 实现逻辑：

  1. 通过 `Proxy` 代理目标对象，拦截对象的读取、修改、新增、删除等操作；
  2. `Reflect` 统一操作行为（如 `Reflect.get(target, key)`），保证返回值规范；
  3. 拦截操作时收集 / 触发依赖，实现响应式；

- 优势：

  - 支持监听数组下标 / 长度变化；
  - 支持监听对象新增 / 删除属性；
  - 非侵入式代理，无需递归遍历（懒代理，访问属性时才递归子对象）；
  - 支持 Map/Set 等复杂数据类型。


### 2. 组件通信方式（高频）

|    通信方式     |            Vue2 用法             |           Vue3 用法           |        适用场景        |
| :-------------: | :------------------------------: | :---------------------------: | :--------------------: |
|   Props/Emits   |   props 接收，this.$emit 触发    | props 接收，defineEmits 触发  | 父子组件通信（最常用） |
|   自定义事件    |        this.on/off/$emit         |      移除（推荐 Emits）       |     跨组件简易通信     |
|  事件总线 Bus   |  Vue.prototype.$bus = new Vue()  |          mitt 库替代          |     非父子组件通信     |
|   Vuex/Pinia    | Vuex （state/mutations/actions） |   Pinia（更轻量，支持 TS）    |      全局状态管理      |
| parent/children |    this.parent/this.children     |  移除（推荐 provide/inject）  |    父子组件直接访问    |
| provide/inject  |     this.provide/this.inject     |      provide/inject 函数      |      隔代组件通信      |
| attrs/listeners |    this.attrs/this.listeners     | 保留，listeners 合并到 $attrs |    透传属性 / 事件     |
| v-model 修饰符  |           .sync 修饰符           |   v-model:xxx 自定义绑定名    |    父子组件双向绑定    |

### 3. 生命周期（核心）

#### Vue2 生命周期（Options API）

```plaintext
创建阶段：beforeCreate → created（可访问数据，未挂载 DOM）
挂载阶段：beforeMount → mounted（DOM 挂载完成，可操作 DOM）
更新阶段：beforeUpdate → updated（数据更新，DOM 重新渲染）
销毁阶段：beforeDestroy → destroyed（组件销毁，清理定时器/事件）
```

#### Vue3 生命周期（Composition API）

- setup 替代 `beforeCreate/created`（setup 执行时，组件实例已创建但未挂载）；
- 其余钩子需加 `on` 前缀，且只能在 setup 中调用：

```plaintext
挂载：onBeforeMount → onMounted
更新：onBeforeUpdate → onUpdated
卸载：onBeforeUnmount → onUnmounted（替代 destroyed）
错误捕获：onErrorCaptured（新增）
```

### 4. 路由（Vue Router）

#### 核心考点

1. 路由模式：

   - hash 模式：基于 URL 哈希（#），兼容性好，无需后端配置；
   - history 模式：基于 HTML5 History API，URL 无 #，需后端配置（避免刷新 404）。

2. 路由守卫（高频）：

   - 全局守卫：`router.beforeEach`（登录鉴权）、`router.afterEach`；
   - 路由独享守卫：`beforeEnter`（路由配置中）；
   - 组件内守卫：`beforeRouteEnter`/`beforeRouteUpdate`/`beforeRouteLeave`。

3. 路由传参：

   - 动态路由：`/user/:id`，通过 `$route.params.id`（Vue2）/ `route.params.id`（Vue3）获取；
   - query 参数：`/user?id=1`，通过 `$route.query.id`（Vue2）/ `route.query.id`（Vue3）获取；
   - 注意：`$route` 是当前路由信息，`$router` 是路由实例（跳转用 `$router.push`）。


### 5. 状态管理

#### Vue2：Vuex

核心结构：`state`（状态）、`mutations`（同步修改状态）、`actions`（异步操作）、`getters`（计算属性）、`modules`（模块拆分）。

```javascript
// Vue2 中使用
new Vuex.Store({
  state: { count: 0 },
  mutations: { ADD(state, num) { state.count += num } },
  actions: { addAsync({ commit }, num) { setTimeout(() => commit('ADD', num), 1000) } },
  getters: { doubleCount: state => state.count * 2 }
})
```

#### Vue3：Pinia（替代 Vuex）

- 优势：无 mutations、支持 TS、自动代码分割、更轻量；

```javascript
// Vue3 中使用
import { defineStore } from 'pinia'
export const useCountStore = defineStore('count', {
  state: () => ({ count: 0 }),
  actions: { add(num) { this.count += num } }, // 同步/异步都写在 actions
  getters: { doubleCount: (state) => state.count * 2 }
})
```

### 6. 性能优化（高频）

1. 通用优化：

   - 合理使用 v-if/v-show（频繁切换用 v-show，否则用 v-if）；
   - v-for 加 key（避免就地复用，推荐用唯一 ID，不用 index）；
   - 避免 v-for 和 v-if 同用（可通过 computed 过滤数据）；
   - 异步组件（Vue2：`component: () => import('./Comp.vue')`；Vue3：`defineAsyncComponent`）。

2. Vue2 专属优化：

   - 冻结不响应式数据：`Object.freeze()`（避免被劫持）；
   - 按需引入组件：减少打包体积；
   - 自定义指令替代频繁 DOM 操作。

3. Vue3 专属优化：

   - 使用 `shallowRef/shallowReactive`（浅响应式，避免深度监听）；
   - 使用 `readonly`（只读数据，不收集依赖）；
   - 利用 Teleport 优化弹窗层级（避免样式隔离）；
   - Suspense 处理异步组件加载状态。


## 三、高频面试题及标准答案

### 1. Vue2 响应式的缺陷？Vue3 如何解决？

- Vue2 响应式基于 `Object.defineProperty`，缺陷有：① 无法监听数组下标 / 长度变化；② 无法监听对象新增 / 删除属性；③ 深度监听需递归遍历，性能差；
- Vue3 改用 `Proxy + Reflect` 代理整个对象，解决了上述问题：① 支持监听数组下标 / 长度；② 支持对象新增 / 删除属性；③ 懒代理（访问属性时才递归子对象），降低性能开销。

### 2. Composition API 对比 Options API 的优势？

- **代码复用**：Options API 复用逻辑需用 mixin（易冲突、来源不清晰），Composition API 可将逻辑抽为独立函数（Composables），复用更清晰；
- **逻辑聚合**：Options API 分散在 data/methods/watch 中，Composition API 可将相关逻辑写在 setup 中，便于维护；
- **TypeScript 支持**：Composition API 原生支持 TS，类型推导更友好；
- **灵活性**：可按需组合逻辑，无需受选项约束。

### 3. v-if 和 v-for 优先级？为什么不建议同用？

- Vue2 中 v-for 优先级高于 v-if，Vue3 中 v-if 优先级高于 v-for；
- 不建议同用的原因：① Vue2 中会先遍历所有元素再判断条件，浪费性能；② Vue3 中 v-if 执行时无法访问 v-for 的变量（如 `v-if="item.visible"` 中 item 未定义）；
- 解决方案：通过 computed 先过滤数据，再用 v-for 渲染。

### 4. 如何实现组件缓存？

- 使用 `keep-alive` 标签包裹需要缓存的组件，可缓存不活动的组件实例（避免重复渲染）；

- 核心属性：

  - `include`：仅缓存匹配的组件名；
  - `exclude`：不缓存匹配的组件名；
  - `max`：最多缓存的组件实例数；

- 生命周期：缓存组件会触发 `activated`（激活）/`deactivated`（失活）钩子，替代 destroyed。

### 5. Vue3 的 setup 中如何获取组件实例？

- Vue3 中 setup 没有 this，需通过 `getCurrentInstance()` 获取组件实例：

```javascript
import { getCurrentInstance } from 'vue'
setup() {
  const instance = getCurrentInstance()
  // 访问 router：instance.proxy.$router
  // 访问 store：instance.proxy.$store
  return {}
}
```

## 四、总结

### 关键点回顾

1. **响应式**：Vue2 用 `Object.defineProperty`（有缺陷），Vue3 用 `Proxy + Reflect`（更完善）；
2. **API 风格**：Vue2 以 Options API 为主，Vue3 主推 Composition API（逻辑复用 / TS 更友好）；
3. **核心优化**：面试高频考点包括组件通信、生命周期、v-if/v-for 优先级、性能优化、路由守卫等，需重点记忆。

## vue状态中断

### 1. Watcher 内部的中断：`onWatcherCleanup` (Vue 3.3+)

这是面试最常考的场景：当一个副作用（如 API 请求）还没完成，状态又变了，如何取消上一次的操作？

- **话术：** “在 Vue 3.3+ 中，我们可以利用 `watch` 或 `watchEffect` 回调中提供的 `onCleanup` 函数。
- **原理：** 当状态再次改变触发 watcher 前，Vue 会先执行上一次注册的 cleanup。
- **代码背诵：**

```javascript
watch(id, (newId, oldId, onCleanup) => {
  const controller = new AbortController(); // 创建中断控制器
  fetchData(newId, { signal: controller.signal }); 

  onCleanup(() => {
    controller.abort(); // 如果 id 变了，直接中断上一次未完成的请求
  });
});
```

### 2. 响应式层面的“降级”：`shallowRef` 与 `markRaw`

针对你 JD 里的**大数据可视化**，这是必杀技。如果数据量达到万级，深度响应式递归会卡死页面。

- **话术：** “对于大数据量（如 ECharts 的原始数据或 Three.js 实例），我会使用 `shallowRef` 代替 `ref`。
- **逻辑：** `shallowRef` 只监听 `.value` 本身的指向变化，不会对内部属性做响应式代理。这相当于**中断了深度监听**，极大降低了 CPU 消耗。如果是第三方库实例，我会用 `markRaw` 彻底防止它被变成 Proxy。将一个对象标记为不可被转为代理。返回该对象本身。

### 3. 架构级的中断：`effectScope` (生命周期管理)

如果你在做复杂的**插件式架构**（JD 要求 4），这个 API 能展现你的架构能力。

- **话术：** “当我们需要手动控制一组副作用时，可以使用 `effectScope`。它能像‘一键关灯’一样，中断在该作用域下产生的所有响应式监听（computed, watch 等），这在开发复杂插件或动态组件卸载时非常有用。”
- **代码背诵：**

```javascript
const scope = effectScope();
scope.run(() => {
  const doubled = computed(() => count.value * 2);
  watch(doubled, () => console.log(doubled.value));
});

scope.stop(); // 瞬间中断 scope 内所有的状态更新监听
```

### 4. 渲染频率的中断：防抖 (Debounce) 与 节流 (Throttle)

针对**高频交互**（如地图缩放、滚动）。

- **话术：** “在 UI 层面，我会通过 `customRef` 实现防抖响应式。在底层逻辑层，如果状态更新太频繁（比如鼠标移动触发的数据大屏动效），我会通过节流函数来‘中断’掉中间的更新过程，只允许最后一次或固定频率的更新进入渲染队列。”

------

### 💡 面试官可能的追问：

**“Vue 的 nextTick 此时起什么作用？”**

- **满分回答：** “Vue 的 DOM 更新是异步的，它会将一个周期内所有的状态变更缓冲在一个队列中（Job Queue）。`nextTick` 实际上是等待这个队列刷新完成。所以，我们通过上述手段‘中断’更新，本质上是在 **Job 进入队列前**将其拦截掉，从而避免不必要的 DOM 重绘。”

# Axios/Fetch/Ajax & HTTP/HTTPS 

## 一、核心概念铺垫

### 1. 前端请求的本质

前端通过 HTTP/HTTPS 协议向服务端发起**网络请求**，获取 / 提交数据，实现前后端交互；

- 早期：原生 Ajax（XMLHttpRequest）实现异步请求；
- 现代：Fetch API（浏览器原生）、Axios（基于 XHR 封装的库）成为主流。

### 2. 同步 / 异步请求（基础考点）

- **同步请求**：请求发出后，阻塞页面渲染 / 交互，直到拿到响应（如早期 `form` 提交）；
- **异步请求**：请求发出后，不阻塞页面，响应返回后通过回调 / Promise 处理（Ajax/Fetch/Axios 均为异步）。

## 二、Ajax/Fetch/Axios 核心对比

### 1. 三者核心差异表

表格

|     维度     |            Ajax（XMLHttpRequest）             |               Fetch API（原生）                |                  Axios（第三方库）                   |
| :----------: | :-------------------------------------------: | :--------------------------------------------: | :--------------------------------------------------: |
|   底层实现   |           基于 XMLHttpRequest 对象            |         浏览器原生 API（基于 Promise）         |            基于 XHR 封装（Promise 风格）             |
|   语法风格   |             回调函数（callback）              |                Promise 链式调用                |            Promise 链式 /async-await 风格            |
|    兼容性    |           兼容所有浏览器（包括 IE）           |             不兼容 IE，需 polyfill             |            兼容所有浏览器（内置兼容处理）            |
| 请求参数处理 |       需手动拼接 `formData`/JSON 字符串       |         默认不携带 Cookie，需手动配置          |          自动序列化 JSON，支持 URL 参数拼接          |
|   响应处理   |    需手动解析 `responseText`/`responseXML`    |      需手动调用 `.json()`/`.text()` 解析       |           自动解析 JSON 响应，直接返回数据           |
|   超时处理   |        需手动设置 `timeout` + 事件监听        |     无原生超时 API，需结合 `Promise.race`      |                内置 `timeout` 配置项                 |
|   中断请求   |              调用 `xhr.abort()`               |  调用 `controller.abort()`（AbortController）  | 调用 `cancelToken`（v0.x）/`AbortController`（v1.x） |
|    拦截器    |            无原生支持，需手动封装             |                   无原生支持                   |          内置请求 / 响应拦截器（核心优势）           |
|   错误处理   | 仅网络错误触发 `onerror`，状态码非 2xx 不触发 | 仅网络错误 reject，状态码 4xx/5xx 视为 resolve |        状态码非 2xx 直接 reject，统一错误处理        |
| 取消重复请求 |                  需手动封装                   |        需手动结合 AbortController 封装         |         可通过拦截器 + CancelToken 轻松实现          |
|   请求取消   |               支持，但逻辑繁琐                |            支持（AbortController）             |                  支持（封装更友好）                  |
|   进度监控   |     支持 `onprogress` 监听上传 / 下载进度     |           支持（`response.body` 流）           |     内置 `onUploadProgress`/`onDownloadProgress`     |

### 2. 逐个详解（用法 + 核心考点）

#### （1）Ajax（XMLHttpRequest）—— 原生底层

```javascript
// 原生 Ajax 示例（回调地狱问题明显）
const xhr = new XMLHttpRequest();
// 1. 初始化请求
xhr.open('GET', 'https://api.example.com/data', true); // true=异步
// 2. 设置请求头
xhr.setRequestHeader('Content-Type', 'application/json');
// 3. 监听响应状态
xhr.onreadystatechange = function() {
  // readyState=4 表示请求完成，status=200 表示响应成功
  if (xhr.readyState === 4 && xhr.status === 200) {
    const data = JSON.parse(xhr.responseText); // 手动解析 JSON
    console.log(data);
  } else if (xhr.readyState === 4) {
    console.error('请求失败：', xhr.status);
  }
};
// 4. 监听网络错误
xhr.onerror = function() {
  console.error('网络错误');
};
// 5. 设置超时
xhr.timeout = 5000;
xhr.ontimeout = function() {
  console.error('请求超时');
};
// 6. 发送请求
xhr.send(); // GET 请求不传参，POST 传 JSON.stringify(data)
```

**核心考点**：

- `readyState` 状态码：0（未初始化）→1（已打开）→2（已发送）→3（接收中）→4（完成）；
- 缺点：回调地狱、API 繁琐、需手动处理参数 / 解析响应。

#### （2）Fetch API —— 浏览器原生 Promise 方案

```javascript
// Fetch 基础示例
fetch('https://api.example.com/data', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({ name: 'test' }), // POST 需手动序列化
  credentials: 'include', // 携带 Cookie（默认不携带）
})
  .then(response => {
    // 注意：状态码 4xx/5xx 不会 reject，需手动判断
    if (!response.ok) { // response.ok 等价于 status >=200 && status <300
      throw new Error(`请求失败：${response.status}`);
    }
    return response.json(); // 手动解析 JSON
  })
  .then(data => console.log(data))
  .catch(error => console.error('错误：', error));
```

**核心考点**：

- Fetch 核心缺陷：

  ① 状态码 4xx/5xx 不会触发 catch，需手动判断 response.ok；

  ② 默认不携带 Cookie，需配置 credentials: 'include'；

  ③ 无原生超时配置，需结合 Promise.race

   实现：

  ```javascript
// Fetch 超时处理示例
  const fetchWithTimeout = (url, options = {}, timeout = 5000) => {
    return Promise.race([
      fetch(url, options),
      new Promise((_, reject) => 
        setTimeout(() => reject(new Error('请求超时')), timeout)
      )
    ]);
  };
  ```
  

#### （3）Axios —— 前端主流请求库

```javascript
// Axios 基础示例（支持 Promise/async-await）
import axios from 'axios';

// 1. 基础请求
axios({
  method: 'POST',
  url: 'https://api.example.com/data',
  params: { id: 1 }, // URL 参数（拼接在?后）
  data: { name: 'test' }, // 请求体参数
  timeout: 5000, // 超时时间
  headers: { 'token': 'xxx' }
})
.then(res => console.log(res.data)) // 自动解析 JSON
.catch(err => console.error(err));

// 2. 拦截器（核心优势）
// 请求拦截器：添加统一 token、修改请求参数
axios.interceptors.request.use(
  config => {
    config.headers.token = localStorage.getItem('token');
    return config;
  },
  error => Promise.reject(error)
);
// 响应拦截器：统一处理错误、解析数据
axios.interceptors.response.use(
  res => res.data, // 直接返回数据，简化上层调用
  error => {
    // 统一处理 401（未登录）、403（权限不足）
    if (error.response.status === 401) {
      router.push('/login');
    }
    return Promise.reject(error);
  }
);
```

**核心考点**：

- Axios 核心优势：

  ① 内置拦截器（请求 / 响应统一处理）；

  ② 自动序列化请求参数、解析响应数据；

  ③ 支持取消请求（CancelToken/AbortController）；

  ④ 统一错误处理（状态码 4xx/5xx 直接 reject）；

  ⑤ 支持请求 / 响应进度监控。

### 3. 面试高频题：为什么项目中优先用 Axios 而非 Fetch/Ajax？

答：

1. **开发效率**：Axios 封装了大量通用功能（拦截器、超时、取消请求），无需手动封装；
2. **错误处理**：Fetch 需手动判断状态码，Axios 自动将 4xx/5xx 视为错误，处理更统一；
3. **兼容性**：Axios 兼容 IE，Fetch 需额外引入 polyfill；
4. **功能丰富**：Axios 支持进度监控、取消重复请求、批量请求（`axios.all`），Fetch 需手动实现；
5. **代码风格**：基于 Promise，支持 async-await，避免 Ajax 的回调地狱。

## 三、HTTP/HTTPS 核心考点

### 1. HTTP 协议基础

- **定义**：超文本传输协议（HyperText Transfer Protocol），基于 TCP/IP 协议的应用层协议，用于传输超文本（HTML/JSON 等）；

- 核心特点：

  ① 无状态：每次请求独立，服务端不保存客户端状态（需 Cookie/Session 解决）；

  ② 无连接：HTTP/1.0 每次请求需建立新 TCP 连接，HTTP/1.1 支持长连接（Keep-Alive）；

  ③ 明文传输：数据以明文形式传输，易被劫持、篡改。

### 2. HTTP 请求方法（RESTful 核心）

|  方法   |         语义         | 幂等性 |         常用场景          |
| :-----: | :------------------: | :----: | :-----------------------: |
|   GET   |       获取资源       |   是   | 查询数据（如列表 / 详情） |
|  POST   |       创建资源       |   否   |    提交表单、新增数据     |
|   PUT   |   更新资源（全量）   |   是   |     完整更新一条数据      |
|  PATCH  |   更新资源（部分）   |   否   | 部分更新数据（如改昵称）  |
| DELETE  |       删除资源       |   是   |         删除数据          |
|  HEAD   |  获取响应头（无体）  |   是   |     检查资源是否存在      |
| OPTIONS | 查看服务器支持的方法 |   是   |   跨域预检请求（CORS）    |

**考点**：幂等性 → 多次请求结果一致（如 GET 多次请求都是查数据，POST 多次提交会创建多条数据）。

### 3. HTTP 状态码（高频）

按分类记忆，回答时结合场景：

- **1xx（信息）**：请求已接收，继续处理（如 100 Continue）；
- **2xx（成功）**：请求处理完成（200 OK、201 Created）；
- **3xx（重定向）**：需进一步操作（301 永久重定向、302 临时重定向、304 缓存未修改）；
- **4xx（客户端错误）**：请求有误（400 参数错误、401 未登录、403 权限不足、404 资源不存在、405 方法不允许）；
- **5xx（服务端错误）**：服务端处理失败（500 服务器内部错误、502 网关错误、503 服务不可用、504 网关超时）。

### 4. HTTP/HTTPS 核心差异（必问）

|   维度   |        HTTP        |           HTTPS            |
| :------: | :----------------: | :------------------------: |
| 安全级别 | 不安全（明文传输） |      安全（加密传输）      |
|   端口   |         80         |            443             |
| 底层协议 |      基于 TCP      | HTTP + SSL/TLS（基于 TCP） |
| 加密方式 |         无         |   对称加密 + 非对称加密    |
|   证书   |        无需        |   需 CA 颁发的 SSL 证书    |
|   性能   |  快（无加密开销）  |  稍慢（加密 / 解密耗时）   |

### 5. HTTPS 加密原理（核心考点）

HTTPS 结合**非对称加密**和**对称加密**，既保证密钥安全，又提升传输效率：

1. 握手阶段（非对称加密）：
   - 客户端向服务端发起请求，服务端返回公钥（含 SSL 证书）；
   - 客户端验证证书合法性（由 CA 机构签发），生成随机对称密钥，用服务端公钥加密后发送给服务端；
   - 服务端用私钥解密，获取对称密钥。
2. 数据传输阶段（对称加密）：
   - 后续所有数据传输，均通过「对称密钥」加密 / 解密（对称加密效率远高于非对称加密）。

**考点补充**：

- 非对称加密：公钥加密、私钥解密（用于传输对称密钥，保证密钥不被劫持）；
- 对称加密：同一密钥加密 / 解密（用于传输数据，保证效率）；
- CA 证书：解决「公钥伪造」问题，客户端通过 CA 机构验证服务端公钥的合法性。

### 6. HTTP/1.1 vs HTTP/2 vs HTTP/3（高频）

|   版本   |           核心特性           |          解决的问题           |
| :------: | :--------------------------: | :---------------------------: |
| HTTP/1.1 |      长连接、管道化请求      | 解决 HTTP/1.0 频繁建连的问题  |
|  HTTP/2  | 多路复用、二进制帧、头部压缩 |  解决 HTTP/1.1 队头阻塞问题   |
|  HTTP/3  |    基于 QUIC 协议（UDP）     | 解决 TCP 队头阻塞、握手慢问题 |

**考点**：队头阻塞 → HTTP/1.1 中一个连接同时只能处理一个请求，前一个请求阻塞会导致后续请求排队；HTTP/2 多路复用可在一个连接中并行处理多个请求。

### 7. 跨域问题（和请求强相关）

- **定义**：浏览器同源策略限制，当请求的「协议、域名、端口」任意一个与当前页面不同，即为跨域；

- 解决方案（高频）：

  ① CORS（服务端配置：Access-Control-Allow-Origin）；

  ② 代理（开发环境：webpack devServer proxy；生产环境：Nginx 反向代理）；

  ③ JSONP（仅支持 GET 请求，利用 script 标签无跨域限制）；

  ④ 其他：postMessage、WebSocket（无跨域限制）。

## 四、高频面试题及标准答案

### 1. HTTPS 如何保证数据传输安全？

HTTPS 结合「非对称加密 + 对称加密 + CA 证书」三层保障：

1. 非对称加密：握手阶段传输对称密钥，避免密钥被劫持；

2. 对称加密：数据传输阶段用对称密钥加密，保证传输效率；

3. CA 证书：验证服务端公钥合法性，防止公钥伪造；

   最终实现数据的「机密性（加密）、完整性（防篡改）、身份认证（CA 证书）」。

### 2. GET 和 POST 的区别？（必问）

核心区别分维度回答，避免只说「参数位置」：

1. **语义**：GET 用于获取资源，POST 用于创建资源；
2. **参数位置**：GET 参数拼接在 URL 中，POST 参数放在请求体；
3. **幂等性**：GET 幂等（多次请求结果一致），POST 非幂等；
4. **缓存**：GET 可被浏览器缓存，POST 默认不缓存；
5. **长度限制**：GET 受 URL 长度限制（不同浏览器不同），POST 无明确限制；
6. **安全性**：均为明文传输（HTTPS 下都加密），GET 参数暴露在 URL 中，相对更不安全。

### 3. 前端如何处理请求缓存？

1. HTTP 缓存（服务端控制）：
   - 强缓存：利用 `Cache-Control`（max-age）、`Expires` 头，浏览器直接读取本地缓存；
   - 协商缓存：利用 `Last-Modified/If-Modified-Since`、`ETag/If-None-Match` 头，服务端判断资源是否更新；
2. 前端手动缓存：
   - 用 localStorage/sessionStorage 缓存非敏感数据；
   - Axios 拦截器中缓存 GET 请求结果，短时间内重复请求直接返回缓存。

### 4. 如何取消重复的请求？（Axios 场景）

通过 Axios 拦截器 + CancelToken/AbortController 实现：

```javascript
// 存储请求标识（如 URL+参数）和取消函数
const pendingRequests = new Map();

// 请求拦截器
axios.interceptors.request.use(config => {
  // 生成唯一请求 key
  const requestKey = `${config.url}-${JSON.stringify(config.params)}-${JSON.stringify(config.data)}`;
  // 取消已存在的重复请求
  if (pendingRequests.has(requestKey)) {
    pendingRequests.get(requestKey)('取消重复请求');
    pendingRequests.delete(requestKey);
  }
  // 创建取消令牌
  const source = axios.CancelToken.source();
  config.cancelToken = source.token;
  pendingRequests.set(requestKey, source.cancel);
  return config;
});

// 响应拦截器
axios.interceptors.response.use(
  res => {
    const requestKey = `${res.config.url}-${JSON.stringify(res.config.params)}-${JSON.stringify(res.config.data)}`;
    pendingRequests.delete(requestKey);
    return res;
  },
  err => {
    const requestKey = `${err.config.url}-${JSON.stringify(err.config.params)}-${JSON.stringify(err.config.data)}`;
    pendingRequests.delete(requestKey);
    return Promise.reject(err);
  }
);
```

## 五、总结

### 关键点回顾

1. **请求库**：Axios 因拦截器、统一错误处理、功能丰富成为主流，Fetch 需手动封装，Ajax 易产生回调地狱；
2. **HTTP/HTTPS**：HTTPS = HTTP + SSL/TLS，通过「非对称 + 对称加密」保证安全，核心解决 HTTP 明文传输问题；
3. **高频考点**：GET/POST 区别、HTTPS 加密原理、HTTP 状态码、跨域解决方案、Axios 拦截器 / 取消请求是面试核心。

# Webpack & Vite 

## 一、核心概念铺垫

### 1. 前端构建工具的本质

前端构建工具（Webpack/Vite）解决「原生前端开发痛点」：

- 模块化兼容：将 ESM/CJS/AMD 等模块化语法转换为浏览器可识别的代码；
- 资源处理：编译 ES6+/TS/Vue/React 代码、处理 CSS / 图片 / 字体等静态资源；
- 工程化能力：代码压缩、按需加载、环境变量、热更新、缓存优化等；
- 核心目标：提升开发效率，优化生产环境代码性能。

### 2. 构建工具发展脉络

- 早期：Grunt/Gulp（任务流工具，无模块化打包能力）；
- 主流：Webpack（全能型打包工具，2015 年后成为标配）；
- 新一代：Vite（基于 ESM 的极速构建工具，2020 年后崛起，适配 Vue3/React 等框架）。

## 二、Webpack 核心考点

### 1. Webpack 核心概念（必背）

Webpack 是「静态模块打包器」，核心思想：**一切皆模块**，将所有资源（JS/CSS/ 图片等）视为模块，通过 loader 转换、plugin 扩展，最终打包为静态资源。

| 核心概念 |                             作用                             |
| :------: | :----------------------------------------------------------: |
|  Entry   |         入口，指定 Webpack 从哪个文件开始构建依赖图          |
|  Output  |            出口，指定打包后的文件输出路径和文件名            |
|  Loader  | 转换器，处理非 JS 文件（如 css-loader 处理 CSS，babel-loader 转 ES6+） |
|  Plugin  | 扩展插件，实现打包优化、资源管理等（如 HtmlWebpackPlugin、CleanWebpackPlugin） |
|   Mode   |   模式（development/production/none），内置不同的优化策略    |
|  Chunk   | 代码块，打包过程中拆分的代码片段（如入口 chunk、异步 chunk） |
|  Bundle  |        最终输出的打包文件（由一个或多个 chunk 组成）         |

### 2. Webpack 核心工作流程（面试高频）

```fortran
flowchart TD
    A[初始化] --> B[编译]
    B --> C[构建依赖图]
    C --> D[模块转换]
    D --> E[代码优化]
    E --> F[输出]
```

1. **初始化**：读取配置文件 / 命令行参数，创建 Compiler 实例，初始化内置插件；
2. **编译**：从 Entry 入口开始，递归解析所有模块的依赖（`require/import`）；
3. **构建依赖图**：将所有模块的依赖关系整理为「依赖图」；
4. **模块转换**：通过对应的 Loader 处理非 JS 模块（如 Babel 转 ES6+、css-loader 解析 CSS）；
5. **代码优化**：Plugin 介入（如代码分割、压缩、Tree-Shaking）；
6. **输出**：将处理后的模块打包为 Bundle，输出到 Output 指定路径。

### 3. Webpack 关键特性实现

#### （1）模块热更新（HMR）

- **作用**：开发时修改代码，无需刷新页面即可更新（保留页面状态）；
- 原理：
  1. Webpack Dev Server 启动时创建 WebSocket 连接（客户端 + 服务端）；
  2. 代码修改后，Webpack 重新编译变化的模块，生成「更新后的模块补丁」；
  3. 服务端通过 WebSocket 通知客户端有模块更新；
  4. 客户端加载补丁，替换旧模块，执行 HMR 回调（如 Vue 组件重新渲染）。

#### （2）Tree-Shaking

- **作用**：剔除代码中「未被使用的死代码」（如未调用的函数、未引用的变量），减小打包体积；

- 核心条件：

  ① 基于 ES6 模块化（ESM）：import/export是静态语法（编译时确定依赖），CommonJS（require）是动态语法，无法 Tree-Shaking；

  ② 生产模式（production）：Webpack 内置 TerserPlugin 实现代码压缩 + Tree-Shaking；

- **注意**：需避免「副作用代码」（如修改全局变量的函数），可通过 `package.json` 的 `sideEffects` 声明无副作用文件。

#### （3）代码分割（Code Splitting）

- **作用**：将打包后的 Bundle 拆分为多个小 Chunk，实现「按需加载」，减小首屏加载体积；

- 实现方式：

  ① 入口分割：entry 配置多个入口；

  ② 动态导入：import('./module.js').then(module => {})（返回 Promise，自动拆分 Chunk）；

  ③ 配置 splitChunks：提取公共代码（如第三方库 React/Vue）、重复代码；

  ```javascript
// webpack.config.js 示例：splitChunks 提取公共代码
  module.exports = {
    optimization: {
      splitChunks: {
        chunks: 'all', // 分割所有 chunk（同步+异步）
        cacheGroups: {
          vendor: { // 提取第三方库
            test: /[\\/]node_modules[\\/]/,
            name: 'vendors',
            chunks: 'all'
          }
        }
      }
    }
  };
  ```
  
  

### 4. Webpack 性能优化（面试必问）

#### （1）开发环境优化

- 开启 `cache`：缓存 Loader 处理结果（`cache-loader` 或 Webpack5 内置缓存）；
- 优化 `devtool`：开发时用 `eval-cheap-module-source-map`（速度快，源码映射友好）；
- 排除无需处理的文件：`module.noParse` 忽略无需解析的库（如 jQuery）；

#### （2）生产环境优化

- 代码分割：`splitChunks` 提取公共代码、动态导入实现按需加载；
- Tree-Shaking：开启生产模式，声明 `sideEffects`；
- 压缩资源：TerserPlugin 压缩 JS，css-minimizer-webpack-plugin 压缩 CSS；
- 静态资源处理：`asset/resource` 输出图片 / 字体，配合 `image-webpack-loader` 压缩图片；
- 开启持久化缓存：`output.filename: '[name].[contenthash].js'`，利用浏览器缓存；

## 三、Vite 核心考点

### 1. Vite 核心定位与优势

Vite 是「新一代前端构建工具」，核心分为两部分：

- **开发环境**：基于浏览器原生 ESM，无需打包，启动 / 热更新极速；
- **生产环境**：基于 Rollup 打包（Rollup 对 ESM 打包更高效，体积更小）；

**核心优势**：

- 冷启动快：无需打包所有模块，启动时间从 Webpack 的「秒级」降至「毫秒级」；
- 热更新快：修改代码后，仅需重新请求变化的模块，无需重新打包；
- 按需编译：开发时仅编译当前页面需要的模块，而非全量编译；

### 2. Vite 核心原理（面试核心）

#### （1）开发环境：原生 ESM 驱动

Webpack 开发时需「全量打包」所有模块，Vite 彻底颠覆这一逻辑：

1. 启动阶段：
   - Vite 启动一个开发服务器，不打包任何代码；
   - 当浏览器请求入口文件（如 `main.js`），Vite 对文件进行「即时编译」（如 Vue 文件编译、TS 转 JS），并返回 ESM 格式的代码；
2. 模块请求阶段：
   - 浏览器通过 `import` 引入模块时，会向 Vite 服务器发起请求（如 `import './App.vue'`）；
   - Vite 服务器接收到请求后，对该模块进行「按需编译」（如 Vue SFC 编译为 JS），并返回编译后的 ESM 代码；
3. 依赖预构建：
   - 对 `node_modules` 中的第三方库（如 Vue、Axios）进行预构建（转为 ESM 格式，合并零散文件）；
   - 解决两个问题：① 第三方库可能用 CommonJS 编写，浏览器原生 ESM 不支持；② 减少网络请求（合并零散文件）；

#### （2）生产环境：Rollup 打包

- Vite 生产环境不使用原生 ESM（避免大量网络请求），而是基于 Rollup 打包：
  1. Rollup 对 ESM 模块打包更高效（Tree-Shaking 更彻底，打包体积更小）；
  2. Vite 内置 Rollup 配置，支持代码分割、压缩、按需加载等；

### 3. Vite 热更新（HMR）原理

Vite 的 HMR 比 Webpack 更高效，核心逻辑：

1. 修改代码后，Vite 仅重新编译「变化的模块」（如单文件组件 `.vue`）；
2. 通过 WebSocket 通知浏览器「模块更新」；
3. 浏览器直接重新请求该模块（ESM 原生支持），无需刷新页面；

- 对比 Webpack：Webpack 需重新打包变化模块的「整个 Chunk」，Vite 仅更新单个模块，效率更高。

### 4. Vite 适用场景与局限性

#### （1）适用场景

- 中小型项目、组件库开发（ESM 优先）；
- Vue3/React18+ 等现代框架项目（原生支持 ESM）；
- 追求开发体验的团队（启动 / 热更新快）；

#### （2）局限性

- 对旧项目兼容性差：不支持 IE 浏览器（依赖原生 ESM）；
- 大型项目适配：早期版本对大型项目的分包、缓存支持不如 Webpack，Vite4+ 已大幅优化；
- 插件生态：Webpack 插件生态更丰富，Vite 部分场景需自定义插件；

## 插件

``vitejs/plugin-vue``：提供 Vue 3 单文件组件支持。

``vitejs/plugin-vue-jsx``：提供 Vue 3 JSX 支持（通过 [专用的 Babel 转换插件](https://github.com/vuejs/babel-plugin-jsx)).

``vitejs/plugin-react``：

使用 [Oxc 转换器](https://oxc.rs/docs/guide/usage/transformer) and [Babel](https://babeljs.io/)，以较小的软件包占用空间和使用 Babel 转换管道的灵活性实现快速 HMR。如果没有额外的 Babel 插件，在构建过程中只能使用 Oxc 转换器

``vitejs/plugin-react-swc``：

在开发时会将 Babel 替换为 [SWC](https://swc.rs/)。在生产环境构建期间，若使用了插件则会使用 SWC+Oxc 转换器，若没有使用插件则仅会用到 Oxc 转换器。对于需要自定义插件的大型项目，如果该插件在 SWC 中也可用，冷启动和模块热替换（HMR）将会有显著提升。

``vitejs/plugin-rsc``：

Vite 通过该插件支持 [React Server Components (RSC)](https://react.dev/reference/rsc/server-components)。它利用 [Environment API](https://cn.vite.dev/guide/api-environment) 提供底层原语，React 框架可以使用这些原语来集成 RSC 功能。你可以通过以下方式尝试一个最小的独立 RSC 应用程序：

```js
npm create vite@latest -- --template rsc
```

``vitejs/plugin-legacy``：为打包后的文件提供传统浏览器兼容性支持。

`vite-plugin-pwa`：实现 PWA 功能；

`vite-plugin-imagemin`：压缩图片资源。

## 四、Webpack vs Vite 核心对比（面试高频）

|    维度    |               Webpack               |                            Vite                            |
| :--------: | :---------------------------------: | :--------------------------------------------------------: |
|  核心架构  |      基于「打包」（全量编译）       | 开发环境基于「原生 ESM」（按需编译），生产基于 Rollup 打包 |
|  启动速度  |        慢（需打包所有模块）         |                  快（无需打包，按需编译）                  |
| 热更新速度 |   慢（修改代码需重新打包 Chunk）    |                 快（仅更新变化的单个模块）                 |
| 模块化支持 |     兼容 ESM/CJS/AMD（需转换）      |     开发环境仅支持 ESM（原生），生产兼容 CJS（预构建）     |
|  生产打包  |            自研打包逻辑             |                基于 Rollup（ESM 打包更优）                 |
|  插件生态  |            丰富（成熟）             |                      较新（快速增长）                      |
|   兼容性   |            好（支持 IE）            |               差（不支持 IE，依赖原生 ESM）                |
|  适用场景  | 大型 / 复杂项目、兼容性要求高的项目 |           中小型项目、现代框架项目、追求开发体验           |

### 面试高频题：为什么 Vite 比 Webpack 启动快？

核心差异在于「构建逻辑」：

1. Webpack 启动时需**全量打包**：递归解析所有模块的依赖，构建完整依赖图，将所有模块编译为浏览器可识别的代码，启动时间随项目体积线性增长；

2. Vite 启动时无需打包：

   - 直接启动开发服务器，依赖浏览器原生 ESM 加载模块；

   - 仅在浏览器请求模块时，才对该模块进行「即时编译」（按需编译）；

   - 对 node_modules中的第三方库做「预构建」（一次性处理），避免重复编译；

     因此 Vite 启动时间不受项目体积影响，远快于 Webpack。

   

## 五、高频面试题及标准答案

### 1. Webpack 和 Vite 的核心区别？

1. **构建模式**：Webpack 是「打包式构建」，开发时需全量打包所有模块；Vite 是「按需编译」，开发时基于浏览器原生 ESM，仅编译请求的模块；
2. **启动 / 热更新速度**：Webpack 启动慢（全量打包），热更新随项目体积变慢；Vite 启动 / 热更新极速（按需编译）；
3. **生产打包**：Webpack 自研打包逻辑，Vite 基于 Rollup（对 ESM 打包更高效）；
4. **兼容性**：Webpack 支持 IE，Vite 依赖原生 ESM，不支持 IE；
5. **生态**：Webpack 插件生态更成熟，Vite 生态快速发展中。

### 2. Webpack 的 Loader 和 Plugin 区别？

1. 作用维度：
   - Loader 是「转换器」，针对**单个模块**处理（如将 Vue 文件转为 JS、TS 转为 ES6+），解决「非 JS 模块无法被 Webpack 解析」的问题；
   - Plugin 是「扩展器」，针对**整个打包流程**扩展（如生成 HTML 文件、代码压缩、热更新），解决「打包流程的个性化需求」；
2. 使用方式：
   - Loader 配置在 `module.rules` 中，指定「匹配规则 + 处理 Loader」；
   - Plugin 配置在 `plugins` 数组中，通过实例化插件类使用；
3. 运行时机：
   - Loader 在「模块解析阶段」运行（构建依赖图时）；
   - Plugin 在「打包全流程」运行（可通过钩子介入初始化、编译、输出等阶段）。

### 3. Vite 的依赖预构建做了什么？

Vite 的依赖预构建是为了解决原生 ESM 加载第三方库的问题，核心做两件事：

1. **格式转换**：将 `node_modules` 中 CommonJS/UMD 格式的第三方库（如 Vue、Axios）转为 ESM 格式，适配浏览器原生 ESM；

2. 文件合并：将第三方库的零散文件合并为单个文件，减少浏览器的网络请求次数（如将 Lodash 的上百个文件合并为一个）；

   预构建仅在「首次启动」或「依赖变化」时执行，后续启动复用缓存，保证效率。

### 4. 为什么 Vite 生产环境不用 ESM 直接部署？

1. **网络请求成本**：原生 ESM 需加载多个零散模块，会产生大量 HTTP 请求，导致首屏加载慢；
2. **性能优化**：Rollup 可对代码进行 Tree-Shaking、代码分割、压缩等优化，打包后的文件体积更小、加载更快；
3. **兼容性**：部分浏览器对原生 ESM 的支持不完善，打包为兼容格式可覆盖更多场景。

## 六、总结

### 关键点回顾

1. **Webpack**：核心是「全量打包」，通过 Loader 处理模块、Plugin 扩展流程，生态成熟，适合大型 / 兼容要求高的项目；
2. **Vite**：核心是「开发环境按需编译 + 生产环境 Rollup 打包」，启动 / 热更新极速，适合现代框架项目，不兼容 IE；
3. **核心差异**：Webpack 是「打包后再运行」，Vite 是「运行时按需编译」，这是 Vite 速度快的根本原因。

# 函数柯里化

柯里化是一种将使用多个参数的一个函数转换成一系列使用一个参数的函数的技术。

柯里化实际是把简答的问题复杂化，但是复杂化的同时，在使用函数时拥有了更加多的自由度。 而这里对于函数参数的自由处理，正是柯里化的核心所在。 柯里化本质上是降低通用性，提高适用性；



# V8引擎机制&垃圾回收
## 1.V8如何执行一段JS代码
1.  预解析：检查语法错误但不生成AST
2. 生成AST：经过词法/语法分析，生成抽象语法树
3. 生成字节码：基线编译器（lgnition）将AST转换成字节码
4. 生成机器码：优化编译器（Turbofan）将字节码转换成优化过的机器码，此外在逐
   行执行字节码的过程中，如果一段代码经常被执行，那么V8会将这段代码直接
   转换成机器码保存起来，下一次执行就不必经过字节码，优化了执行速度

## 2.垃圾回收机制有哪些
1. 引用计数：给一个变量赋值引用类型，则该对象的引用次数+1，如果这个变量
   变成了其他值，那么该对象的引用次数-1，垃圾回收器会回收引用次数为0的对
   象。但是当对象循环引用时，会导致引用次数永远无法归零，造成内存无法释
   放。

2. 标记清除：垃圾收集器先给内存中所有对象加上标记，然后去掉被引用的对象和
   运行环境中对象的标记，剩下的被标记的对象就是等待回收的对象。

## 3.V8如何进行垃圾回收
1. 栈内存：调用栈上下文切换后，就被回收

2. 堆内存：
   1. 堆内存分类：
      V8的堆内存分为新生代内存和老生代内存，新生代内存是临时分配的内
      存，存在时间短，老生代内存反之，存在时间长
   2. 新生代内存回收机制
      新生代内存容量小，64位系统下仅有32M。新生代内存分为From、To两
      部分，进行垃圾回收时，先扫描From，将非存活对象回收，将存活对象
      顺序复制到To中，之后调换From/To，等待下一次回收（之所以分为
      From/To而不直接回收非存活对象，是为了解决内存碎片的问题
   3. 老生代内存回收机制
      晋升：如果新生代的变量经过多次回收依然存在，那么就会被放入老生
      代内存中
      标记-清除：老生代内存会先遍历所有对象并打上标记，然后对正在使用
      或被强引用的对象取消标记，回收被标记的对象（也就是标记清除）
      整理内存碎片：V8直接把对象挪到一端（内存回收中最耗时的部分）

## 3.JS相较于JAVA/C++为什么慢，而V8做了哪些优化，使得V8如此之快
1. JS的问题

   1. 动态类型：导致每次存取属性/寻求方法时候，都需要先检查类型，也很难在
      编译阶段进行优化

   2. 属性存取：C++/Java等语言中方法、属性是存储在数组中的，仅需数组位移
      就可以获取，而JS存储在对象中，每次获取都要进行哈希查询

2. V8的优化：

   1. 优化JIT（即时编译）：相较于C++/Java这类编译型语言，JS一边解释一边执
      行，效率低。V8对这个过程进行了优化：如果一段代码被执行多次，那么
      V8会把这段代码转化为机器码缓存下来，下次运行时直接使用机器码。

   2. 隐藏类：对于C++这类语言来说，仅需几个指令就能通过偏移量获取变量信
      息，而JS需要进行字符串匹配，效率低，V8借用了类和偏移位置的思想，将
      对象划分成不同的组，这就是隐藏类

   3. 内嵌缓存：即缓存对象查询的结果。常规查询过程是：获取隐藏类地址-
      根据属性名查找偏移值->计算该属性地址，内嵌缓存就是对这一过程结果
      的缓存

   4. 垃圾回收管理：上文已经介绍