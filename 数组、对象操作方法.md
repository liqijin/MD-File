# 一、数组操作大全（Array）

数组是有序集合，**区分 可变操作（修改原数组）/ 不可变操作（返回新数组）**（前端框架 React/Vue 必须用不可变操作）

## 1. 基础增删改查（核心基础）

|                方法                 |          作用          |  类型  |             应用场景             |
| :---------------------------------: | :--------------------: | :----: | :------------------------------: |
|          `push(...items)`           |      末尾添加元素      |  可变  |    列表追加数据、加载更多分页    |
|               `pop()`               |      删除末尾元素      |  可变  | 栈结构（后进先出）、删除最后一项 |
|         `unshift(...items)`         |      开头添加元素      |  可变  |           列表置顶数据           |
|              `shift()`              |      删除开头元素      |  可变  |       队列结构（先进先出）       |
| `splice(start, delCount, ...items)` |  任意位置增 / 删 / 改  |  可变  |     动态删除列表项、插入数据     |
|         `slice(start, end)`         |       截取子数组       | 不可变 |      复制数组、截取部分列表      |
|             `at(index)`             | 按索引取值（支持负数） | 不可变 |    获取最后一项 `arr.at(-1)`     |

------

## 2. 遍历迭代（最常用，数据处理核心）

|               方法               |            作用            |     返回值      |                 应用场景                 |
| :------------------------------: | :------------------------: | :-------------: | :--------------------------------------: |
|           `forEach()`            |      遍历数组，无返回      |    undefined    |       单纯遍历渲染 DOM、执行副作用       |
|             `map()`              |    映射数组，返回新数组    |     新数组      | 列表渲染、数据格式转换（接口数据格式化） |
|            `filter()`            | 过滤数组，保留符合条件元素 |     新数组      |       搜索筛选、列表过滤、数据去重       |
|            `reduce()`            |     累计计算，归并数组     |     任意值      |   求和 / 求平均值、数组转对象、扁平化    |
|             `find()`             | 查找**第一个**符合条件元素 | 元素 /undefined |      查找单条数据（根据 id 查对象）      |
|          `findIndex()`           |     查找第一个元素索引     |    索引 /-1     |      定位数据位置，用于修改 / 删除       |
| `findLast()` / `findLastIndex()` |        从后往前查找        |   元素 / 索引   |        查找最后一条符合条件的数据        |
|             `some()`             |   只要一个满足条件就返回   |     Boolean     |         表单校验（至少一项勾选）         |
|            `every()`             |   所有元素满足条件才返回   |     Boolean     |       全选校验、批量数据合法性校验       |
|            `for...of`            |         遍历数组值         |        -        |      支持 break/continue，灵活遍历       |

------

## 3. 查找与判断

|         方法         |          作用          |              应用场景              |
| :------------------: | :--------------------: | :--------------------------------: |
|   `includes(item)`   |   判断是否包含某元素   | 快速判断值是否存在（替代 indexOf） |
|   `indexOf(item)`    | 返回元素第一次出现索引 |       旧版浏览器判断元素存在       |
| `Array.isArray(arr)` |     判断是否为数组     |            数据类型校验            |

------

## 4. 排序、反转、拼接

|        方法         |          作用          |  类型  |             应用场景             |
| :-----------------: | :--------------------: | :----: | :------------------------------: |
| `sort((a,b)=>a-b)`  | 排序（默认字符串排序） |  可变  | 数字排序、列表按时间 / 价格排序  |
|     `reverse()`     |        反转数组        |  可变  |           列表倒序展示           |
| `concat(...arrays)` |      合并多个数组      | 不可变 |           拼接多个列表           |
|     `join(sep)`     |      数组转字符串      | 不可变 | 拼接字符串（标签、逗号分隔文本） |

------

## 5. 扁平化（嵌套数组处理）

|     方法      |      作用      |              应用场景              |
| :-----------: | :------------: | :--------------------------------: |
| `flat(depth)` | 扁平化嵌套数组 |     处理多级菜单、树形数据展平     |
|  `flatMap()`  | map + flat(1)  | 先映射再扁平化（接口嵌套数据处理） |

------

## 6.  ES2023 不可变操作（前端框架必备）

**不修改原数组，返回新数组**（React/Vue 状态管理强制使用）

|         方法         |         作用         |    替代方案    |
| :------------------: | :------------------: | :------------: |
|     `toSorted()`     |      不可变排序      |     sort()     |
|    `toReversed()`    |      不可变反转      |   reverse()    |
|    `toSpliced()`     |      不可变增删      |    splice()    |
| `with(index, value)` | 不可变修改指定索引值 | arr[index] = x |

------

## 7. 迭代器方法

|    方法     |      作用       |
| :---------: | :-------------: |
|  `keys()`   |    遍历索引     |
| `values()`  |     遍历值      |
| `entries()` | 遍历 [索引，值] |

------

# 二、对象操作大全（Object）

对象是**无序键值对**，核心操作：增删改查、遍历、拷贝、合并、防护

## 1. 基础增删改查

|   操作   |                 语法                 |            场景            |
| :------: | :----------------------------------: | :------------------------: |
| 新增属性 | `obj.key = val` / `obj['key'] = val` |      动态添加表单数据      |
| 修改属性 |                 同上                 |   更新用户信息、状态修改   |
| 删除属性 |           `delete obj.key`           | 删除无用字段、接口参数过滤 |
| 访问属性 |        `obj.key` / `obj[key]`        |   获取数据、动态属性访问   |

------

## 2. 对象遍历（开发高频）

|           方法            |         作用          |                 特点                 |
| :-----------------------: | :-------------------: | :----------------------------------: |
|        `for...in`         |  遍历对象可枚举属性   |       会遍历原型链属性（慎用）       |
|    `Object.keys(obj)`     |   返回**键名**数组    |        最常用，只遍历自身属性        |
|   `Object.values(obj)`    |   返回**键值**数组    |            快速获取所有值            |
|   `Object.entries(obj)`   | 返回`[key,value]`数组 |        遍历键值对、对象转 Map        |
| `Object.hasOwn(obj, key)` | 判断自身是否包含属性  | 替代`obj.hasOwnProperty()`（ES2022） |

------

## 3. 合并与拷贝（核心！区分深浅拷贝）

### （1）浅拷贝（只拷贝第一层）

|                方法                 |       作用       |             场景             |
| :---------------------------------: | :--------------: | :--------------------------: |
| `Object.assign(target, ...sources)` | 合并对象、浅拷贝 |    合并配置项、浅拷贝对象    |
|        展开运算符 `{...obj}`        |      浅拷贝      | 状态更新、对象合并（最常用） |

### （2）深拷贝（拷贝所有层级，解决嵌套对象引用问题）

```js
// 标准深拷贝
JSON.parse(JSON.stringify(obj)) 
// 完美深拷贝（支持函数、Symbol、日期）
structuredClone(obj)  // ES2022 原生方法
```

**场景**：嵌套对象修改、状态隔离、避免数据污染

------

## 4. 对象创建与属性防护

|           方法            |             作用             |           场景           |
| :-----------------------: | :--------------------------: | :----------------------: |
|  `Object.create(proto)`   |       基于原型创建对象       |         原型继承         |
| `Object.defineProperty()` |  定义属性（get/set/ 枚举）   |     Vue2 响应式原理      |
|   `Object.freeze(obj)`    | 冻结对象（不可改 / 删 / 增） | 常量对象、防止数据被篡改 |
|    `Object.seal(obj)`     |  密封对象（可改，不可增删）  |       保护对象结构       |

------

## 5.  ES6+ 高级对象方法

|           方法            |       作用       |                应用场景                 |
| :-----------------------: | :--------------: | :-------------------------------------: |
| `Object.fromEntries(arr)` | 键值对数组转对象 |       Map 转对象、表单数据格式化        |
| `Object.groupBy(arr, fn)` |  数组按条件分组  | 数据分类（按性别 / 状态分组列表）ES2024 |
|  解构赋值 `{key} = obj`   |   快速提取属性   |       接口数据解构、函数参数解构        |

------

# 三、解构赋值（数组 + 对象通用神器）

## 1. 对象解构

```js
const { name, age } = user; // 快速提取属性
const { name = '默认值' } = user; // 带默认值
```

**场景**：函数参数、接口数据提取

## 2. 数组解构

```js
const [a, b] = arr;
const [first, ...rest] = arr; // 剩余参数
```

**场景**：交换变量、批量赋值、函数多返回值

------

# 四、 开发高频场景速查

## 数组高频场景

1. 数组去重

   ```js
   // 1、new Set
   [...new Set(arr)]
   // 2、Set + Array.from
   const uniqueArr = Array.from(new Set(arr))
   // 3、filter + indexOf（ES5 经典）
   const uniqueArr = arr.filter((item, index) => arr.indexOf(item) === index)
   // 4、reduce + includes
   const uniqueArr = arr.reduce((prev, cur) => prev.includes(cur) ? prev : [...prev, cur], [])
   // 5、Map 去重（高性能）
   const map = new Map()
   const uniqueArr = arr.filter(item => !map.has(item) && map.set(item, 1))
   // 6、双层循环 + splice（兼容 IE）
   for(let i=0; i<arr.length; i++){
     for(let j=i+1; j<arr.length; j++){
       if(arr[i] === arr[j]) arr.splice(j,1)
     }
   }
   // 7、排序 + 相邻去重
   const uniqueArr = arr.sort().filter((item, index, arr) => item !== arr[index-1])
   ```

2. 数组求和

   ```js
   arr.reduce((sum, cur) => sum + cur, 0)
   ```

3. 根据 ID 删除数组对象

   ```js
   arr.filter(item => item.id !== id)
   ```

4. 树形数组展平

   ```js
   arr.flat(Infinity)
   ```

5. 数组转对象（键值映射）

   ```js
   arr.reduce((obj, item) => (obj[item.id] = item, obj), {})
   ```

6. 对象数组去重（按唯一 key 去重）

   对象不能直接用 `Set` 去重，必须根据 **id/key** 匹配

   ```js
   // 1、reduce + Map（最优方案）
   const arr = [{id:1},{id:2},{id:1},{id:3}]
   const map = new Map()
   const uniqueArr = arr.reduce((prev, cur) => {
     if(!map.has(cur.id)) {
       map.set(cur.id, true)
       prev.push(cur)
     }
     return prev
   }, [])
   // [{id:1},{id:2},{id:3}]
   // 2、filter + Map
   const map = new Map()
   const uniqueArr = arr.filter(item => !map.has(item.id) && map.set(item.id, 1))
   // 3、循环 + 对象缓存
   const obj = {}
   const uniqueArr = arr.filter(item => !obj[item.id] && (obj[item.id] = true))
   //  4、不推荐：JSON.stringify + Set
   [...new Set(arr.map(JSON.stringify))].map(JSON.parse)
   // 缺陷：顺序不同、正则 / 日期会丢失，性能差
   ```

   #### 数组去重总结（直接选）

   1. **简单数组**：优先 `[...new Set(arr)]`
   2. **对象数组**：优先 `reduce + Map`
   3. **大数据量**：用 `Map` 方案（性能最高）

## 对象高频场景

1. 对象数组分组（ES2024）

   ```js
   Object.groupBy(list, item => item.status)
   ```

2. 对象合并（不修改原对象）

   ``````js
   const newObj = {...obj1, ...obj2}
   ``````

3. 动态判断对象属性

   ``````js
   Object.hasOwn(user, 'name')
   ``````

4. 对象拷贝

   ### 浅拷贝

   ### 1. 展开运算符 `{...obj}`（最常用、最新）

   ```javascript
   const obj = {a:1, b:{c:2}}
   const copy = {...obj}
   ```

   **优点**：简洁、现代、性能高

   **缺点**：仅浅拷贝

   ### 2. Object.assign()

   ```javascript
   const copy = Object.assign({}, obj)
   ```

   ### 3. 数组专用浅拷贝

   ```javascript
   const arrCopy = arr.slice()
   const arrCopy2 = arr.concat()
   ```

   ### 4. 手动循环遍历

   ```javascript
   const copy = {}
   for(let key in obj) obj.hasOwnProperty(key) && (copy[key] = obj[key])
   ```

   ### 5. ES2023 数组不可变方法

   ```javascript
   const copy = arr.toSorted() // 浅拷贝新数组
   ```

   ### 深拷贝

   ### 1、structuredClone ()（ES2022 官方最新 / 最优）

   ```js
   const obj = {a:1, b:{c:2}, d:new Date(), e:/123/}
   const copy = structuredClone(obj)
   ```

   ✅ **支持**：Date/RegExp/Set/Map/ 循环引用

   ✅ **优点**：原生、性能高、无第三方依赖

   ❌ **不支持**：函数、Symbol、Generator

   ### 2、JSON.parse (JSON.stringify ())（最通用基础方案）

   ```js
   const copy = JSON.parse(JSON.stringify(obj))
   ```

   ✅ **优点**：简单、无依赖、兼容所有浏览器

   ❌ **缺陷**：丢失函数、undefined、Symbol、Date、RegExp，无法处理循环引用

   ### 3、手写递归深拷贝（万能自定义）

   支持**函数、Symbol、循环引用**，完全定制

   ``````js
   function deepClone(obj, cache = new WeakMap()) {
     if(obj === null || typeof obj !== 'object') return obj
     if(cache.has(obj)) return cache.get(obj)
     const copy = obj instanceof Array ? [] : {}
     cache.set(obj, copy)
     Reflect.ownKeys(obj).forEach(key => copy[key] = deepClone(obj[key], cache))
     return copy
   }
   ``````

   ### 4、lodash.cloneDeep ()（第三方稳定）

   ``````
   import _ from 'lodash'
   const copy = _.cloneDeep(obj)
   ``````

   ✅ **优点**：支持所有数据类型、稳定可靠

   ❌ **缺点**：需要引入库

   ### 5、jQuery.extend (true)（老项目兼容）

   ``````
   const copy = $.extend(true, {}, obj)
   ``````

   #### 对象拷贝总结（直接选）

   1. **单层对象 / 数组**：用浅拷贝 `{...obj}` / `[...arr]`
   2. **嵌套对象（现代开发）**：优先 `structuredClone()`
   3. **无特殊要求**：用 `JSON.parse(JSON.stringify())`
   4. **需要支持函数 / 循环引用**：用**手写递归**或 `lodash`

------

# 五、关键注意事项（必看！避坑）

1. 可变 vs 不可变
   - 数组：`push/pop/splice/sort/reverse` 会修改原数组
   - 框架开发：优先用**不可变方法**（map/filter/concat/ES2023 新方法）
2. 浅拷贝 vs 深拷贝
   - 单层数据：浅拷贝足够
   - 嵌套对象 / 数组：必须用**深拷贝**（structuredClone）
3. 遍历选择
   - 格式化数据：`map`
   - 筛选数据：`filter`
   - 累计计算：`reduce`
   - 查找单条：`find`

## 数组去重

- 简单数组：`[...new Set(arr)]`
- 对象数组：`reduce + Map`

## 对象拷贝

- 浅拷贝：`{...obj}`
- 深拷贝（最新最优）：`structuredClone(obj)`
- 深拷贝（通用）：`JSON.parse(JSON.stringify(obj))`

------

# 总结

1. **数组**：核心是**遍历、过滤、映射、不可变操作**，处理有序列表数据；
2. **对象**：核心是**遍历、拷贝、合并、分组**，处理无序键值对数据；
3. **ES6+**：解构、展开运算符、`reduce`、`Object.groupBy` 是现代 JS 开发效率天花板；
4. **框架开发**：牢记**不可变数据原则**，不直接修改原数组 / 对象。