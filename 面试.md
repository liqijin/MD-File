# 一、JavaScript 基础与进阶（8 题）

## 1. 说说闭包是什么？应用场景？缺点？

**答案：**

- 闭包：**函数嵌套，内部函数访问外部函数作用域变量**，形成不销毁的作用域。
- 场景：**模块化、防抖节流、柯里化、私有变量、缓存**。
- 缺点：**变量常驻内存，滥用会造成内存泄漏**。

## 2. this 指向规则？箭头函数 this 有什么特点？

**答案：**

- 规则：

  1. 默认绑定 → window

  2. 隐式绑定 → 对象调用

  3. 显式绑定 → call/apply/bind

  4. new 绑定 → 新实例

     优先级：new > 显式 > 隐式 > 默认

- 箭头函数：**无 this，继承外层作用域 this**，不能 new、没有 arguments。

## 3. 宏任务 / 微任务执行顺序？举例说明

**答案：**

执行顺序：

**同步代码 → 微任务 → 渲染 → 宏任务**

- 微任务：`Promise.then、queueMicrotask、MutationObserver`
- 宏任务：`setTimeout、setInterval、ajax、requestAnimationFrame`

## 4. Promise 有几种状态？手写 Promise.all

**答案：**

- 状态：**pending → fulfilled / rejected**，状态一旦改变不可变。
- Promise.all：全部成功才成功，任一失败则失败。

```js
Promise.all = function (promises) {
  return new Promise((resolve, reject) => {
    let result = [], count = 0
    promises.forEach((p, i) => {
      Promise.resolve(p).then(res => {
        result[i] = res
        if (++count === promises.length) resolve(result)
      }, reject)
    })
  })
}
```

## 5. 浅拷贝 vs 深拷贝？手写深拷贝

**答案：**

- 浅拷贝：只拷贝第一层，引用类型共享地址。
- 深拷贝：递归拷贝所有层级，完全独立。

```js
function deepClone(obj, hash = new WeakMap()) {
  if (obj == null || typeof obj !== 'object') return obj
  if (obj instanceof Date) return new Date(obj)
  if (obj instanceof RegExp) return new RegExp(obj)
  if (hash.has(obj)) return hash.get(obj)
  let clone = Array.isArray(obj) ? [] : {}
  hash.set(obj, clone)
  for (let key in obj) {
    if (obj.hasOwnProperty(key)) clone[key] = deepClone(obj[key], hash)
  }
  return clone
}
```

## 6. 原型、原型链、instanceof 原理

**答案：**

- `__proto__` 指向原型
- `prototype` 是函数的原型对象
- 原型链：**实例.\**proto\** → 原型 → 原型.\**proto\** → Object.prototype → null**
- `instanceof`：判断**构造函数的 prototype 是否在实例的原型链上**。

## 7. 防抖、节流区别与应用场景

**答案：**

- 防抖：**频繁触发只执行最后一次**（搜索输入、窗口 resize）。
- 节流：**固定时间执行一次**（滚动、点击、鼠标移动）。

## 8. JS 垃圾回收机制？内存泄漏场景

**答案：**

- GC：**标记清除（主流）、引用计数**。
- 泄漏场景：全局变量、定时器未清除、闭包滥用、DOM 引用未释放、console 残留。

## 9. 继承

## 一、前置概念

- JS 是**基于原型**的继承，不是类继承
- 继承目标：**属性独立、方法复用、可传参**
- 原型链继承的通病：**引用类型属性共享**

## 二、ES5 6 种继承方式

### 1. 原型链继承（最基础）

```js
function Parent() {
  this.name = 'parent';
  this.arr = [1, 2, 3]; // 引用类型
}
Parent.prototype.say = function () {};

function Child() {}
Child.prototype = new Parent(); // 核心
```

**优点**

- 实现简单
- 父类方法能复用

**缺点**

- 父类**引用类型属性被所有实例共享**
- 子类实例化**不能向父类传参**

**适用**：简单演示，不适合实际项目

------

### 2. 借用构造函数继承（经典继承）

```js
function Parent(name) {
  this.name = name;
  this.arr = [1,2,3];
}

function Child(name) {
  Parent.call(this, name); // 核心
}
```

**优点**

- 解决**引用类型共享问题**
- 可以**向父类传参**

**缺点**

- 方法都写在构造函数里，**无法复用**，每次新建实例都创建新方法

**适用**：只需要继承属性，不需要复用方法

------

### 3. 组合继承（最常用 ES5 继承）

**思路：原型链继承方法 + 借用构造函数继承属性**

```js
function Parent(name) {
  this.name = name;
  this.arr = [1,2,3];
}
Parent.prototype.say = function () {};

function Child(name, age) {
  Parent.call(this, name); // 继承属性（第1次调用父构造）
  this.age = age;
}

Child.prototype = new Parent(); // 继承方法（第2次调用父构造）
Child.prototype.constructor = Child; // 修正constructor
```

**优点**

- 属性独立、方法复用
- 支持传参
- 业界**最常用的 ES5 继承**

**缺点**

- 父类构造函数**被调用两次**
- 原型上会多出无用属性

**适用**：老式项目、面试常考

------

### 4. 原型式继承

```js
function createObj(o) {
  function F() {}
  F.prototype = o;
  return new F();
}
```

等同于 ES5 `Object.create()`

**优点**

- 不需要构造函数，基于对象快速继承

**缺点**

- 同样**引用类型共享**

**适用**：不需要单独创建构造函数，只想简单继承一个对象

------

### 5. 寄生式继承

在原型式继承基础上**增强对象**：

```js
function createChild(o) {
  const clone = Object.create(o);
  clone.say = function () {};
  return clone;
}
```

**适用**：对已有对象进行扩展，不关注类型

------

### 6. 寄生组合式继承（ES5 最佳方案）

解决组合继承**父构造调用两次**的问题。

```js
// 封装继承逻辑
function inheritPrototype(Child, Parent) {
  const prototype = Object.create(Parent.prototype);
  prototype.constructor = Child;
  Child.prototype = prototype;
}

function Child(name) {
  Parent.call(this, name); // 只调用1次
}

inheritPrototype(Child, Parent); // 核心
```

**优点**

- 父构造**只调用一次**
- 原型链正常
- 属性独立、方法复用
- **理论上 ES5 最完美继承**

**适用**：高级库、框架底层、追求性能的场景

## 三、ES6 class 继承（实际开发首选）

```js
class Parent {
  constructor(name) {
    this.name = name;
    this.arr = [1,2,3];
  }
  say() {}
}

class Child extends Parent {
  constructor(name, age) {
    super(name); // 必须先调用super
    this.age = age;
  }
}
```

**关键点**

- `extends` 实现继承
- `super()` 相当于 `Parent.call(this)`
- 子类`this`必须在`super`之后使用
- 底层仍是**原型继承**，只是语法糖

**优点**

- 语法清晰、语义化
- 无引用类型共享问题
- 支持静态方法、getter/setter

**适用**：现代 Vue/React 项目、所有新代码

## 四、一张表总结所有继承

|     继承方式      |          优点          |        缺点        |     评价     |
| :---------------: | :--------------------: | :----------------: | :----------: |
|    原型链继承     |     简单、方法复用     | 引用共享、不能传参 |  差，不推荐  |
|   借用构造函数    |   可传参、无引用共享   |    方法无法复用    |     一般     |
|     组合继承      |   属性独立、方法复用   |   父构造调用两次   |     常用     |
|    原型式继承     |      快速继承对象      |      引用共享      |   简单场景   |
|    寄生式继承     |        增强对象        |      引用共享      |     辅助     |
|   寄生组合继承    | 完美，只调用一次父构造 |     写法稍复杂     |   ES5 最优   |
| ES6 class extends |   语法简洁、功能完整   |    底层仍是原型    | 现代开发首选 |

## 五、面试必背一句话

1. 原型链继承问题：**引用类型属性共享**
2. 解决方法：**借用构造函数 call (this)**
3. ES5 最优：**寄生组合继承**
4. 实际开发：**ES6 class extends**



##  如何解决原型链继承中父类引用类型属性被所有子类实例共享的问题？

## 问题根源

原型链继承：

```js
Child.prototype = new Parent();
```

- 父类的**实例属性**会变成子类的**原型属性**
- 原型上的**引用类型（数组、对象）是共享的**
- 一个实例修改，所有实例都会受影响

## 标准答案：使用「借用构造函数」解决

### 方案：在子类构造函数中调用父类构造函数

```js
function Child() {
  // 核心：借用构造函数
  Parent.call(this); 
  // 或者 Parent.apply(this)
}
```

### 原理

1. `call(this)` 会把父类构造函数里的**属性绑定到当前子类实例上**
2. 父类的引用类型属性就变成了**每个实例自己的属性**，而不是原型上的
3. 实例之间互不影响，解决共享问题

## 最终最优方案：组合继承 / 寄生组合继承

真正生产和面试中，都是**结合两种方式**：

```js
function Child(name) {
  Parent.call(this, name); // 解决引用类型共享 + 传参
}

Child.prototype = new Parent(); // 继承方法
Child.prototype.constructor = Child;
```

- **借用构造函数**：解决**引用类型共享**问题
- **原型链**：解决**方法复用**问题

这就是为什么**组合继承是 ES5 最常用继承方式**的原因。

## 一句话面试背诵版

> 原型链继承中父类引用类型属性会被所有子类实例共享，
>
> 解决方法是在**子类构造函数内部使用 call/apply 调用父类构造函数**，
>
> 让父类属性挂载到子类实例自身，而不是原型上，从而实现属性独立。





------

# 二、浏览器原理（5 题）

## 9. 浏览器渲染流程？重排重绘如何优化？

**答案：**

流程：

HTML → DOM → CSSOM → RenderTree → Layout → Paint → Composite

- 重排（Reflow）：几何属性改变（宽高、位置）。

- 重绘（Repaint）：样式改变不影响布局。

  优化：读写分离、批量 DOM、使用 transform/opacity（合成层）、避免频繁获取布局属性。


## 10. 事件捕获 / 冒泡 / 委托？

**答案：**

- 顺序：**捕获 → 目标 → 冒泡**。
- 事件委托：利用冒泡，把事件绑父级，减少绑定，支持动态元素。

## 11. 跨域是什么？解决方案？

**答案：**

同源策略：协议、域名、端口一致。

方案：

1. CORS（最常用）
2. 代理（webpack/vite/nginx）
3. JSONP（仅 GET）
4. postMessage、iframe、websocket

## 12. 浏览器缓存：强缓存 vs 协商缓存

**答案：**

- 强缓存：`Cache-Control: max-age`，**直接用本地，不发请求**。
- 协商缓存：`304`，发请求，服务端对比 ETag/Last-Modified。

## 13. XSS / CSRF 原理与防御

**答案：**

- XSS：注入脚本偷数据 → 转义、CSP、httpOnly。
- CSRF：伪造请求 → token、SameSite=Lax、校验 Referer。

------

# 三、Vue2 / Vue3（7 题）

## 14. Vue2 响应式原理？缺陷？

**答案：**

- Object.defineProperty劫持 get/set。
  
  缺陷：

  无法监听数组下标 / 长度、对象新增删除属性，需 $set。


## 15. Vue3 为什么用 Proxy？优势？

**答案：**

- Proxy 代理整个对象，支持：

  新增 / 删除、数组下标、Map/Set、懒递归。

  

  配合 Reflect 保证返回值规范。

## 16. Vue diff 原理？key 作用？

**答案：**

- 同层比较，不跨级，双指针比较。

- key：

  保证节点唯一性，避免就地复用错误，提升 diff 效率。

  禁止用 index 做 key。


## 17. Vue 组件通信方式（中高级要答全）

**答案：**

1. props / emit
2. v-model
3. provide/inject
4. eventBus/mitt
5. vuex/pinia
6. attrs/listeners
7. ref / defineExpose

## 18. Vue 生命周期？setup 执行时机？

**答案：**

- setup 在 **beforeCreate 之前**执行，无 this。
- 常用：onMounted、onUpdated、onUnmounted。

## 19. computed vs watch

**答案：**

- computed：**计算属性，有缓存，依赖变化才执行**，多值影响一值。
- watch：**监听单一数据**，执行异步 / 复杂逻辑。

## 20. Vue 项目性能优化（必背）

**答案：**

- v-show 频繁切换
- v-for 必 key
- 异步组件、路由懒加载
- keep-alive 缓存页面
- 图片懒加载、防抖节流
- 压缩、分包、CDN、关闭 sourcemap

------

# 四、工程化 Webpack / Vite（4 题）

## 21. Webpack loader vs plugin

**答案：**

- loader：**转译模块**（处理非 JS：vue、ts、css）。
- plugin：**扩展生命周期**（压缩、html、分包、优化）。

## 22. Webpack Tree-Shaking 条件

**答案：**

- ESM 静态导入
- production 模式
- 无副作用（sideEffects）

## 23. Vite 为什么快？

**答案：**

- 开发：**浏览器原生 ESM，不打包，按需编译**。
- 依赖预构建
- 生产用 Rollup

## 24. 代码分割（Code Split）怎么做？

**答案：**

- 动态 import
- splitChunks 抽离第三方库
- 路由懒加载

------

# 五、网络 HTTP / HTTPS（3 题）

## 25. GET vs POST（标准高分答法）

**答案：**

- 语义：GET 查、POST 改
- 幂等：GET 幂等，POST 不
- 缓存：GET 可缓存
- 参数：GET 在 URL，POST 在 body
- 安全：**都不安全，HTTPS 才安全**

## 26. HTTPS 加密原理

**答案：**

- 非对称加密（交换对称密钥）
- 对称加密（传输数据）
- CA 证书认证身份

## 27. HTTP2 优点

**答案：**

- 二进制分帧
- 多路复用（一条连接并发）
- 头部压缩
- 服务端推送

------

# 六、架构 / 性能 / 手写（3 题）

## 28. 前端性能优化从哪些维度做？

**答案：**

- 网络：压缩、缓存、CDN、懒加载、分包
- 渲染：减少重排、合成层、虚拟列表
- JS：防抖节流、WebWorker
- 资源：图片 WebP、字体、按需加载

## 29. 前端模块化：ESM vs CommonJS

**答案：**

- ESM：import/export，**静态编译，支持 Tree-Shaking**。
- CommonJS：require/module.exports，运行时加载。

## 30. 你做过的工程化建设（中高级必背话术）

**答案：**

- 封装请求、工具库、hooks、组件库
- 规范：ESLint、Prettier、commitlint
- 构建优化：分包、缓存、CDN、压缩
- 监控：错误监控、性能监控、上报
- 权限：路由权限、按钮权限、菜单权限

# 一、JS 核心

## 1. 数据类型 & 内存

- 8 种数据类型：**7 种原始类型 + Object**

- 原始类型存在**栈**，引用类型存在**堆**

- 浅拷贝 / 深拷贝区别、实现方式

  - 浅拷贝：`...`、`Object.assign`
  - 深拷贝：`JSON.parse(JSON.stringify)` 缺陷 + 手写递归深拷贝

  一、 基本数据类型（原始类型）
  
  保存在栈内存中的简单数据段，直接按值访问。
  
  1. **Undefined**: 未定义。变量被声明但未赋值时的默认值。
  2. **Null**: 空值。表示一个空指针对象。
  3. **Number**: 数字。包含整数和浮点数（如 \(10\)、\(3.14\)），以及用于表示极大数值或非数值的 `NaN`、`Infinity` 等。
  4. **String**: 字符串。用单引号或双引号包裹的文本（如 `'hello'`）。
  5. **Boolean**: 布尔值。仅包含 `true` 和 `false` 两个值。
  6. **BigInt**: 大整数。用于表示大于 \(2^{53} - 1\) 的整数，数字后需加 `n`（如 `123n`）。
  7. **Symbol**: 符号。ES6 新增，表示独一无二的值，常用于作为对象属性的唯一标识符。
  
  二、 引用数据类型（对象类型）
  
  保存在堆内存中的对象。变量在栈内存中存储的是指向实际对象的**内存地址（引用）**。
  
  - **Object**: 基础对象。
  - **Array**: 数组（如 `[1, 2, 3]`）。
  - **Function**: 函数（可执行代码块）。
  - 常见内置对象还包括 `Date`（日期）、`RegExp`（正则）、`Map`、`Set` 等。
  
  数据类型判断方法
  
  - **`typeof`**：最常用的方法，适合判断基本类型（特例：`typeof null` 会返回 `"object"`）。
  - **`instanceof`**：适合判断具体的引用数据类型（如 `arr instanceof Array`）。
  - **`Object.prototype.toString.call()`**：最精准的类型判断方法（如返回 `[object Array]`）。

## 2. 闭包（必考）

- 定义：**函数嵌套，内部函数访问外部函数变量**
- 作用：**私有化变量、延长变量生命周期、柯里化**
- 缺点：内存泄漏、滥用导致性能问题

## 3. 原型 & 原型链（必问）

- `__proto__` 指向原型
- `prototype` 是函数才有的
- 原型链终点：`null`
- `new` 关键字做了什么
- `instanceof` 原理

## 4. 执行上下文 / 作用域 / 作用域链

- 执行上下文三部分：**变量对象、作用域链、this**
- 作用域：全局、函数、块级（let/const）
- 作用域链：**当前作用域 → 父 → 全局**

## 5. this 指向（高频）

1. 默认绑定 → window
2. 隐式绑定 → 调用者
3. 显式绑定 → call /apply/bind
4. new 绑定 → 新对象
5. 箭头函数 → 继承外层 this

## 6. 异步编程（超级高频）

- 宏任务 / 微任务（执行顺序必背）

  

  同步 → 微任务 → 宏任务

- Promise A+ 规范（三种状态、链式调用、异常穿透）

- `Promise.all / race / allSettled / any` 区别

- async/await 原理 = **Generator + Promise**

- 手写 Promise、手写 Promise.all、防抖节流

## 7. 垃圾回收 & 内存泄漏

- GC 算法：引用计数、标记清除
- 内存泄漏场景：闭包滥用、定时器未清、DOM 引用、全局变量泛滥

## 8. 柯里化、偏函数、compose

- 柯里化：**参数分步接收**
- compose：从右到左执行
- pipe：从左到右执行

# call /apply/bind 都是用来改变函数中 this 指向的。

# 二、三者区别（面试必问）

1. **call**：立即执行函数，参数**逐个传递**
2. **apply**：立即执行函数，参数以**数组形式**传递
3. **bind**：**不立即执行**，返回一个新函数，参数逐个传递

# 三、基本用法

## 1. call

```js
fn.call(thisArg, arg1, arg2, arg3, ...)
```

示例：

```js
function fn(a, b) {
  console.log(this, a, b);
}
fn.call({name: 'call'}, 1, 2); 
// this → {name: 'call'}
```

## 2. apply

```js
fn.apply(thisArg, [arg1, arg2, ...])
```

示例：

```js
fn.apply({name: 'apply'}, [1, 2]);
```

**经典场景：求数组最大值**

```js
const arr = [3,1,4,2];
const max = Math.max.apply(Math, arr);
```

## 3. bind

```js
const newFn = fn.bind(thisArg, arg1, arg2, ...)
newFn(); // 需要手动调用
```

示例：

```js
const newFn = fn.bind({name: 'bind'}, 1, 2);
newFn();
```

**经典场景：React 中绑定 this、定时器绑定 this**

# 四、call、apply、bind 手写原理

## 1. 手写 call

思路：

- 给传入的`thisArg`临时添加一个方法
- 执行这个方法
- 删除临时方法，返回结果

```js
Function.prototype.myCall = function (context, ...args) {
  // 1. 确定this指向，默认为window
  context = context || window;
  // 2. 把当前函数挂到context上
  const fn = Symbol();
  context[fn] = this;
  // 3. 执行函数
  const result = context[fn](...args);
  // 4. 删除临时属性
  delete context[fn];
  return result;
};
```

## 2. 手写 apply

和 call 几乎一样，只是参数是数组：

```js
Function.prototype.myApply = function (context, args) {
  context = context || window;
  const fn = Symbol();
  context[fn] = this;
  const result = context[fn](...(args || []));
  delete context[fn];
  return result;
};
```

## 3. 手写 bind

难点：

- 返回新函数
- 支持**柯里化**（分两次传参）
- 支持`new`关键字

```js
Function.prototype.myBind = function (context, ...args1) {
  const _this = this;
  return function F(...args2) {
    // 判断是否用new调用
    if (this instanceof F) {
      return new _this(...args1, ...args2);
    }
    return _this.apply(context, [...args1, ...args2]);
  };
};
```

# 五、高频

1. **call、apply、bind 区别？**
   - call/apply 立即执行，bind 返回新函数
   - call 传参逐个，apply 传数组
   - bind 可柯里化，支持 new
2. **apply 常用场景？**
   - `Math.max.apply(null, arr)` 求数组最大 / 最小值
   - 类数组转数组 `Array.prototype.slice.apply(arguments)`
3. **bind 返回的函数能被 new 吗？**
   - 可以，此时 bind 绑定的 this 会**被忽略**，this 指向新实例
4. **手写 call 核心思想？**
   - 利用**对象调用函数时 this 指向该对象**，把函数挂载到目标对象上执行

# 六、一句话记忆口诀

> **call 逐个传，apply 数组传，bind 返回函数不执行；**
>
> **原理都是挂对象，执行完再删掉。**

------

# 二、浏览器原理

## 1. 浏览器渲染流程

1. 解析 HTML → DOM 树
2. 解析 CSS → CSSOM 树
3. 合成 → 渲染树（Render Tree）
4. 布局（Layout / Reflow）
5. 绘制（Paint）
6. 合成（Composite）

## 2. 重排（Reflow）重绘（Repaint）

- 重排：**几何属性变化**（宽高、位置）

- 重绘：**颜色、样式**不影响布局

- 如何优化：

  - 读写分离
  - 批量修改 DOM
  - 使用 transform /opacity（直接合成层）

  

## 3. 事件机制

- 捕获 → 目标 → 冒泡
- 事件委托：利用冒泡，减少事件绑定
- `preventDefault`、`stopPropagation` 区别

## 4. 跨域（必问）

- 同源策略：协议、域名、端口一致

- 解决方案：

  1. CORS（最常用）
  2. 代理（webpack/vite）
  3. Nginx 反向代理
  4. JSONP（仅 GET）
  5. postMessage、iframe、websocket

  

## 5. 浏览器存储

- localStorage / sessionStorage / cookie / indexDB
- cookie 属性：`httpOnly`、`secure`、`sameSite`、`domain`、`path`

## 6. 浏览器缓存（高频）

- 强缓存：`Cache-Control`、`Expires`
- 协商缓存：`Last-Modified` / `ETag`

## 7. 安全（XSS / CSRF）

- XSS：注入脚本 → 防御：转义、CSP
- CSRF：伪造请求 → 防御：token、SameSite、Referer

------

# 三、Vue2 / Vue3

## 1. 响应式原理

- Vue2：

  ```
  Object.defineProperty
  ```

  

  缺陷：不能监听数组下标、对象新增属性

- Vue3：

  ```
  Proxy + Reflect
  ```

  

  优势：支持数组、新增删除、懒监听

## 2. diff 算法

- 同层比较，不跨级
- key 作用：**就地复用 + 识别节点**
- 不要用 index 做 key

## 3. 组件通信（必须全会）

1. props / emit
2. v-model
3. provide / inject
4. eventBus
5. vuex / pinia
6. $attrs / $listeners
7. ref / defineExpose

## 4. Vue 生命周期

- Vue2：8 个
- Vue3 setup：onMounted、onUnmounted…

## 5. Vue3 新特性

- Composition API
- 多根节点 Fragment
- Teleport、Suspense
- 响应式：ref /reactive/computed /watch
- Pinia 替代 Vuex

## 6. 性能优化

- v-show / v-if
- v-for 必须 key
- 异步组件
- keep-alive
- 懒加载、图片懒加载
- 防抖节流

------

# 四、工程化：Webpack / Vite

## 1. Webpack 核心

- 一切皆模块
- Loader：转译模块
- Plugin：扩展生命周期
- 热更新 HMR
- Tree-Shaking（ESM 才能）
- Code Split 代码分割

## 2. Vite 为什么快？

- 开发环境：**浏览器原生 ESM**
- 不打包，按需编译
- 依赖预构建
- 生产用 Rollup

## 3. 优化点

- 持久化缓存
- 分包
- 压缩
- 图片优化
- CDN

------

# 五、网络：HTTP / HTTPS

## 1. HTTP 方法

GET / POST / PUT / PATCH / DELETE

幂等性：GET 幂等，POST 不幂等

## 2. 状态码

- 200 成功
- 301/302 重定向
- 304 缓存
- 400 参数、401 未登录、403 无权限、404 不存在
- 500 服务端错误

## 3. HTTPS 原理

- 非对称加密（握手）+ 对称加密（传输）
- CA 证书防篡改

## 4. HTTP1.1 / HTTP2 / HTTP3

- HTTP2：多路复用、头部压缩、二进制帧
- HTTP3：基于 QUIC（UDP）

------

# 六、工程化 & 架构

## 1. 模块化

- ESM、CommonJS、UMD 区别

## 2. 性能优化（通用）

- 网络：压缩、缓存、CDN、懒加载
- 渲染：减少重排重绘
- JS：防抖节流、虚拟列表、WebWorker
- 图片：WebP、懒加载、压缩

## 3. 设计模式

- 单例、观察者、发布订阅、策略、代理、适配器

## 4. Git

- Git 工作流
- merge /rebase 区别
- 冲突解决

------

# 七、手写题

1. 防抖
2. 节流
3. 深拷贝
4. Promise.all
5. 手写 Promise
6. 柯里化
7. call / apply / bind
8. 数组去重、flat、扁平化
9. 发布订阅模式
10. 虚拟 DOM 渲染真实 DOM

------

# 八、项目 & 架构

- 项目难点怎么解决
- 性能优化做了什么
- 权限怎么做（路由权限、按钮权限）
- 封装过什么组件、hooks
- 低代码 / 微前端 / 监控体系（有就加分）

# 一、Vite 面试高频问题

#### 1. Vite 是什么？它和 Webpack 的核心区别是什么？

**核心答案**：

- Vite 是尤雨溪开发的新一代前端构建工具，基于 ES Module（ESM）原生支持，主打 “极速的冷启动”“按需编译”“热模块替换（HMR）”。

- 和 Webpack 的核心区别：

  | 维度          | Vite                                            | Webpack                                                |
| :------------ | :---------------------------------------------- | :----------------------------------------------------- |
  | 启动原理      | 开发环境不打包，直接用原生 ESM 加载模块         | 开发环境先打包所有模块（构建依赖图），再启动 devServer |
  | 启动速度      | 极快（毫秒级），和项目体积无关                  | 较慢，项目越大启动越久                                 |
  | 热更新（HMR） | 按需更新，只编译修改的模块                      | 重新打包修改模块及依赖，体积大时卡顿                   |
  | 构建产物      | 生产环境基于 Rollup 打包（更优的 Tree-Shaking） | 自身打包，需配置优化 Tree-Shaking                      |
  | 适用场景      | 中小型前端项目、Vue/React 现代框架项目          | 大型复杂项目（支持更多定制化、插件生态更全）           |
  
  

#### 2. Vite 为什么开发环境启动这么快？

**核心答案**：

- **跳过打包步骤**：Vite 开发环境不做整体打包，直接利用浏览器原生支持的 ESM（``），让浏览器自己请求模块。
- **按需编译**：只有当浏览器请求某个模块时，Vite 才会对该模块进行编译（如 TS 转 JS、Vue 单文件编译），而非一次性编译所有模块。
- **预构建优化**：对第三方依赖（如 node_modules 里的包）做预构建（基于 esbuild，Go 编写，比 JS 快 10-100 倍），将 CommonJS/UMD 格式转为 ESM，且合并依赖减少请求数。

#### 3. Vite 的预构建（Pre-Bundling）做了什么？为什么需要预构建？

**核心答案**：

- 预构建的核心目标：提升开发体验，解决两个关键问题：

  1. **兼容格式**：第三方依赖大多是 CommonJS/UMD 格式（如 lodash），浏览器原生 ESM 无法直接识别，预构建将其转为 ESM 格式。
2. **减少网络请求**：将多个小依赖合并为一个文件（如把 lodash 的所有子模块合并成一个 `lodash.js`），减少浏览器请求次数（避免 “请求瀑布”）。
  

  
- 触发时机：

  - 首次启动 Vite 时自动执行；
  - 依赖变更（如 package.json 或 lock 文件修改）时自动重新预构建；
  - 可通过 `vite optimize` 命令手动触发。

  

#### 4. Vite 的热更新（HMR）原理是什么？

**核心答案**：

- Vite 的 HMR 基于 WebSocket 实现，且是按需更新，核心流程：

  1. 开发时，Vite 会监控文件变化（基于 chokidar 库）；
2. 当文件修改后，Vite 仅编译该文件（而非整个项目），并通过 WebSocket 通知浏览器；
  3. 浏览器接收到更新通知后，仅替换修改的模块（如 Vue 组件、JS 模块），不刷新整个页面；
4. 对于无法热更新的模块（如入口文件、路由配置），会自动刷新页面。
  
  
  
- 优势：相比 Webpack 的 HMR，Vite 无需重新打包依赖链，更新速度不受项目体积影响。

#### 5. Vite 生产环境为什么用 Rollup 而不是 esbuild？

**核心答案**：

- esbuild 的优势是快，但劣势是：

  1. 生态不够完善，对一些前端高级特性（如 CSS 分割、产物优化、兼容多格式）支持不如 Rollup；
2. Tree-Shaking、代码分割（Code Splitting）的成熟度低于 Rollup；
  

  
- Rollup 专为生产环境的产物优化设计，支持更精细的打包配置、更优的 Tree-Shaking、更完善的插件生态，能生成体积更小、兼容性更好的产物；

- Vite 做了折中：开发环境用 esbuild 做预构建和编译（快），生产环境用 Rollup 做最终打包（优）。

#### 6. Vite 如何处理 CSS？

**核心答案**：

- 开发环境：Vite 会将 CSS 文件作为 ESM 模块加载，通过 `` 标签注入到页面，支持 HMR（修改 CSS 即时生效）；

- 生产环境：

  1. 提取所有 CSS 到单独文件（默认），避免样式内联导致的闪屏；
  2. 支持 CSS 预处理器（Less/Sass/Stylus），无需额外配置（只需安装对应依赖，如 `npm i less -D`）；
  3. 支持 PostCSS（自动识别项目中的 postcss.config.js）；
  4. 支持 CSS Modules（文件名以 `.module.css` 结尾自动启用）。

  

#### 7. Vite 如何实现跨域代理？

**核心答案**：

- Vite 内置代理功能，在 `vite.config.js` 中通过 `server.proxy` 配置，底层基于 http-proxy 库：

```js
// vite.config.js
export default {
  server: {
    proxy: {
      // 匹配以 /api 开头的请求
      '/api': {
        target: 'http://localhost:3000', // 后端接口地址
        changeOrigin: true, // 开启跨域
        rewrite: (path) => path.replace(/^\/api/, '') // 去掉路径中的 /api
      }
    }
  }
}
```

#### 8. Vite 支持哪些框架？如何集成？

**核心答案**：

- 原生支持 Vue（尤雨溪主导，适配性最好）、React、Preact、Svelte 等主流框架；

- 集成方式：

  - Vue：使用 `create-vite` 脚手架选择 Vue 模板（`npm create vite@latest my-vue-app -- --template vue`）；
  - React：选择 React 模板（`npm create vite@latest my-react-app -- --template react`）；
  - 本质是 Vite 通过对应插件（如 `@vitejs/plugin-vue`、`@vitejs/plugin-react`）处理框架特有文件（如 .vue、JSX/TSX）。

  

### 二、Vite 核心工作流程

Vite 的工作流程分为**开发环境**和**生产环境**，核心逻辑如下：

#### 1. 开发环境（vite dev）

Vue/TS/JSXCSS静态资源启动 vite dev预构建依赖（esbuild）启动开发服务器（Koa）浏览器请求入口文件（index.html）Vite 改写 index.html，注入 ESM 支持浏览器请求具体模块（如 App.vue、main.js）模块类型？esbuild 实时编译为 ESM编译为 CSS 模块，注入 style 标签直接返回资源返回编译后的模块给浏览器文件修改Vite 监控到变化触发 HMR，仅更新修改的模块

```
graph TD
    A[启动 vite dev] --> B[预构建依赖（esbuild）]
    B --> C[启动开发服务器（Koa）]
    C --> D[浏览器请求入口文件（index.html）]
    D --> E[Vite 改写 index.html，注入 ESM 支持]
    E --> F[浏览器请求具体模块（如 App.vue、main.js）]
    F --> G{模块类型？}
    G -->|Vue/TS/JSX| H[esbuild 实时编译为 ESM]
    G -->|CSS| I[编译为 CSS 模块，注入 style 标签]
    G -->|静态资源| J[直接返回资源]
    H & I & J --> K[返回编译后的模块给浏览器]
    L[文件修改] --> M[Vite 监控到变化]
    M --> N[触发 HMR，仅更新修改的模块]
    N --> K
```

**关键步骤拆解**：

1. **预构建**：启动时先用 esbuild 处理 node_modules 中的依赖，转 ESM 并合并；
2. **启动 Dev Server**：基于 Koa 搭建本地服务器，监听端口；
3. **处理 index.html**：Vite 将 index.html 作为入口，改写其中的脚本引用为 ESM 格式，并注入 HMR 客户端代码；
4. **按需编译模块**：浏览器请求模块时，Vite 实时编译（如 Vue 单文件解析为 js + css），返回 ESM 模块；
5. **HMR 监控**：通过 chokidar 监控文件变化，触发按需更新。

#### 2. 生产环境（vite build）

Vue/TS/JSXCSS静态资源启动 vite build预构建依赖（esbuild）Rollup 打包（基于 vite 预设配置）处理模块Rollup 插件编译为 ES5/ES6提取 CSS 到单独文件，压缩处理资源（哈希、压缩、Base64 内联）生成产物（dist 目录）产物优化（Tree-Shaking、代码分割、压缩）

```
graph TD
    A[启动 vite build] --> B[预构建依赖（esbuild）]
    B --> C[Rollup 打包（基于 vite 预设配置）]
    C --> D{处理模块}
    D -->|Vue/TS/JSX| E[Rollup 插件编译为 ES5/ES6]
    D -->|CSS| F[提取 CSS 到单独文件，压缩]
    D -->|静态资源| G[处理资源（哈希、压缩、Base64 内联）]
    E & F & G --> H[生成产物（dist 目录）]
    H --> I[产物优化（Tree-Shaking、代码分割、压缩）]
```

**关键步骤拆解**：

1. **预构建**：和开发环境一致，确保依赖是 ESM 格式；
2. **Rollup 打包**：Vite 封装了 Rollup 的默认配置，按框架特性（如 Vue 的单文件处理）加载对应插件；
3. **产物优化**：自动做 Tree-Shaking、代码分割、CSS 提取、资源压缩，生成可部署的静态文件。

### 三、深入理解 Vite 的核心特性

#### 1. 为什么 Vite 不支持低版本浏览器？

Vite 开发环境完全依赖浏览器的**原生 ESM 支持**（ES2015+），低版本浏览器（如 IE11）不支持 ESM，因此开发环境无法运行；

- 生产环境可通过配置 `@vitejs/plugin-legacy` 生成兼容低版本浏览器的产物（注入 polyfill + 转译 ES5）。

#### 2. Vite 的插件机制

Vite 插件兼容 Rollup 插件（大部分 Rollup 插件可直接在 Vite 中使用），同时扩展了开发环境的钩子（如 `configureServer` 用于自定义 Dev Server）；

- 常用插件：

  - `@vitejs/plugin-vue`：处理 Vue 单文件组件；
  - `@vitejs/plugin-react`：处理 React 的 JSX/TSX；
  - `vite-plugin-pwa`：实现 PWA 功能；
  - `vite-plugin-imagemin`：压缩图片资源。

  

#### 3. Vite 的性能优化技巧（面试延伸）

- 开发环境：

  1. 配置 `server.fs.strict` 为 false，放宽文件访问限制；
  2. 用 `optimizeDeps.include` 强制预构建特定依赖；

  

- 生产环境：

  1. 开启 `build.cssCodeSplit` 拆分 CSS（默认开启）；
  2. 配置 `build.rollupOptions` 自定义代码分割；
  3. 使用 `vite-plugin-compression` 开启 gzip/brotli 压缩；
  4. 配置 `build.chunkSizeWarningLimit` 调整包体积警告阈值。

  

### 总结

1. **核心优势**：Vite 基于原生 ESM 实现开发环境 “按需编译”，借助 esbuild 实现极速启动，生产环境基于 Rollup 保证产物优化；
2. **核心区别**：和 Webpack 相比，Vite 开发环境不打包、按需编译，启动 / 热更新速度远快于 Webpack，但生产环境依赖 Rollup 保证产物质量；
3. **关键流程**：开发环境 “预构建 → Dev Server → 按需编译 → HMR”，生产环境 “预构建 → Rollup 打包 → 产物优化”。

掌握这些内容，既能应对面试中关于 Vite 的基础和深度问题，也能在实际工作中理解 Vite 的运行逻辑，快速定位和解决问题。

前端高级开发深入理论的技术面试，不带有八股文，帮我列举一些冷门的题目

前端高级开发的深度面试题，核心是考察**底层原理理解**、**工程化思维**、**问题抽象能力**和**技术决策逻辑**，而非死记硬背的 “八股文”。以下是一批偏向冷门但能真正区分高级开发者的题目，覆盖浏览器底层、构建工具、性能优化、工程化、框架深度等维度，每个题目都附带**考察核心**和**答题思路方向**，帮你理解题目背后想考察的能力：

# 一、浏览器底层 & 运行时

#### 1. 为什么 Chrome 中 setTimeout 的最小延迟是 4ms？能否突破这个限制？

**考察核心**：浏览器事件循环的底层机制、定时器节流策略

**思路方向**：

- 根源：HTML 标准规定，嵌套层级超过 5 层的定时器，或非激活标签页的定时器，最小延迟强制为 4ms（防止定时器抢占主线程）；
- 突破场景：Web Worker 中的 setTimeout 不受 4ms 限制（因为 Worker 有独立的事件循环）；
- 延伸：结合 `requestIdleCallback`/`requestAnimationFrame` 说明不同异步任务的调度优先级。

#### 2. 浏览器的 Preload/Prefetch/Preconnect 有什么本质区别？实际项目中如何精准选型？

**考察核心**：资源加载策略、性能优化的精准性（而非泛泛而谈）

**思路方向**：

- Preload：**强制、高优先级**加载当前页面需要的资源（如关键 JS/CSS），阻塞 onload 但不阻塞渲染，适合 “当前页面必须用且加载慢” 的资源；
- Prefetch：**低优先级**预加载未来页面可能用到的资源（如跳转后的页面 JS），浏览器空闲时加载，不影响当前页面性能；
- Preconnect：仅建立 TCP 连接 + TLS 握手（不加载资源），适合提前预热域名（如 CDN、接口域名）；
- 反例：滥用 Preload 会导致关键资源排队，反而降低性能；Prefetch 加载未使用的资源会浪费带宽（移动端需谨慎）。

#### 3. 解释一下 V8 引擎的 Turbofan 编译器和 Ignition 解释器的协作机制？为什么 V8 要分 “解释执行” 和 “编译执行”？

**考察核心**：JS 引擎底层执行逻辑、性能优化的底层逻辑

**思路方向**：

- 分层执行：Ignition（解释器）负责快速启动，将 JS 转为字节码并执行；Turbofan（编译器）对 “热点代码”（多次执行的函数）做优化编译（转为机器码）；
- 退优化（Deoptimization）：当优化假设失效（如类型变化），Turbofan 会回退到解释执行，避免错误；
- 设计原因：平衡 “启动速度” 和 “执行速度”—— 纯解释执行启动快但运行慢，纯编译执行启动慢但运行快，分层策略兼顾两者。

#### 4. 浏览器的 “合成层（Compositor Layer）” 和 “布局层（Layout Layer）” 有什么区别？如何主动创建合成层优化性能？

**考察核心**：浏览器渲染流水线、性能优化的底层手段

**思路方向**：

- 渲染流水线：Layout（布局）→ Paint（绘制）→ Composite（合成）；
- 布局层：元素发生几何变化（宽高、位置）时，会触发整个布局层的重排（Reflow），成本高；
- 合成层：由 GPU 管理，变化时仅触发合成（Composite），不影响其他层，成本极低；
- 主动创建合成层的方式：`transform: translateZ(0)`、`will-change: transform`、视频 / Canvas 元素、CSS 3D 变换；
- 注意：合成层过多会占用更多 GPU 内存，导致移动端卡顿。

### 二、构建工具 & 工程化（冷门深度）

#### 1. Vite 的 optimizeDeps 预构建时，如何处理带有动态导入（dynamic import）的依赖？

**考察核心**：Vite 预构建的底层逻辑、动态导入的处理机制

**思路方向**：

- 常规预构建：将 CommonJS/UMD 依赖转为 ESM，并合并减少请求；
- 动态导入处理：Vite 会静态分析动态导入的路径（如 `import(`./locales/${lang}.js`)`），若路径是 “有限枚举”（如 lang 只有 zh/en），会预构建所有可能的模块；若路径是 “完全动态”（如 `import(path)`），则无法预构建，运行时按需编译；
- 延伸：可通过 `optimizeDeps.include` 强制预构建动态导入的依赖，或通过 `exclude` 排除不需要预构建的依赖。

#### 2. Rollup 的 Tree-Shaking 为什么比 Webpack 更彻底？两者的实现原理有什么差异？

**考察核心**：模块打包的底层逻辑、Tree-Shaking 实现机制

**思路方向**：

- 核心差异：模块系统的处理方式；
- Rollup：基于 ESM 静态分析，直接删除未被引用的代码（“语句级” Tree-Shaking），且支持 “副作用分析”（sideEffects），能删除空模块；
- Webpack：早期基于 CommonJS 动态分析，只能做 “模块级” Tree-Shaking（删除未引用的模块），后期支持 ESM 但需配置 `mode: production` 才会开启，且对嵌套模块的处理不如 Rollup 彻底；
- 延伸：Webpack 可通过 `webpack-deep-scope-plugin` 增强 Tree-Shaking，Vite 生产环境用 Rollup 正是看中其更优的 Tree-Shaking。

#### 3. 如何设计一个前端工程的 “多环境构建体系”？既要支持开发 / 测试 / 生产，也要支持定制化环境（如客户私有部署）？

**考察核心**：工程化设计能力、环境配置的可扩展性

**思路方向**：

- 分层配置：基础配置（base.config.js）+ 环境配置（dev/test/prod.config.js）+ 定制配置（custom.config.js），通过 `deepmerge` 合并；
- 环境注入：通过 `define`（Vite）/`DefinePlugin`（Webpack）注入环境变量，区分环境；
- 构建脚本：封装 npm script（如 `build:custom --env=customerA`），通过命令行参数动态加载定制配置；
- 产物隔离：不同环境的产物输出到不同目录（如 dist/dev、dist/prod、dist/customerA）；
- 配置校验：通过 schema 校验配置项，避免定制化配置导致的构建错误。

#### 4. 解释一下 esbuild 的 “增量编译” 原理？为什么它比 TSC/ Babel 快几个数量级？

**考察核心**：编译器底层性能优化、增量构建逻辑

**思路方向**：

- 语言层面：esbuild 用 Go 编写（编译型语言），TSC/Babel 用 JS 编写（解释型语言），Go 的执行效率远高于 JS；
- 增量编译：esbuild 会缓存模块的 AST 和编译结果，仅重新编译修改的模块，且缓存是 “内存级”+“磁盘级” 双缓存；
- 并行处理：esbuild 利用 Go 的 goroutine 并行编译多个模块，而 JS 是单线程（需依赖 Worker 实现并行，成本高）；
- 简化逻辑：esbuild 专注于 “编译”（TS→JS、JSX→JS），不做复杂的代码转换（如 Babel 的插件系统），牺牲部分灵活性换性能。

### 三、框架深度 & 运行时（冷门但区分度高）

#### 1. Vue 3 的响应式系统中，为什么 Ref 的值需要通过 .value 访问？能否去掉 .value？

**考察核心**：Vue 响应式的底层实现、JS 语言特性限制

**思路方向**：

- 核心原因：JS 的原始类型（string/number/boolean）是值传递，无法被 Proxy 劫持；

- Ref 本质：将原始类型包装为对象（`{ value: 原始值 }`），通过 Proxy 劫持 `value` 属性的 get/set；

- 能否去掉：

  - 编译时：SFC 中 `` 里的 Ref 可通过编译语法糖省略 `.value`（如 `const count = ref(0)`，模板中直接用 count）；
  - 运行时：纯 JS 中无法去掉，因为原始类型无法被响应式追踪；

  

- 延伸：React 的 useState 无需 `.value`，因为它是 “状态替换” 而非 “响应式劫持”，两者设计理念不同。

#### 2. React 的 Fiber 架构中，“工作单元（Work Unit）” 和 “过期时间（Expiration Time）” 是如何协作实现优先级调度的？

**考察核心**：React 底层调度机制、性能优化逻辑

**思路方向**：

- Fiber 本质：将渲染任务拆分为最小的 “工作单元”（如一个组件的 render），每个单元都有 “过期时间”（优先级）；

- 调度流程：

  1. 调度器（Scheduler）根据过期时间给工作单元排序（高优先级先执行）；
  2. 执行工作单元时，若有更高优先级任务（如用户输入），则暂停当前任务，先执行高优先级任务；
  3. 任务暂停后，通过 “链表” 保存当前工作单元的状态，恢复时从暂停点继续执行；

  

- 过期时间的作用：替代传统的 “同步执行”，实现 “可中断、可恢复” 的渲染，避免主线程阻塞。

#### 3. Vue 3 的 Teleport 组件底层是如何实现的？为什么 Teleport 移动的元素还能保持原组件的响应式？

**考察核心**：Vue 组件渲染机制、响应式绑定逻辑

**思路方向**：

- 实现原理：

  1. Teleport 渲染时，先创建一个 “占位节点” 在原组件位置；
  2. 将子节点挂载到指定的目标容器（如 `body`）；
  3. 组件卸载时，同时删除占位节点和目标容器中的子节点；

  

- 响应式保持：

  Teleport 只是 “DOM 节点的物理移动”，子组件的虚拟 DOM 仍属于原组件的 VNode 树，响应式依赖收集仍在原组件的作用域中，因此数据变化仍能触发更新。

### 四、性能优化 & 问题排查（冷门但实用）

#### 1. 如何定位前端项目中的 “长任务（Long Task）”？长任务会导致哪些用户体验问题？如何拆解长任务？

**考察核心**：性能问题排查能力、主线程阻塞的解决思路

**思路方向**：

- 定位方式：

  1. Chrome DevTools 的 Performance 面板，筛选 “Long Tasks”（执行时间 > 50ms 的任务）；
  2. 使用 `LongTasks API` 监控生产环境的长任务（`new PerformanceObserver((list) => { ... })`）；

  

- 体验问题：长任务阻塞主线程，导致用户交互（点击、输入）延迟、动画卡顿、页面加载慢；

- 拆解方法：

  1. 将长任务拆分为多个短任务（用 `requestIdleCallback` 或 `setTimeout` 分片执行）；
  2. 非关键任务放到 Web Worker 中执行（如大数据处理、复杂计算）；
  3. 懒加载非首屏的组件 / 逻辑，避免启动时执行过多代码。

  

#### 2. 移动端前端项目中，“点击穿透（Click Through）” 的底层原因是什么？如何彻底解决？

**考察核心**：移动端事件机制、兼容性问题的深度理解

**思路方向**：

- 底层原因：

  移动端有 “300ms 延迟”（为了判断是否是双击），且 touch 事件触发早于 click 事件（touchEnd → 300ms → click）；

  若上层元素通过 touchEnd 隐藏，300ms 后 click 事件会穿透到下层元素，触发下层元素的点击；

- 彻底解决：

  1. 禁用 300ms 延迟（``）；
  2. 用 touch 事件替代 click 事件（需处理 touchStart/touchEnd 的防抖）；
  3. 阻止 touch 事件的默认行为（`e.preventDefault()`），避免触发 click 事件；
  4. 用 `pointer-events: none` 临时禁用下层元素的点击。

  

### 总结

1. 高级前端面试的冷门题目，核心考察**底层原理**（如 V8 执行机制、浏览器渲染流水线）和**工程化思维**（如多环境构建、性能问题拆解），而非单纯的 API 记忆；
2. 答题时需体现 “知其然且知其所以然”—— 不仅要说出答案，还要解释背后的设计逻辑、权衡取舍（如 Vite 用 Rollup 做生产打包的原因）；
3. 这类题目没有标准答案，重点考察你的技术视野、问题分析能力和实际项目经验，回答时可结合自己做过的项目案例展开。



# 一、Nginx 反向代理

就是我们刚才说的**商场前台**：

用户 → Nginx（反向代理）→ 后端服务（Tomcat/Node.js/Java/Python）

## 作用

1. 隐藏后端服务器真实地址（安全）
2. 负载均衡（把请求分给多台后端）
3. 统一入口、SSL 证书部署、缓存静态资源
4. 接口路由 / 灰度发布

## 最简 Nginx 反向代理配置

```nginx
# 监听 80 端口，用户访问 http://localhost 就会进入这里
server {
    listen 80;
    server_name localhost;

    # 所有请求都转发给后端的 8080 服务（比如 Tomcat）
    location / {
        # 核心指令：proxy_pass 实现代理转发
        proxy_pass http://127.0.0.1:8080;
        
        # 传递客户端真实IP给后端（必备）
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## 带负载均衡的升级版

```nginx
# 定义后端服务器池
upstream backend_servers {
    server 127.0.0.1:8080;
    server 127.0.0.1:8081;
    server 127.0.0.1:8082;
}

server {
    listen 80;
    location / {
        proxy_pass http://backend_servers; # 转发给集群
    }
}
```

------

# 二、Nginx 正向代理

就是我们刚才说的**代购**：

内网客户端 → Nginx（正向代理）→ 外网网站

## 适用场景

- 内网机器不能直接上网，通过 Nginx 统一访问外网
- 现在基本被 VPN / 网关替代，**Nginx 正向代理已边缘化**

## 极简配置

```nginx
server {
    listen 8080;
    location / {
        resolver 8.8.8.8; # DNS
        proxy_pass http://$host$request_uri;
    }
}
```

⚠️ 缺点：不支持 HTTPS 正向代理（需要额外编译模块），实用性极低。

------

# 三、Nginx 代理核心指令

|            指令             |                 作用                 |
| :-------------------------: | :----------------------------------: |
| `proxy_pass http://ip:port` |  **最核心**：把请求转发给目标服务器  |
|         `upstream`          |    定义后端服务集群，实现负载均衡    |
|     `proxy_set_header`      | 向后端传递客户端真实 IP、Host 等信息 |
|   `proxy_connect_timeout`   |           代理连接超时时间           |

------

# 四、一张图秒懂 Nginx + Proxy

```
【反向代理（工作用）】
用户 ➜ Nginx ➜ 后端服务器
（Nginx 替服务器干活）

【正向代理（几乎不用）】
客户端 ➜ Nginx ➜ 外网网站
（Nginx 替客户端干活）
```

------

### 总结

1. **Nginx = 最流行的反向代理服务器**
2. **Proxy = Nginx 的核心转发能力**
3. **工作中说 Nginx 代理，默认指 反向代理**
4. 核心配置只有一句：`proxy_pass 目标地址`